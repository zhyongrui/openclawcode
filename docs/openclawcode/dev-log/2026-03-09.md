# Development Log - 2026-03-09

## Goal

Start implementation based on the documents in this repository and keep a clear written record of what is being built.

## Inputs Reviewed

The following documents were used as the starting point for implementation:

- `README.md`
- `docs/idea-outline.md`
- `docs/mvp.md`
- `docs/architecture.md`
- `docs/workflows.md`
- `docs/openclaw-strategy.md`

## Initial Implementation Focus

The first implementation step focuses on establishing a minimal but clean code skeleton for the GitHub-driven workflow system.

Planned initial modules:

- workflow contracts and core types
- issue intake model
- execution spec model
- workflow state machine scaffold
- planner, builder, verifier role interfaces
- project structure documentation

## Progress Update

Rebased the effort onto the real fork repository and restored the product docs into:

- `docs/openclawcode/`
- `docs/openclawcode/dev-log/2026-03-09.md`

Reintroduced the first workflow-core slice under `src/openclawcode/`:

- `contracts/` for workflow domain types and GitHub issue mapping
- `workflow/` for stage transitions and verification decisions
- `roles/` for planner, builder, verifier interfaces
- `orchestrator/` for deterministic issue-to-run sequencing
- `testing/` for fake agents and focused orchestration tests
- `index.ts` as the product export surface

Added the second slice for persistence and audit-friendly run tracking:

- expanded `WorkflowRun` with timestamps, attempt counters, stage records, and richer draft PR metadata
- added `src/openclawcode/persistence/store.ts` with a filesystem-backed `FileSystemWorkflowRunStore`
- updated the orchestrator so run state is persisted after each major transition
- added targeted persistence coverage to `src/openclawcode/testing/orchestrator.test.ts`

Added isolated validation entrypoints for this product slice:

- `vitest.openclawcode.config.mjs`
- `tsconfig.openclawcode.json`

Not implemented yet in this fork snapshot:

- worktree isolation
- queue persistence and adapter ingress
- OpenClaw runtime bridge
- PR publishing to GitHub
- human checkpoint policy beyond the current state machine

## Validation

Validated the current workflow-core and persistence slices locally with:

- `npx --yes -p vitest@4.0.18 vitest run --config vitest.openclawcode.config.mjs`
- `npx --yes -p typescript@5.9.3 -p @types/node@25.3.5 bash -lc 'tmp_bin=$(printf "%s\n" "$PATH" | tr ":" "\n" | sed -n "2p"); tmp_root=$(dirname "$tmp_bin"); tsc --noEmit --target es2023 --module NodeNext --moduleResolution NodeNext --strict --skipLibCheck --typeRoots "$tmp_root/@types" src/openclawcode/contracts/types.ts src/openclawcode/contracts/github.ts src/openclawcode/contracts/index.ts src/openclawcode/workflow/state-machine.ts src/openclawcode/workflow/index.ts src/openclawcode/persistence/store.ts src/openclawcode/persistence/index.ts src/openclawcode/roles/interfaces.ts src/openclawcode/roles/index.ts src/openclawcode/orchestrator/run.ts src/openclawcode/orchestrator/index.ts src/openclawcode/testing/fakes.ts src/openclawcode/testing/index.ts src/openclawcode/index.ts'`

## Notes

This log is tracking the actual state of the fork repository. Future slices should update it immediately after validation so the implementation record stays trustworthy.
