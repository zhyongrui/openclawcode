# 2026-03-21

- Continued the remaining blueprint-first handoff hardening in `/home/zyr/pros/openclawcode`.
  - added structured workflow handoff entries to `WorkflowRun` artifacts so runs can now persist:
    - stage-gate decisions captured at run start
    - rerun requests
    - runtime reroute requests
    - manual takeover context
    - manual resume context
    - suitability overrides
  - mirrored the same handoff entries into operator status snapshots so chat/UI surfaces can consume a single machine-readable ledger instead of reconstructing it from scattered fields
  - `/occode-status` now adds a compact top-level `Handoffs:` summary line when tracked status snapshots include handoff entries
  - updated `docs/openclawcode/openclaw-capability-mapping.md` to mark `persist all handoff and override decisions in workflow run artifacts` as landed while leaving broader runtime-aware handoff work open

- Validation:
  - `pnpm exec vitest run src/openclawcode/testing/run-issue.test.ts --pool threads`
  - `pnpm exec vitest run src/openclawcode/testing/openclaw-plugin-store.test.ts --pool threads -t "persists structured workflow run snapshots alongside issue statuses"`
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "shows handoff summary through /occode-status|shows suitability override details through /occode-status"`

- Continued chat-native setup hardening in `/home/zyr/pros/openclawcode`.
  - the plugin service now proactively starts one host-side GitHub device-login
    flow on startup when:
    - the plugin has a concrete configured chat target
    - host GitHub auth is still missing
    - that chat target does not already have a saved setup session
  - the service now pushes the verification URL and one-time code to the
    configured chat target without waiting for `/occode-setup`
  - when exactly one repo maps to that target, the proactive session also pins
    the repo so auth completion can flow straight into repo validation and
    bootstrap
  - updated:
    - `docs/openclawcode/chat-setup-onboarding.md`
    - `docs/openclawcode/development-plan-chat-native-autonomy.md`

- Validation:
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "proactively starts GitHub auth on service start for configured chat targets|does not proactively restart GitHub auth when the target chat already has a setup session|does not proactively start GitHub auth when host auth is already ready"`

- Continued the provider-routing completion slice in `/home/zyr/pros/openclawcode`.
  - role-routing artifacts now persist explicit stage routing so operators can
    see which provider/role pair owns planning, building, verifying, review,
    and post-completion stages
  - role-routing artifacts now support role-specific fallback chains layered on
    top of the global fallback chain
  - workflow run snapshots now mirror both role-stage coverage and stage-route
    entries so runtime artifacts stay aligned with the repo-local routing plan
  - CLI and chat routing summaries now print stage routing directly
  - updated `docs/openclawcode/openclaw-capability-mapping.md` to mark:
    - `support mixed-mode routing by stage`
    - `support fallback by role`
    as landed while leaving `per-stage runtime steering` open

- Validation:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "persists a provider-neutral role routing plan with mixed Codex and Claude assignments"`
  - `pnpm exec vitest run src/openclawcode/testing/run-issue.test.ts --pool threads -t "captures blueprint-first planning context from repo-local artifacts"`
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "shows provider-role routing through /occode-routing|updates provider-role routing through /occode-route-set"`

- Continued the stage-handoff completion slice in `/home/zyr/pros/openclawcode`.
  - `openclaw code run` now supports an explicit planning checkpoint through:
    - `--require-plan-approval`
    - `--approve-plan-digest <digest>`
  - workflow runs can now pause at `awaiting-plan-approval` after planning,
    persist a stable `planDigest`, and resume only when the rerun supplies the
    exact same digest
  - workflow JSON output now exposes top-level plan-review fields so CLI and
    future chat surfaces can inspect approval state without unpacking nested
    run artifacts
  - updated:
    - `docs/openclawcode/openclaw-capability-mapping.md`
    - `docs/openclawcode/run-json-contract.md`

- Validation:
  - `pnpm exec vitest run src/openclawcode/testing/run-issue.test.ts --pool threads -t "halts after planning when explicit plan approval is required|continues into build when the current plan digest is explicitly approved|re-halts when a supplied approval digest no longer matches the current plan"`
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "forwards plan-approval metadata into workflow runs|prints plan-review fields when a run is waiting for plan approval"`

- Continued the same stage-handoff slice in `/home/zyr/pros/openclawcode`.
  - `openclaw code run` now also supports `--plan-edit-file <path>` so an
    operator can apply a repo-local JSON patch to the planned `ExecutionSpec`
    before code execution starts
  - edited plans now persist audit metadata on the workflow run, including:
    - edited field list
    - actor
    - note
    - source path
    - applied timestamp
  - when plan approval is also required, the approval digest is now generated
    from the edited plan rather than the original planner output
  - updated:
    - `docs/openclawcode/openclaw-capability-mapping.md`
    - `docs/openclawcode/run-json-contract.md`

- Validation:
  - `pnpm exec vitest run src/openclawcode/testing/run-issue.test.ts --pool threads -t "applies a plan edit patch before code execution|recomputes the plan digest after a plan edit when approval is required"`
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "forwards a parsed plan edit patch into workflow runs|prints plan-edit fields when a run carries edited planning metadata"`

- Continued the same stage-handoff slice in `/home/zyr/pros/openclawcode`.
  - added `openclaw code reroute-run` so CLI operators can now mirror the
    chat-native runtime reroute flow against operator-managed state
  - the command now handles three operator states directly:
    - active runs: record or update a deferred reroute that will auto-rerun if
      the current run ends in `failed`
    - queued runs: rewrite the queued coder/verifier runtime override before
      execution starts
    - tracked failed or paused snapshots: queue a reroute rerun from the
      stored snapshot, including `awaiting-plan-approval`
  - non-paused terminal snapshots are blocked explicitly so the CLI does not
    silently rerun from completed states
  - updated:
    - `src/cli/program/register.code.ts`
    - `docs/openclawcode/openclaw-capability-mapping.md`

- Validation:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "queues a reroute rerun from a failed tracked snapshot|queues a reroute rerun from an awaiting-plan-approval snapshot|updates the queued runtime override before execution starts|records a deferred reroute when the current run is still active|blocks reroute requests from non-paused tracked stages"`

- Continued the backlog/decomposition completion slice in `/home/zyr/pros/openclawcode`.
  - `openclaw code discover-work-items` now also synchronizes discovered
    evidence into `.openclawcode/work-items.json` so discovery results become
    first-class repo-local backlog entries instead of living only in
    `discovery-work-items.json`
  - discovered backlog sync preserves existing discovered issue links when the
    same fingerprint reappears and marks no-longer-active discovered items as
    `superseded`
  - updated `docs/openclawcode/openclaw-capability-mapping.md` to mark
    broader discovered decomposition as landed

- Validation:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "discovers a missing work-item artifact from an agreed blueprint|discovers stale work-item drift after the blueprint changes|discovers setup-check regressions, provider pauses, and upstream sync drift"`

- Continued the discovery completion slice in `/home/zyr/pros/openclawcode`.
  - runtime discovery now inspects tracked operator issue snapshots for the
    current repo and emits discovered backlog entries for:
    - failed tracked runs
    - tracked runs blocked in `changes-requested`
  - this extends discovery beyond static artifact drift/setup state into live
    operator runtime state
  - updated `docs/openclawcode/openclaw-capability-mapping.md` to mark broader
    runtime discovery as landed

- Validation:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "discovers setup-check regressions, provider pauses, and upstream sync drift"`
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "discovers a missing work-item artifact from an agreed blueprint|discovers stale work-item drift after the blueprint changes"`

- Continued the provider-role completion slice in `/home/zyr/pros/openclawcode`.
  - added repo-local `.openclawcode/runtime-steering.json` support so
    operators can persist explicit per-stage runtime overrides for:
    - `building`
    - `verifying`
  - added CLI surfaces:
    - `openclaw code runtime-steering-show`
    - `openclaw code runtime-steering-set`
  - workflow runtime routing now applies those stage overrides before build and
    verification, and agent-backed builder/verifier execution reuses the
    persisted `stage-steering` selection instead of falling back to env/default
    routing
  - updated `docs/openclawcode/openclaw-capability-mapping.md` to mark
    per-stage runtime steering as landed

- Validation:
  - `pnpm exec vitest run src/openclawcode/roles/agent-backed.test.ts --pool threads -t "stage-steering|adapter defaults"`
  - `pnpm exec vitest run src/openclawcode/testing/run-issue.test.ts --pool threads -t "runtime routing selections|per-stage runtime steering"`
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "runtime steering overrides|provider-neutral role routing plan"`

- Continued the stage-handoff completion slice in `/home/zyr/pros/openclawcode`.
  - per-stage runtime steering now records structured `runtime-steering`
    handoff entries on each workflow run
  - operator status snapshots now persist a `runtimeRoutingSummary` derived
    from the live run so chat/status surfaces keep the selected runtime visible
    after handoff
  - run notifications and `/occode-status` now surface both:
    - runtime routing summaries
    - unified handoff counts, including `runtime-steering`
  - updated `docs/openclawcode/openclaw-capability-mapping.md` to mark
    runtime-aware handoff as landed

- Validation:
  - `pnpm exec vitest run src/openclawcode/testing/run-issue.test.ts --pool threads -t "per-stage runtime steering"`
  - `pnpm exec vitest run src/openclawcode/testing/openclaw-plugin-store.test.ts src/openclawcode/testing/openclaw-plugin-integration.test.ts --pool threads -t "structured workflow run snapshots|formats run status updates"`
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "shows handoff summary through /occode-status"`

- Continued the chatops completion slice in `/home/zyr/pros/openclawcode`.
  - added chat commands:
    - `/occode-runtime-steering`
    - `/occode-runtime-steering-set`
  - chat operators can now inspect repo-local stage overrides and set or clear
    runtime steering for `building` / `verifying` without dropping to the CLI
  - updated `docs/openclawcode/openclaw-capability-mapping.md` to mark
    blueprint-first chatops as complete

- Validation:
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "runtime steering through /occode-runtime-steering|runtime steering through /occode-runtime-steering-set|shows handoff summary through /occode-status"`
  - `pnpm exec vitest run src/openclawcode/testing/run-issue.test.ts src/openclawcode/testing/openclaw-plugin-store.test.ts src/openclawcode/testing/openclaw-plugin-integration.test.ts --pool threads -t "per-stage runtime steering|structured workflow run snapshots|formats run status updates"`

- Closed the proof-operationalization slice in `/home/zyr/pros/openclawcode`.
  - recorded a dated refreshed sync-branch blueprint-first proof for:
    - `sync/upstream-2026-03-18 @ 4f47c82f808164a6063e0a4ba28b9978d7ae30d0`
  - recorded a dated long-lived baseline blueprint-first proof for:
    - `main @ 3a7cc32af268f1313d6b762dc9cc99a7675f508e`
  - updated:
    - `docs/openclawcode/proof-matrix.md`
    - `docs/openclawcode/operator-proof-routine.md`
    - `docs/openclawcode/openclaw-capability-mapping.md`

- Validation:
  - refreshed sync branch:
    - `pnpm exec vitest run extensions/openclawcode/index.test.ts src/openclawcode/testing/run-issue.test.ts src/openclawcode/testing/openclaw-plugin-integration.test.ts --pool threads -t "shows provider-role routing through /occode-routing|updates provider-role routing through /occode-route-set|captures runtime routing selections before build and verification|formats run status updates for chat notifications"`
  - long-lived baseline:
    - `pnpm exec vitest run extensions/openclawcode/index.test.ts src/openclawcode/testing/run-issue.test.ts src/openclawcode/testing/openclaw-plugin-store.test.ts src/openclawcode/testing/openclaw-plugin-integration.test.ts --pool threads -t "shows provider-role routing through /occode-routing|updates provider-role routing through /occode-route-set|shows repo-local runtime steering through /occode-runtime-steering|updates repo-local runtime steering through /occode-runtime-steering-set|shows handoff summary through /occode-status|per-stage runtime steering|structured workflow run snapshots|formats run status updates"`
