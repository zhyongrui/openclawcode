## Temporary Public Ingress Automation

Added a small operator-facing workflow for the current constraint where
`openclawcode` cannot use a truly fixed public domain yet and must rely on
anonymous `trycloudflare` URLs.

Implemented:

- `scripts/openclawcode-webhook-tunnel.sh`
  - `start`
    - starts a `cloudflared` quick tunnel
    - waits for the `https://*.trycloudflare.com` URL
    - updates the GitHub webhook URL automatically
  - `run`
    - runs the tunnel in the foreground for dedicated terminals or `tmux`
  - `sync-hook`
    - rewrites the GitHub webhook URL to the current tunnel host
  - `status`, `stop`, `restart`, `print-url`
- `docs/openclawcode/webhook-operations.md`
  - runbook for temporary public ingress mode
  - startup order and operator commands
- `docs/openclawcode/README.md`
  - linked the new webhook operations runbook from the index

Hardening details:

- the script now auto-sources `~/.openclaw/openclawcode.env` by default
- webhook sync writes both:
  - the current quick-tunnel URL
  - the local GitHub webhook secret, when present
- this avoids a regression where rewriting only the URL caused real GitHub
  deliveries to fail signature verification with `401`

Real validation:

- started the local gateway again on port `18789`
- established a new quick tunnel at:
  - `https://portsmouth-securities-creatures-meal.trycloudflare.com`
- updated GitHub webhook `600049842` for
  `zhyongrui/openclawcode` to the new tunnel URL
- replayed the real issue webhook delivery for issue `#40`
- GitHub reported the replay as:
  - `status: OK`
  - `status_code: 202`
  - delivered at `2026-03-11T02:31:02Z`

Operational note:

- anonymous `trycloudflare` hostnames are still ephemeral
- the new script removes the manual GitHub webhook editing step, but not the
  temporary-domain limitation itself
- issue `#40` currently has stale local workflow state from earlier interrupted
  runs, so the next clean end-to-end workflow validation should use a fresh real
  GitHub issue rather than reusing `#40`

## Webhook Runtime Hardening

Closed two runtime gaps that only showed up when replaying real GitHub issue
webhook deliveries against the plugin route.

Implemented:

- normalized the GitHub repository owner field in
  `src/integrations/openclaw-plugin/chatops.ts`
  - real GitHub issue payloads can send `repository.owner` as an object with a
    `login` field instead of a plain string
  - added `readGitHubRepositoryOwner(...)`
  - repo matching now accepts both owner shapes
- hardened `extensions/openclawcode/index.ts`
  - webhook intake now fails closed with `invalid-repository-owner` when the
    owner shape is unreadable
  - issue-webhook notifications now use `scheduleNotification(...)` so the
    webhook response can return without waiting on outbound chat delivery

Why this mattered:

- the real webhook payload for issue `#40` previously failed because the plugin
  assumed `repository.owner` was always a string
- awaiting chat delivery inside the webhook route risked making GitHub observe
  timeouts even after local issue intake had already succeeded

Added coverage in `extensions/openclawcode/index.test.ts`:

- accepts the real GitHub `repository.owner` object shape
- returns webhook acceptance without waiting for chat notification delivery

Validated locally with:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`

## Operator Inbox Command: `/occode-inbox`

Added a small operator-facing inbox view for the bundled OpenClaw plugin so the
chat side can show pending approvals and recent workflow activity without
opening the raw plugin state file.

Implemented in `extensions/openclawcode/index.ts`:

- new `/occode-inbox` command
- optional repo argument:
  - `/occode-inbox owner/repo`
  - or `/occode-inbox` when exactly one repo is configured
- summarizes, for the selected repo:
  - pending approvals waiting on chat action
  - the current running issue
  - queued issue runs
  - the newest completed tracked runs from persisted snapshots

Why this matters:

- `approve` mode now has a direct chat-native operator overview
- the human no longer needs to remember exact issue numbers just to see what is
  waiting to be approved or what recently completed
- this is the first compact "run ledger" slice from the development plan,
  implemented without changing the workflow core

Added coverage in `extensions/openclawcode/index.test.ts`:

- `/occode-inbox` reports pending, running, queued, and recent-completed items
- `/occode-inbox` returns a compact empty summary when nothing is tracked

## GitHub Review Status Healing

Extended the existing GitHub snapshot reconciliation path so operator commands
can heal review outcomes, not only merged PRs.

Implemented:

- `src/openclawcode/github/client.ts`
  - added `fetchLatestPullRequestReview(...)`
  - normalizes the newest meaningful review state from GitHub as either:
    - `approved`
    - `changes-requested`
- `src/integrations/openclaw-plugin/github-sync.ts`
  - if GitHub reports a newer `CHANGES_REQUESTED` review, the local snapshot now
    heals to `changes-requested`
  - if the local snapshot is already `changes-requested` and GitHub later shows
    a newer `APPROVED` review, the snapshot heals back to
    `ready-for-human-review`

Why this matters:

- `/occode-status` and `/occode-sync` now reflect a real human review loop on
  GitHub instead of only the merge outcome
- the plugin state stays useful even when review actions happen outside the
  local workflow process

Added coverage:

- `src/openclawcode/testing/openclaw-plugin-github-sync.test.ts`
  - review changes requested
  - review approved after changes requested
- `extensions/openclawcode/index.test.ts`
  - `/occode-status` heals a tracked issue to `changes-requested` from GitHub

## Rerun Continuity: Reuse Existing Open Pull Request

Closed one important request-changes continuity gap in the workflow engine:
rerunning the same issue branch no longer attempts to open a second pull
request when an open PR already exists for that branch.

Implemented:

- `src/openclawcode/github/client.ts`
  - added `findOpenPullRequestForBranch(...)`
  - queries GitHub for an open PR matching the issue branch and base branch
- `src/openclawcode/app/run-issue.ts`
  - before draft PR publication, checks whether the issue branch already has an
    open PR on GitHub
  - if one exists, reuses its PR number and URL in the new workflow run
  - records a history note:
    - `Reusing existing pull request: <url>`
  - skips fresh PR creation in that case

Why this matters:

- request-changes follow-up runs now preserve branch and PR continuity
- the workflow stops fighting GitHub with duplicate PR creation attempts for the
  same branch
- later review and merge automation can continue operating on the original PR
  thread

Added coverage:

- `src/openclawcode/testing/run-issue.test.ts`
  - reruns reuse an existing open PR and do not call the publisher again

## GitHub Status Healing: Closed Without Merge

Closed another lifecycle-consistency gap in the GitHub reconciliation path:
the plugin now notices when a tracked pull request was closed on GitHub without
being merged.

Implemented in `src/integrations/openclaw-plugin/github-sync.ts`:

- if GitHub reports `state: closed` and `merged: false` for a tracked PR, the
  snapshot now heals to:
  - stage: `escalated`
  - status summary explaining that the PR was closed without merge

Why this matters:

- `/occode-status` and `/occode-sync` no longer keep showing a stale
  ready-for-review state after a human manually closes the PR
- operators can distinguish:
  - merged successfully
  - changes requested
  - approved again
  - closed without merge

Added coverage:

- `src/openclawcode/testing/openclaw-plugin-github-sync.test.ts`
  - closed-unmerged PR escalates the snapshot
- `extensions/openclawcode/index.test.ts`
  - `/occode-status` heals to `Stage: Escalated` for a closed-unmerged PR

## GitHub Lifecycle Webhook Intake: PR and Review Events

Started the next implementation slice from the refreshed development plan by
closing the biggest remaining freshness gap in chatops: review and merge
outcomes no longer have to wait for `/occode-status` or `/occode-sync` to heal
tracked issue state.

Implemented:

- `src/integrations/openclaw-plugin/github-lifecycle.ts`
  - new adapter-local lifecycle rules for:
    - `pull_request_review` approved
    - `pull_request_review` changes requested
    - `pull_request` merged
    - `pull_request` closed without merge
- `src/integrations/openclaw-plugin/store.ts`
  - workflow status snapshots now also persist:
    - `notifyChannel`
    - `notifyTarget`
  - GitHub delivery records can now persist:
    - tracked pull request number
  - added lookup by repo + pull request number so lifecycle events can recover
    the tracked issue snapshot directly
- `extensions/openclawcode/index.ts`
  - webhook route now accepts:
    - `issues`
    - `pull_request`
    - `pull_request_review`
  - delivery-id deduplication now works for lifecycle events as well as issue
    intake
  - tracked PR and review events update the issue snapshot immediately and
    schedule a chat notification without waiting on operator polling
  - lifecycle notifications resolve their destination in this order:
    1. the snapshot's persisted chat target
    2. the repo binding
    3. the repo's default notification target

Why this matters:

- the main operator loop is no longer issue-event push plus PR/review polling
- once a tracked PR is reviewed or closed on GitHub, chat-visible state can
  move immediately instead of waiting for a manual sync command
- keeping the original chat target in the snapshot lets the same issue continue
  reporting back to the conversation that started it

Added coverage:

- `extensions/openclawcode/index.test.ts`
  - approved review events update a tracked snapshot and notify the original
    chat target
  - changes-requested review events update a tracked snapshot and fall back to
    the repo binding when the snapshot has no saved target
  - merged PR events update a tracked snapshot and repeated deliveries are
    rejected as `duplicate-delivery`
  - closed-without-merge PR events escalate the tracked snapshot immediately
- `src/openclawcode/testing/openclaw-plugin-store.test.ts`
  - persisted workflow snapshots keep their notify target metadata
  - snapshots can be looked up by tracked pull request number

Validated locally with:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts src/openclawcode/testing/openclaw-plugin-store.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`

Still pending for this slice:

- replaying a real GitHub `pull_request` or `pull_request_review` delivery
  through the live webhook route for end-to-end runtime validation

## Request-Changes Rerun Control and Artifact Continuity

Closed the next planned review-loop gap so request-changes follow-up work is now
an explicit operator action instead of an implicit rerun.

Implemented:

- `extensions/openclawcode/index.ts`
  - added `/occode-rerun`
  - reruns now require an existing tracked issue snapshot
  - rerun notifications prefer:
    1. the current chat target
    2. the tracked snapshot target
    3. the repo binding/default target
- `src/integrations/openclaw-plugin/chatops.ts`
  - run requests and CLI argv generation now carry explicit rerun context
  - added CLI flags for:
    - prior run id / stage
    - rerun reason / request time
    - latest review decision / time / summary / URL
- `src/openclawcode/app/run-issue.ts`
  - workflow runs now persist `rerunContext`
  - rerun history now records:
    - rerun reason
    - prior run linkage
    - latest review metadata
- `src/integrations/openclaw-plugin/store.ts`
  - workflow status snapshots now preserve latest review metadata from rerun
    artifacts so the next rerun keeps the same context
- `src/commands/openclawcode.ts`
  - `openclaw code run --json` now exposes stable top-level rerun fields

Why this matters:

- request-changes is now a first-class control path in chatops rather than a
  manual recreation of the original run
- reruns preserve issue, branch, and PR continuity while carrying the latest
  GitHub review reason into the new artifact
- downstream tooling can inspect rerun intent from JSON output without parsing
  free-form history text

Added coverage:

- `extensions/openclawcode/index.test.ts`
  - `/occode-rerun` queues a rerun with persisted review metadata
  - current-chat vs snapshot-target notification routing
  - missing tracked-run rejection
- `src/openclawcode/testing/run-issue.test.ts`
  - rerun context and latest review metadata persist into the saved run artifact
- `src/commands/openclawcode.test.ts`
  - rerun CLI flags flow into the workflow request
  - stable top-level rerun JSON fields are exposed

Validated locally with:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts src/openclawcode/testing/run-issue.test.ts src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`

Build note:

- `pnpm build` is still failing outside this slice on
  `src/gateway/credential-planner.ts`
  - error:
    - `TS2339: Property 'enabled' does not exist on type 'GatewayRemoteConfig'`

## Operator Ledger Expansion: `/occode-inbox`

Closed the next operator-facing visibility gap so chat now has a compact ledger
view of the most important lifecycle state without opening raw plugin files.

Implemented:

- `src/integrations/openclaw-plugin/store.ts`
  - workflow snapshots now persist:
    - rerun lineage:
      - reason
      - requested time
      - prior run id
      - prior stage
    - last notification metadata:
      - channel
      - target
      - timestamp
      - sent/failed status
      - last error when delivery fails
- `extensions/openclawcode/index.ts`
  - `/occode-inbox` now renders:
    - active pending/running/queued items
    - rerun context for queued/running reruns
    - recent lifecycle events from the GitHub delivery ledger
    - final disposition for recent tracked issues
    - last notification target/timing for recent tracked issues
  - lifecycle webhook notifications now write notification metadata back into
    the tracked issue snapshot
  - completed workflow notifications now do the same, so ledger output stays
    aligned with the actual chat target

Why this matters:

- operators can answer "what changed?" and "where did it notify?" from chat
  without reading `chatops-state.json`
- rerun chains are no longer implicit; they are visible in both workflow
  artifacts and the chat ledger
- external lifecycle updates now have a compact operator-facing trail instead of
  being visible only through raw delivery records

Added coverage:

- `extensions/openclawcode/index.test.ts`
  - lifecycle notifications persist notification metadata
  - `/occode-inbox` renders rerun lineage, lifecycle events, final disposition,
    and notification info
  - empty ledger output stays compact
- `src/openclawcode/testing/openclaw-plugin-store.test.ts`
  - structured snapshots persist rerun lineage and notification metadata
  - explicit snapshot notification writes are persisted for ledger use

Validated locally with:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts src/openclawcode/testing/openclaw-plugin-store.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`

Build note:

- `pnpm build` still fails on the same unrelated repository error:
  - `src/gateway/credential-planner.ts`
  - `TS2339: Property 'enabled' does not exist on type 'GatewayRemoteConfig'`

## Live `/occode-rerun` Validation and Reusable Branch Refresh Hardening

Closed the planned fresh rerun gap by validating a real `/occode-rerun` path
against issue `#40`, then hardened the reusable worktree behavior based on what
that live run exposed.

Validated live:

- triggered the real `/occode-rerun #40` handler against the live plugin store
  without bypassing the chatops command logic
- confirmed the live runner picked up the queued rerun and created run artifact:
  - `.openclawcode/runs/zhyongrui-openclawcode-40-1773240869234.json`
- confirmed rerun lineage persisted into the tracked snapshot:
  - `rerunReason: Run is currently at stage planning.`
  - `rerunPriorRunId: zhyongrui-openclawcode-40-1773195881404`
  - `rerunPriorStage: planning`
- confirmed reusable issue-branch continuity reached a real draft PR:
  - `PR #43`
  - `https://github.com/zhyongrui/openclawcode/pull/43`
- confirmed the live run completed to:
  - `stage: ready-for-human-review`
  - verification decision: `approve-for-human-review`
  - notification delivery recorded as `sent`

What the live rerun exposed:

- issue branch `openclawcode/issue-40` already had one unique historical commit
- the first reusable-worktree hardening pass correctly preserved that branch
  history
- but preserving the branch without integrating the latest `main` reopened
  `PR #43` from a stale branch tip
- GitHub reported that reopened PR as:
  - `mergeable_state: dirty`

Implemented follow-up hardening:

- `src/openclawcode/worktree/manager.ts`
  - reusable worktree refresh now distinguishes three cases:
    - issue branch is behind base with no unique commits:
      - reset to latest base
    - issue branch already contains latest base:
      - clean and reuse current head
    - issue branch and base both advanced:
      - merge latest base into the reusable issue branch before the rerun
  - conflicting base merges now abort cleanly instead of leaving the worktree
    in a half-merged state
- `src/openclawcode/testing/worktree.test.ts`
  - added regression coverage for:
    - merging the latest base into a reusable issue branch while preserving its
      committed changes
    - aborting reusable-branch refresh cleanly on merge conflict

Validated locally with:

- `pnpm exec vitest run src/openclawcode/testing/worktree.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`

Result:

- the remaining live workflow gap is no longer fresh rerun validation
- the remaining live workflow gap is now one separate low-risk merged-PR
  validation on the live route

## Rerun Semantics: Prefer Current Escalation Summary Over Stale Review Text

Closed one follow-up gap that would have made the next real `/occode-rerun`
validation misleading after a pull request had already been closed without
merge.

Implemented:

- `extensions/openclawcode/index.ts`
  - tightened `/occode-rerun` reason selection
  - review text still wins for genuinely review-driven states:
    - `changes-requested`
    - `ready-for-human-review` when the latest review was `approved`
  - for non-review-driven states such as `escalated`, reruns now prefer the
    current status summary instead of stale `latestReviewSummary`

Why this mattered:

- after the real lifecycle replay on `PR #37`, the tracked snapshot for issue
  `#36` ended in `stage: escalated` because the PR was closed without merge
- the snapshot correctly preserved the last approved review metadata, but the
  rerun command would previously have reused that stale review comment as the
  rerun reason
- that would have produced a misleading rerun artifact for the next live
  validation slice

Added coverage:

- `extensions/openclawcode/index.test.ts`
  - `/occode-rerun` now prefers the current escalated status summary while
    still preserving the last review metadata in `rerunContext`

Validated locally with:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`

## Live Lifecycle Replay: Real Review and Close-Without-Merge

Completed the next planned validation slice against the real local route and
the real GitHub repo webhook by replaying actual review and pull-request
lifecycle events on `PR #37` for `issue #36`.

Validated sequence:

1. a second GitHub account submitted a real `CHANGES_REQUESTED` review on
   `https://github.com/zhyongrui/openclawcode/pull/37`
2. the local plugin route accepted the resulting `pull_request_review`
   `submitted` delivery
3. the tracked snapshot for `zhyongrui/openclawcode#36` moved to:
   - `stage: changes-requested`
   - `latestReviewDecision: changes-requested`
   - `latestReviewSummary: openclawcode live replay: request changes`
4. the same second account then submitted a real `APPROVED` review on the same
   PR
5. the tracked snapshot moved back to:
   - `stage: ready-for-human-review`
   - `latestReviewDecision: approved`
   - `latestReviewSummary: openclawcode live replay: approve`
6. the PR was then manually closed without merge
7. the real `pull_request` `closed` delivery moved the tracked snapshot to:
   - `stage: escalated`
   - summary explaining that GitHub closed the PR without merge

Observed local state after replay:

- `zhyongrui/openclawcode#36` snapshot now keeps:
  - `pullRequestNumber: 37`
  - `pullRequestUrl: https://github.com/zhyongrui/openclawcode/pull/37`
  - last notification metadata for the live replay events
- the local delivery ledger recorded three accepted lifecycle events:
  - `review-changes-requested`
  - `review-approved`
  - `pull-request-closed-without-merge`

Observed GitHub-side lifecycle events:

- review delivery:
  - `pull_request_review`
  - `action: submitted`
  - `status_code: 202` confirmed for the approved replay
- close delivery:
  - `pull_request`
  - `action: closed`
  - `status_code: 202`
- PR end state:
  - `state: closed`
  - `merged: false`
  - `closed_at: 2026-03-11T14:22:03Z`

Operator caveat confirmed in practice:

- the author of a GitHub PR cannot submit `Request changes` on their own pull
  request
- a second GitHub reviewer account is required to exercise the
  `changes-requested` path in a real replay

Result:

- live `pull_request_review` webhook handling is now proven for both:
  - `changes requested`
  - `approved`
- live `pull_request` webhook handling is now proven for:
  - `closed without merge`
- the next remaining live workflow gap is no longer review freshness
- the next remaining live workflow gap is one fresh rerun path through
  `/occode-rerun` plus a separately chosen low-risk merged-PR validation

## Live Replay Preflight Fixes: Webhook Event Sync and PR-Link Recovery

While preparing the next planned slice, a real operator preflight exposed two
remaining local blockers ahead of PR/review lifecycle replay.

Implemented:

- `scripts/openclawcode-webhook-tunnel.sh`
  - `sync-hook` now keeps the GitHub webhook event set aligned with the plugin
    route instead of only rewriting the URL and secret
  - default managed event set:
    - `issues`
    - `pull_request`
    - `pull_request_review`
  - added `OPENCLAWCODE_GITHUB_HOOK_EVENTS` override
  - hardened tunnel process detection so `status`, `start`, and `stop` can
    recover when the pidfile is stale but a matching foreground tunnel is still
    running
- `scripts/openclawcode-setup-check.sh`
  - can now verify the live GitHub webhook subscription when
    `OPENCLAWCODE_SETUP_GITHUB_HOOK_ID` or `OPENCLAWCODE_GITHUB_HOOK_ID` is set
  - fails strict setup checks if the webhook is inactive or missing required
    lifecycle events
  - added the same tunnel-process recovery fallback used by the helper
- `src/integrations/openclaw-plugin/reconcile.ts`
  - local-run reconciliation now recovers pull request number and URL from the
    newest older run that still has PR metadata for the same issue branch
  - this prevents a newer rerun artifact from dropping PR continuity when the
    run stage advanced but the draft PR metadata was missing
- `src/openclawcode/testing/openclaw-plugin-reconcile.test.ts`
  - added regression coverage for:
    - recovering PR metadata from an older run for the same branch
    - refusing to inherit PR metadata across different branch histories
- `src/openclawcode/testing/setup-check.test.ts`
  - added static coverage for:
    - required GitHub webhook event subscription checks
    - tunnel helper event-sync behavior

Why this mattered:

- the real GitHub webhook that was about to be used for live replay was still
  subscribed only to `issues`, so `pull_request` and `pull_request_review`
  events would never have reached the route
- the tracked snapshot for real issue `#36` had lost its `PR #37` linkage
  because the newest rerun artifact omitted draft PR metadata even though an
  earlier run had preserved it
- both problems would have made the planned live PR/review replay inconclusive
  even though the lifecycle webhook implementation itself was already in place

Validated locally with:

- `./scripts/openclawcode-webhook-tunnel.sh sync-hook`
  - confirmed the managed GitHub webhook is now subscribed to:
    - `issues`
    - `pull_request`
    - `pull_request_review`
- `OPENCLAWCODE_SETUP_GITHUB_HOOK_ID=600049842 ./scripts/openclawcode-setup-check.sh --strict`
  - passed with webhook subscription verification enabled
- `pnpm exec vitest run src/openclawcode/testing/setup-check.test.ts src/openclawcode/testing/openclaw-plugin-reconcile.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- reconciled the real local operator state and confirmed
  `zhyongrui/openclawcode#36` now recovers:
  - `pullRequestNumber: 37`
  - `pullRequestUrl: https://github.com/zhyongrui/openclawcode/pull/37`

Result:

- the remaining blockers before live PR/review lifecycle replay are now outside
  the local codebase
- the next step is still the same planned slice:
  replay a real `pull_request` and `pull_request_review` event against the live
  route and verify chat notifications, snapshot updates, and `/occode-inbox`

## Operator Setup Hardening

Closed the main operator-installation gap by turning the current local setup
into one supported runbook plus a repo-local health check.

Implemented:

- `scripts/openclawcode-setup-check.sh`
  - verifies:
    - repo root and built CLI entrypoint
    - env/config/state file presence
    - webhook secret and GitHub token availability
    - local gateway TCP reachability
    - signed local webhook probe against `/plugins/openclawcode/github`
    - saved repo binding presence
    - current `trycloudflare` tunnel status
  - added operator flags:
    - `--strict`
    - `--skip-route-probe`
  - hardened the webhook probe with curl connect/response timeouts
- `src/openclawcode/testing/setup-check.test.ts`
  - added static regression coverage for the critical script checks and
    guardrails
  - kept execution-path coverage in place, but auto-skip it when the local
    sandbox blocks shell spawning
- `docs/openclawcode/operator-setup.md`
  - new single entrypoint for:
    - build prerequisites
    - env file setup
    - bundled plugin config
    - gateway startup
    - `/occode-bind`
    - repo webhook setup
    - temporary tunnel startup
    - health check execution
    - first live validation
- `docs/openclawcode/webhook-operations.md`
  - now explicitly covers only the temporary ingress helper
  - updated the required GitHub webhook events to:
    - `issues`
    - `pull_request`
    - `pull_request_review`
- `docs/openclawcode/README.md`
  - linked the new operator setup doc in the recommended reading order
- `docs/openclawcode/mvp-runbook.md`
  - pointed webhook/chatops readers at the new operator setup runbook
- `docs/openclawcode/development-plan.md`
  - marked the setup-hardening slice implemented
  - moved the immediate next slice to full live PR/review lifecycle validation

Why this matters:

- operators now have one docs-first setup path instead of reconstructing the
  flow from ad hoc notes and state-file spelunking
- repo binding is now documented as the supported routing control, which reduces
  manual edits to `chatops-state.json`
- the next validation task is finally narrower: prove the live PR/review
  lifecycle path, not re-explain setup

Added coverage:

- `src/openclawcode/testing/setup-check.test.ts`
  - static regression checks for:
    - required env/token/secret checks
    - repo binding inspection
    - signed route probe expectations
    - curl timeout guardrails

Validated locally with:

- `pnpm exec vitest run src/openclawcode/testing/setup-check.test.ts --pool threads`
- `bash -n scripts/openclawcode-setup-check.sh scripts/openclawcode-webhook-tunnel.sh`

Local sandbox note:

- execution-path tests for the shell script are present but skipped in this
  environment because sandboxed Vitest workers cannot spawn shell commands here

Build note:

- `pnpm build` still fails on the same unrelated repository error:
  - `src/gateway/credential-planner.ts`
  - `TS2339: Property 'enabled' does not exist on type 'GatewayRemoteConfig'`
