# 2026-03-18

- Synced `upstream/main` into `sync/upstream-2026-03-18`.
  - resolved the only content conflicts in:
    - `extensions/feishu/src/bot.ts`
    - `extensions/feishu/src/bot.test.ts`
  - validated the sync with:
    - `pnpm exec vitest run extensions/feishu/src/bot.test.ts --pool threads`
    - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
    - `pnpm build`
- Continued the low-touch bootstrap track on top of that refreshed branch.
- Extended `openclaw code bootstrap --repo owner/repo` so it now:
  - accepts `--webhook-url`
  - auto-discovers a public webhook URL from:
    - `--webhook-url`
    - `OPENCLAWCODE_BOOTSTRAP_WEBHOOK_URL`
    - a running tunnel log
  - creates or reuses the GitHub repo webhook when a public URL is available
  - persists `OPENCLAWCODE_GITHUB_HOOK_ID` back into `openclawcode.env`
  - reports webhook status in the bootstrap JSON summary
  - emits a sharper `nextAction` for chatops bootstrap when public ingress is still missing
- Added bootstrap webhook regressions in `src/commands/openclawcode.test.ts`.
- Extended the bootstrap flow again so it now:
  - starts the managed webhook tunnel automatically when no public URL can be
    discovered and gateway startup is enabled
  - reports tunnel state in the bootstrap JSON summary
  - emits `start-or-restart-webhook-tunnel` when tunnel startup fails
  - supports `--no-start-tunnel` for operators that want to manage ingress
    themselves
- Extended bootstrap one more time so it can reuse one unique saved chat target
  when the operator asks for `--chat-target auto`.
  - bootstrap stays conservative and falls back to placeholder binding when the
    saved targets are ambiguous
  - fresh-host docs now describe both the managed tunnel path and the
    auto-reused chat target path
- Extended bootstrap again so it now emits exact handoff commands for the next
  proof step.
  - JSON output now includes CLI proof, chat bind, chat start, operator status,
    gates, blueprint, and webhook retry commands when relevant
  - non-JSON output now prints the same handoff commands directly
- Extended bootstrap one more time so it now emits machine-readable proof
  readiness booleans.
  - JSON output now says explicitly whether CLI proof, chat proof, webhook URL,
    and webhook installation are ready
  - bootstrap still pairs those booleans with handoff commands so Codex can act
    without re-deriving state from prose
- Updated the fresh-host and Codex bootstrap docs to reflect the new low-touch
  ingress path.
- Validation for the webhook bootstrap slice passed with:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts src/cli/program/command-registry.test.ts --pool threads`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
  - `pnpm build`
- Continued the single-install onboarding track so this fork can take the user from
  `openclaw onboard` directly into repo setup instead of handing them a separate
  OpenClaw Code install flow.
- Added `src/wizard/setup.code.ts` as the new onboarding-owned OpenClaw Code
  helper.
  - detects GitHub auth from `GH_TOKEN`, `GITHUB_TOKEN`, or `gh auth token`
  - if GitHub auth is missing, onboarding now gives the exact one-time
    `gh auth login` handoff and stops cleanly
  - if GitHub auth is present, onboarding now asks for one repo decision:
    `New repo`, `Existing repo`, or `Skip for now`
  - `New repo` now:
    - asks the user for the repository name directly
    - checks whether `<owner>/<repo>` already exists
    - creates the repo as private by default
    - immediately runs `openclaw code bootstrap --repo owner/repo --json`
    - injects `test = ["echo no-tests-yet"]` so empty repos do not fail the
      current bootstrap test-command inference step
  - `Existing repo` now:
    - accepts either `owner/repo` or a bare repo name under the authenticated
      owner
    - checks that the repo is accessible before bootstrap
    - immediately runs bootstrap after validation
  - both paths summarize the local repo root, blueprint path, and next CLI handoff
    command back into onboarding notes
- `src/wizard/setup.finalize.ts` now delegates the OpenClaw Code stage to the
  new helper instead of printing a static note block.
- Added focused tests in `src/wizard/setup.code.test.ts` for:
  - missing GitHub auth guidance
  - new-repo create + bootstrap flow
  - existing-repo bootstrap flow from a bare repo name
- Simplified `src/wizard/setup.finalize.test.ts` so it only asserts that the
  finalize flow invokes the OpenClaw Code onboarding step.
- Validation for this onboarding slice:
  - `pnpm exec vitest run src/wizard/setup.code.test.ts --pool threads`
  - targeted file-level TypeScript check for the touched wizard files via
    `pnpm exec tsc ... | rg 'src/wizard/setup\\.(finalize|code)(\\.test)?\\.ts'`
- Known validation gap on this host:
  - `pnpm exec vitest run src/wizard/setup.finalize.test.ts --pool threads`
    still hangs and times out in this checkout before reporting results, so it
    is not currently a reliable regression signal for this slice
- Added two product-direction docs to capture the current north star after the
  onboarding work:
  - `docs/openclawcode/chat-native-autonomous-engineering.md`
  - `docs/openclawcode/development-plan-chat-native-autonomy.md`
- These docs now record:
  - why the target product is a chat-native, blueprint-first, GitHub-backed
    long-running engineering operator rather than just a coding bot
  - why Codex and Claude Code should be integrated as role-based engineering
    agents rather than treated as generic chat models
  - the next milestone plan for:
    - true empty-repo blueprint-first bootstrap
    - blueprint-driven next-work selection
    - issue materialization from work items
    - role-based external agent routing
    - chat-native project progress summaries
    - a continuous autonomous progress loop
- Updated `docs/openclawcode/README.md` so the new analysis and plan docs are
  part of the recommended reading order.
- Audited the first 50 canonical execution items from
  `docs/openclawcode/master-delivery-checklist.md`.
  - recorded the exact first-50 list in
    `docs/openclawcode/first-50-checklist-audit.md`
  - confirmed those first 50 entries are already marked `[x]` in the canonical
    checklist, so this pass was an audit plus validation pass rather than a
    claim to re-implement completed history
- While running the current validation baseline, found and fixed a real test
  regression in `src/openclawcode/testing/setup-check.test.ts`.
  - the strict-mode webhook retry test had started failing because it no longer
    stubbed the tunnel status and therefore picked up an unrelated warning
  - fixed the test by adding the same tunnel placeholder setup used by the
    neighboring strict-mode coverage
- Validation for the audit pass:
  - `pnpm exec vitest run src/openclawcode/testing/setup-check.test.ts -t "retries transient GitHub hook subscription probe failures before passing strict mode" --pool threads`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
    passed (`8` files / `93` tests)
  - `pnpm build`
    failed in unrelated optional-extension areas because this checkout is
    currently missing several type-providing dependencies for:
    - `extensions/matrix`
    - `extensions/msteams`
    - `extensions/nostr`
