# 2026-03-22

Closed one more chat-native setup hardening gap in `/home/zyr/pros/openclawcode`.

- `openclaw code bootstrap --json` now persists explicit repair handoff fields
  when plugin activation or chat setup routing is still blocked:
  - `gatewayRestartCommand`
  - `pluginActivationRepairCommand`
  - `chatSetupCommand`
  - `chatSetupStatusCommand`
- human-readable bootstrap output now prints the same host repair and chat retry
  commands instead of leaving `repair-plugin-activation` as an opaque status.
- chat setup bootstrap summaries now replay that same repair guidance through
  `/occode-setup-status`, so the operator can see:
  - restart the gateway with `openclaw gateway restart`
  - retry setup progress with `/occode-setup-status`
- updated docs:
  - `docs/openclawcode/README.md`
  - `docs/openclawcode/chat-setup-onboarding.md`

Validation:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "bootstraps operator files, repo binding, blueprint artifacts, and inferred test commands|reuses existing operator config defaults and accepts explicit chat targets|reuses a unique saved binding when chat-target auto is requested|surfaces an explicit bind handoff when chatops bootstrap still lacks a live target|surfaces plugin-activation repair commands in bootstrap JSON when chat routing is blocked"`
- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "replays the saved bootstrap summary through /occode-setup-status"`

Follow-up Feishu pairing hardening landed the same day:

- unauthorized Feishu DMs in pairing mode now send the pairing retry guidance
  for any slash-style command that needs command authorization, even if plugin
  routing is not yet available for that command
- this closes the silent `/occode-setup` first-touch case where a new operator
  could send the setup command before pairing and receive no feedback

Additional validation:

- `pnpm exec vitest run extensions/feishu/src/bot.test.ts --pool threads -t "replies with pairing guidance for blocked plugin commands even when a pairing request already exists|replies with pairing guidance for blocked slash commands even when plugin routing is unavailable|creates pairing request and drops unauthorized DMs in pairing mode"`
