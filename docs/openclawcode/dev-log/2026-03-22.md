# 2026-03-22

Closed one more chat-native setup hardening gap in `/home/zyr/pros/openclawcode`.

- `openclaw code bootstrap --json` now persists explicit repair handoff fields
  when plugin activation or chat setup routing is still blocked:
  - `gatewayRestartCommand`
  - `pluginActivationRepairCommand`
  - `chatSetupCommand`
  - `chatSetupStatusCommand`
- human-readable bootstrap output now prints the same host repair and chat retry
  commands instead of leaving `repair-plugin-activation` as an opaque status.
- chat setup bootstrap summaries now replay that same repair guidance through
  `/occode-setup-status`, so the operator can see:
  - restart the gateway with `openclaw gateway restart`
  - retry setup progress with `/occode-setup-status`
- updated docs:
  - `docs/openclawcode/README.md`
  - `docs/openclawcode/chat-setup-onboarding.md`

Validation:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "bootstraps operator files, repo binding, blueprint artifacts, and inferred test commands|reuses existing operator config defaults and accepts explicit chat targets|reuses a unique saved binding when chat-target auto is requested|surfaces an explicit bind handoff when chatops bootstrap still lacks a live target|surfaces plugin-activation repair commands in bootstrap JSON when chat routing is blocked"`
- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "replays the saved bootstrap summary through /occode-setup-status"`

Follow-up Feishu pairing hardening landed the same day:

- unauthorized Feishu DMs in pairing mode now send the pairing retry guidance
  for any slash-style command that needs command authorization, even if plugin
  routing is not yet available for that command
- this closes the silent `/occode-setup` first-touch case where a new operator
  could send the setup command before pairing and receive no feedback

Additional validation:

- `pnpm exec vitest run extensions/feishu/src/bot.test.ts --pool threads -t "replies with pairing guidance for blocked plugin commands even when a pairing request already exists|replies with pairing guidance for blocked slash commands even when plugin routing is unavailable|creates pairing request and drops unauthorized DMs in pairing mode"`

Proactive pairing-before-auth landed later the same day:

- service-start setup kickoff no longer assumes an unpaired DM can jump
  straight into `gh auth login --web`
- when the configured setup target is a pairing-gated `user:*` chat, the plugin
  now creates a setup session in `awaiting-chat-pairing` and proactively pushes
  the pairing code into that same chat
- after pairing approval, the background setup loop automatically starts the
  GitHub device flow and pushes the verification URL/code without waiting for
  `/occode-setup`
- the same runner loop now also keeps scanning configured targets after startup,
  so a newly written repo target can trigger proactive setup without requiring
  a gateway restart first
- `/occode-setup-status` now also reports that setup is blocked on pairing when
  the session has not been approved yet
- updated docs:
  - `docs/openclawcode/README.md`
  - `docs/openclawcode/chat-setup-onboarding.md`
  - `docs/openclawcode/development-plan-chat-native-autonomy.md`

Additional validation:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "proactively starts GitHub auth on service start for configured chat targets|proactively requests pairing before GitHub auth for pairing-gated user chat targets|automatically continues from pairing approval into proactive GitHub auth|pushes the next setup message automatically after GitHub auth completes|pushes a retry message automatically after GitHub auth fails"`
- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "reports pairing-gated setup state through /occode-setup-status|proactively starts GitHub auth on service start for configured chat targets|proactively requests pairing before GitHub auth for pairing-gated user chat targets|automatically continues from pairing approval into proactive GitHub auth|discovers newly configured setup targets while the runner is already active|pushes the next setup message automatically after GitHub auth completes|pushes a retry message automatically after GitHub auth fails|promotes a pending setup session to authenticated through /occode-setup-status|replays the saved bootstrap summary through /occode-setup-status"`

Preferred operator chat binding landed next:

- OpenClaw now persists one preferred operator DM target per channel/account in
  `operator-chat-targets.json`
- Feishu quick-actions menu events now capture and save `user:<open_id>` into
  that store instead of requiring the operator to send `/occode-setup` first
- Feishu setup wizard copy now tells the operator to tap Quick actions once
  after the gateway is running so the host learns the intended DM target
- `openclaw code bootstrap --mode chatops --chat-target auto` can now reuse
  that preferred operator target even when there is no existing repo binding yet
- the openclawcode plugin now resolves `bind-pending:*` setup placeholders
  through the saved operator target, so proactive pairing/auth kickoff can begin
  without waiting for a manual `/occode-bind`

Validation:

- `pnpm exec vitest run src/operator-chat-targets/store.test.ts extensions/feishu/src/monitor.bot-menu.test.ts src/commands/openclawcode.test.ts extensions/openclawcode/index.test.ts --pool threads -t "persists and reloads a preferred operator chat target|does not replace an existing target without an explicit replace flag|opens the quick-action launcher card at the webhook/event layer|reuses a preferred operator target when no repo binding exists yet|resolves bind-pending targets through the preferred operator chat target store"`

Provider-pause operator messaging was tightened again after the upstream sync:

- active pause notices in `/occode-status`, `/occode-inbox`, `/occode-start`,
  and `/occode-rerun` now explicitly say:
  - new work can still queue during the pause
  - queued runs will not start until the pause clears
  - queue drain resumes automatically once the pause window elapses
- this closes the remaining operator-facing ambiguity called out in
  `docs/openclawcode/README.md` about what an active provider pause actually
  means for queue behavior and recovery expectations

Additional validation:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "shows an active provider pause through /occode-status|shows an active provider pause through /occode-inbox|mentions active provider pause context when /occode-rerun is queued|includes active provider pause guidance when /occode-start queues work"`

Queue-resume feedback landed in the same area:

- when a provider pause expires and queued work becomes runnable again, the
  worker now pushes an explicit "resuming queue drain" message into the next
  target chat before it posts the usual `openclawcode is starting ...` update
- this closes the separate roadmap gap around queue-resume visibility after a
  pause window clears

Additional validation:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "does not start queued work while a provider pause is active|notifies the chat when queue drain resumes after the provider pause clears"`
Third-party framework evaluation:

- added a second third-party framework evaluation pass in
  `docs/openclawcode/third-party-skill-evaluation.md` covering:
  - `obra/superpowers`
  - `affaan-m/everything-claude-code`
- recorded the main takeaways for future `openclawcode` work:
  - absorb stronger pre-code workflow discipline rather than copying command
    wrappers
  - add a compact quality-gate surface
  - add continuous learning / pattern extraction from real operator incidents
  - publish a stable command/capability map
  - surface loop-health and context-budget diagnostics in operator-visible
    status paths

Install docs alignment:

- corrected `new-machine-install.md` and
  `new-machine-install-validation.md` so they no longer present
  `GH_TOKEN` / `GITHUB_TOKEN` as the preferred operator path
- both runbooks now prefer host GitHub auth through `gh auth login`
- token env vars remain documented only as compatibility fallback for
  non-interactive environments

Quality gate slice:

- added a reusable workflow quality-gate summary derivation layer in
  `src/openclawcode/quality-gate.ts`
- `openclaw code run --json` now exposes compact top-level `qualityGate*`
  fields
- chat status snapshots now persist quality-gate summary fields, and
  `/occode-status` now shows a top-level `Quality gate:` line when present
- updated the run JSON and operator status snapshot contract docs to document
  the new surface
- targeted validation passed:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts extensions/openclawcode/index.test.ts src/openclawcode/testing/openclaw-plugin-store.test.ts --pool threads -t "prints verification counts for ready-for-human-review runs|shows structured failure diagnostics through /occode-status after a failed run is recorded|tracks repeated transient provider failures and clears the pause after a later success"`

Quality gate operator rollout:

- extended the quality-gate surface into:
  - `/occode-inbox` top-level repo summary
  - `/occode-inbox` recent-ledger rows
  - per-repo counts inside `openclaw code operator-status-snapshot-show --json`
- updated `development-plan.md` and `full-program-roadmap.md` so the
  post-checklist queue now explicitly tracks:
  - quality-gate rollout
  - continuous learning
  - command/capability mapping
  - stricter pre-code discipline
  - loop-health diagnostics
- targeted validation passed:
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts src/commands/openclawcode.test.ts --pool threads -t "shows pending, running, queued, and recent activity through /occode-inbox|reports a stable operator status snapshot for tracked queue and status state|shows structured failure diagnostics through /occode-status after a failed run is recorded|prints verification counts for ready-for-human-review runs"`

Incident-learning rollout:

- added a first shared incident-learning summary derivation layer over persisted
  issue snapshots
- per-repo operator status snapshots now expose:
  - `incidentLearningSummary`
  - `providerFailureLearningCount`
  - `reviewRerunLearningCount`
  - `manualRecoveryLearningCount`
  - `runtimeRerouteLearningCount`
- `/occode-inbox` now shows a top-level `Recent learnings:` line when the repo
  has persisted rerun, provider-failure, manual-recovery, or runtime-reroute
  signals worth carrying forward
- updated docs:
  - `docs/openclawcode/operator-status-snapshot-contract.md`

Command/capability map rollout:

- added a first stable machine-readable command/capability map surface through:
  - `openclaw code capability-map-show`
  - `openclaw code capability-map-show --json`
- the new map ties together four stable sections:
  - chat commands
  - CLI commands
  - workflow artifacts
  - runtime roles
- this gives operators and future refactors one explicit inventory for:
  - which surface owns issue status, queue, policy, blueprint, and setup flows
  - which durable artifacts back those flows
  - which runtime roles participate in routed execution
- updated docs:
  - `docs/openclawcode/command-capability-map.md`

Refreshed third-party framework evaluation:

- re-checked the current `obra/superpowers` and
  `affaan-m/everything-claude-code` repo positioning against the existing
  evaluation note
- kept the original recommendation intact, but clarified two additional
  absorptions worth carrying forward:
  - `superpowers`: explicit isolated-worktree / fresh-subagent workflow
    discipline
  - `everything-claude-code`: session lifecycle hooks and mode-specific
    execution contexts
- also updated the note to distinguish:
  - what has already landed in `openclawcode`
  - what still remains as the highest-value follow-up:
    - stricter pre-code discipline
    - loop/context-budget diagnostics
- updated docs:
  - `docs/openclawcode/third-party-skill-evaluation.md`

Pre-code discipline rollout:

- added a first shared pre-code discipline summary derivation layer in:
  - `src/openclawcode/pre-code-discipline.ts`
- `openclaw code run --json` now exposes compact top-level `preCodeDiscipline*`
  fields covering:
  - plan status
  - execution-spec presence
  - explicit test intent presence/count
  - whether plan approval is required
  - whether the plan was edited before execution
- tracked chat status snapshots now persist the same pre-code discipline
  summary fields
- `/occode-status` now shows a top-level `Pre-code discipline:` line when that
  summary exists
- `/occode-inbox` recent ledger rows now show a `pre-code:` line when the
  tracked run carried pre-code warnings or blocks
- updated docs:
  - `docs/openclawcode/development-plan.md`
  - `docs/openclawcode/full-program-roadmap.md`
  - `docs/openclawcode/run-json-contract.md`
  - `docs/openclawcode/operator-status-snapshot-contract.md`
- targeted validation passed:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "reports blocked pre-code discipline when explicit plan approval is still pending"`
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "shows pre-code discipline context through /occode-status|shows pending, running, queued, and recent activity through /occode-inbox"`

GitHub account confirmation now stays visible all the way through onboarding:

- interactive OpenClaw Code onboarding no longer jumps straight from "auth is
  ready" into repo setup without naming the current account
- the wizard now shows:
  - GitHub username
  - name when available
  - email when available
  - auth source (`gh auth`, `GH_TOKEN`, or `GITHUB_TOKEN`)
- `gh auth`-backed sessions now offer an explicit sign-out + re-login branch
  before repo setup
- both the GitHub-account skip path and the repo-setup skip path now explain
  how to come back later through:
  - `/occode-setup`
  - `/occode-setup existing owner/repo`
  - `openclaw code bootstrap --repo owner/repo --json`
- repo-side actions now require one more explicit confirmation before OpenClaw
  creates a GitHub repo or bootstraps an existing one

Additional validation:

- `pnpm exec vitest run src/wizard/setup.code.test.ts --pool threads -t "shows chat-first auth guidance when GitHub auth is missing|creates and bootstraps a new repo with a placeholder empty-repo test command|bootstraps an existing repo from a bare repo name under the authenticated owner|shows explicit chat and cli handoff when repo setup is skipped|can sign out and re-run gh auth login before repo setup|stops before repo creation when the new-repo execution confirmation is declined|stops before bootstrapping an existing repo when execution confirmation is declined"`

Chat-native setup now shows the same GitHub identity and switch guidance:

- setup sessions now persist:
  - `githubAuthLogin`
  - `githubAuthName`
  - `githubAuthEmail`
- `/occode-setup-status`, repo-selected messages, and bootstrap-complete
  messages now all replay that identity instead of only saying `Source:
  gh-auth-token`
- those same messages now also tell the operator how to switch accounts:
  - `gh auth logout --hostname github.com --user <login>`
  - `gh auth login --hostname github.com --web`
  - then resend `/occode-setup-status`
- env-token sessions now instead explain that the host must replace or unset
  `GH_TOKEN` / `GITHUB_TOKEN` before refreshing setup status

Additional validation:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "reports authenticated setup status and preserves the selected repo|promotes a pending setup session to authenticated through /occode-setup-status|replays the saved bootstrap summary through /occode-setup-status|shows the current GitHub identity through /occode-setup-status when auth is already ready"`
- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "shows the current GitHub identity through /occode-setup-status when auth is already ready|promotes a pending setup session to authenticated through /occode-setup-status|replays the saved bootstrap summary through /occode-setup-status"`

Non-interactive local onboarding now reports the same OpenClaw Code GitHub
identity and handoff surface:

- successful `openclaw onboard --non-interactive --mode local` output now also
  reports whether OpenClaw Code GitHub auth is ready on the host
- human-readable output now includes:
  - GitHub username / name / email when available
  - auth source
  - the next OpenClaw Code CLI and chat entry points
  - account-switch guidance for either `gh auth` or env-token sessions
- non-interactive JSON output now also includes a stable `openClawCode`
  summary object with the same host GitHub auth details and next-step commands

Additional validation:

- `pnpm exec vitest run src/commands/onboard-non-interactive.gateway.test.ts --pool threads -t "prints the active OpenClaw Code GitHub identity in non-interactive local output|includes OpenClaw Code auth details in non-interactive local JSON output"`

GitHub auth source labels are now human-readable across setup surfaces:

- setup-facing output no longer exposes raw values such as `gh-auth-token` to
  operators
- chat setup and non-interactive onboarding now render:
  - `gh auth`
  - `GH_TOKEN env var`
  - `GITHUB_TOKEN env var`
- the underlying persisted contract stays unchanged; only operator-facing copy
  changed

Additional validation:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "reports authenticated setup status and preserves the selected repo|shows the current GitHub identity through /occode-setup-status when auth is already ready"`
- `pnpm exec vitest run src/commands/onboard-non-interactive.gateway.test.ts --pool threads -t "prints the active OpenClaw Code GitHub identity in non-interactive local output|includes OpenClaw Code auth details in non-interactive local JSON output"`

Chat-native setup now also tells operators how to switch the selected repo:

- repo-selected and bootstrap-complete setup messages now append:
  - `Wrong repo? Send /occode-setup owner/repo or /occode-setup new <repo-name> ...`
  - `/occode-setup-cancel` as the clean restart path
- this closes the adjacent operator-action gap where setup could confirm the
  current repo but still not say how to change it without guessing the command

Additional validation:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "reports authenticated setup status and preserves the selected repo|shows the current GitHub identity through /occode-setup-status when auth is already ready|promotes a pending setup session to authenticated through /occode-setup-status|replays the saved bootstrap summary through /occode-setup-status"`

Non-interactive remote onboarding now exposes the OpenClaw Code handoff too:

- `openclaw onboard --non-interactive --mode remote` no longer stops at
  `Remote gateway:` + `Auth:`
- human-readable output now tells the operator:
  - continue OpenClaw Code from the remote host or a bound chat
  - run `openclaw code bootstrap --repo owner/repo --json` on the remote host
  - use `/occode-setup` from chat as the chat-native path
  - run `gh auth login` on the remote host first when GitHub auth is missing
- remote non-interactive JSON output now also includes a stable `openClawCode`
  handoff block so automation can see the same next-step commands

Additional validation:

- `pnpm exec vitest run src/commands/onboard-non-interactive.gateway.test.ts --pool threads -t "prints the remote OpenClaw Code handoff in non-interactive remote output|includes remote OpenClaw Code handoff in non-interactive remote JSON output"`

Non-interactive local JSON now also carries a human-readable GitHub auth source
label for OpenClaw Code:

- `openClawCode.githubAuthSource` remains the stable raw value
- `openClawCode.githubAuthSourceLabel` now mirrors the operator-facing copy:
  - `gh auth`
  - `GH_TOKEN env var`
  - `GITHUB_TOKEN env var`
- this lets external consumers reuse the same friendly label without
  re-implementing the mapping

Additional validation:

- `pnpm exec vitest run src/commands/onboard-non-interactive.gateway.test.ts --pool threads -t "includes OpenClaw Code auth details in non-interactive local JSON output"`

Issue-level pre-code repair guidance is now fully covered in `/occode-status`
and mirrored into the `/occode-inbox` recent ledger:

- `/occode-status` no longer stops at the plan-approval repair path in tests
- targeted coverage now verifies repair guidance for:
  - awaiting explicit plan approval
  - missing isolated issue worktree preparation
  - missing mode-specific routing bindings
  - missing fresh-role runtime separation
- the underlying repair generator now emits every applicable repair action for an
  issue instead of only the first non-plan gap
- `/occode-inbox` recent entries now append `pre-code repair:` lines next to the
  existing `pre-code checks:` lines so operators can see the next fix directly
  from the repo summary view

Additional validation:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "shows pre-code discipline context through /occode-status|shows isolated-worktree repair guidance through /occode-status|shows routing and runtime repair guidance through /occode-status|shows pending, running, queued, and recent activity through /occode-inbox"`

Repo-level pre-code repair summaries now preserve action ordering instead of
stopping after the first gap:

- operator status snapshot repos now emit ordered `preCodeDisciplineRepairSummary`
  strings when multiple gaps are present
- `/occode-inbox` now mirrors the same ordered repo-level repair summary
- the current order remains:
  - isolated worktree preparation
  - mode-specific routing bindings
  - fresh-role runtime separation
- additional repair steps are now appended with `; then ` so operators can see
  the next fix without re-querying after each change

Additional validation:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "reports a stable operator status snapshot for tracked queue and status state"`
- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "shows pending, running, queued, and recent activity through /occode-inbox"`

Operator status snapshot repo summaries now also expose the structured ordered
repair list:

- each `repos[*]` entry can now carry `preCodeDisciplineRepairActions`
- this mirrors the same repo-level priority order already used for:
  - `preCodeDisciplineNextActionSummary`
  - `preCodeDisciplineRepairSummary`
- the human-readable summary remains unchanged for chat/CLI surfaces, while
  automation can now consume the stable ordered action array directly

Additional validation:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "reports a stable operator status snapshot for tracked queue and status state"`
