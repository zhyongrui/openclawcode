# 2026-03-14

## Built OpenClawCode Plugin Startup Fix

Implemented the built-runtime startup repair for the bundled `openclawcode`
plugin.

- `tsdown.config.ts`
  - now emits `dist/extensions/openclawcode/index.js`
- `scripts/copy-extension-manifests.ts`
  - now copies bundled `openclaw.plugin.json` files into `dist/extensions/*`
- `package.json`
  - build scripts now run `copy-extension-manifests.ts`
- `src/plugins/bundled-dir.ts`
  - now prefers `<packageRoot>/extensions` before ancestor walking so a partial
    `dist/extensions` tree does not shadow the full bundled plugin set
- `src/plugins/discovery.ts`
  - now supports selective built bundled overrides
  - built runtime, or `OPENCLAW_PREFER_BUILT_BUNDLED_PLUGINS=1`, now redirects
    bundled `openclawcode` to `dist/extensions/openclawcode/index.js`
- `src/plugins/discovery.test.ts`
  - now covers the built bundled override path
- `src/plugins/bundled-dir.test.ts`
  - now covers the package-root `extensions/` preference over `dist/extensions`

Field note:

- a naive "openclawcode-only" diagnostic config is still too broad
- bundled defaults such as `device-pair`, `ollama`, `phone-control`, `sglang`,
  `talk-voice`, `vllm`, and `memory-core` still enable themselves unless the
  proof config also sets:
  - `plugins.allow = ["openclawcode"]`
  - `plugins.slots.memory = "none"`

Validated locally with:

- `pnpm exec vitest run src/plugins/bundled-dir.test.ts --pool threads`
- `pnpm exec vitest run src/plugins/discovery.test.ts`
- `pnpm exec vitest run src/gateway/server-plugins.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Real proofs:

- bundled discovery now resolves `openclawcode` to:
  - `dist/extensions/openclawcode/index.js`
  - `dist/extensions/openclawcode/openclaw.plugin.json`
- direct `jiti()` of the compiled bundled `openclawcode` entry now completes
- direct loader proof with an allowlisted config now loads only `openclawcode`
  and finishes in about `2.3s`
- direct built gateway proof with that same allowlisted config now reaches:
  - `listening on ws://127.0.0.1:18890, ws://[::1]:18890`
  - `Browser control listening on http://127.0.0.1:18892/`

Next slice:

- keep the repaired built openclawcode-only startup path as the baseline
- debug any remaining full-config startup stall as a separate live-config
  component instead of reopening the bundled `openclawcode` loader path

## Setup Check Built Startup Proof

Turned the manual built-startup isolation proof into a repeatable setup-check
capability.

- `scripts/openclawcode-setup-check.sh`
  - now accepts `--probe-built-startup`
  - synthesizes an isolated proof config from the active operator config:
    - `channels = {}`
    - `bindings = []`
    - `plugins.allow = ["openclawcode"]`
    - `plugins.slots.memory = "none"`
  - runs the built `dist/index.js gateway run` entrypoint against that
    synthesized config on a non-default proof port
  - passes only when the listener line is observed
- `src/openclawcode/testing/setup-check.test.ts`
  - now covers the new startup-proof shell path with a stubbed built CLI

Validated locally with:

- `pnpm exec vitest run src/openclawcode/testing/setup-check.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Real host proof:

- `./scripts/openclawcode-setup-check.sh --strict --probe-built-startup --json`
  on `~/.openclaw` now returns:
  - `readiness.gatewayReachable = false`
  - `readiness.routeProbeReady = false`
  - `readiness.builtStartupProofRequested = true`
  - `readiness.builtStartupProofReady = true`
  - `readiness.nextAction = "start-or-restart-live-gateway"`

Live recovery follow-up:

- after restarting the long-lived repo-local gateway on commit `1a9aa38777`,
  `./scripts/openclawcode-setup-check.sh --strict --json` on `~/.openclaw`
  returned:
  - `summary.pass = 17`
  - `summary.warn = 1`
  - `summary.fail = 0`
  - `readiness.gatewayReachable = true`
  - `readiness.routeProbeReady = true`
  - `readiness.nextAction = "resolve-warnings-before-promotion"`
- the remaining strict blocker on the live host is now only the model-inventory
  warning, not gateway reachability or webhook route health

Model-inventory repair follow-up:

- `check_model_inventory()` now runs `models list --json` against a synthesized
  probe config with:
  - `channels = {}`
  - `bindings = []`
  - `plugins.enabled = false`
  - `plugins.entries = {}`
- that probe now passes on the real `~/.openclaw` host too:
  - `summary.pass = 19`
  - `summary.warn = 0`
  - `summary.fail = 0`
  - `modelInventory.available = 1`
  - `readiness.strict = true`
  - `readiness.nextAction = "ready-for-low-risk-proof"`

Field note:

- the allowlist and `plugins.slots.memory = "none"` are now codified in the
  proof path instead of living only in ad hoc debug notes, which makes
  promotion preflight repeatable for later operator hosts and future AI
  sessions
- real host validation against `~/.openclaw` returned:
  - `built gateway startup proof reached listener on ws://127.0.0.1:18890`
  - but overall `--strict --probe-built-startup --json` still failed with
    `gateway not reachable` and `signed webhook probe failed`
  - that means the isolated built-startup proof can now pass independently of
    the long-lived live gateway route, and operators should use
    `--skip-route-probe --probe-built-startup` when they are validating startup
    during a restart window

## Setup Check Readiness Split

Extended setup-check readiness so future sessions and rollout automation can
separate live-route failure from built-startup failure without scraping raw
check lines.

- `scripts/openclawcode-setup-check.sh`
  - readiness JSON now includes:
    - `gatewayReachable`
    - `routeProbeReady`
    - `routeProbeSkipped`
    - `builtStartupProofRequested`
    - `builtStartupProofReady`
  - when the isolated startup proof passes but the long-lived live gateway is
    still down, `nextAction` now becomes:
    - `start-or-restart-live-gateway`
- `src/openclawcode/testing/setup-check.test.ts`
  - now covers the split-readiness case explicitly
  - test helper timeout is widened to `30000` ms because the shell-heavy
    setup-check suite now has several legitimate slow paths

Validated locally with:

- `pnpm exec vitest run src/openclawcode/testing/setup-check.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

## Validation Pool Reconciliation

Turned stale validation-pool cleanup into a first-class repo-local CLI path.

- `src/openclawcode/validation-issues.ts`
  - now parses command-layer validation issue field names from issue titles
  - now assesses command-layer validation issues as:
    - `implemented`
    - `pending`
    - `manual-review`
  - `implemented` currently means the requested field is present in:
    - `src/commands/openclawcode.ts`
    - `src/commands/openclawcode.test.ts`
    - `docs/openclawcode/run-json-contract.md`
- `src/commands/openclawcode.ts`
  - `openclaw code list-validation-issues` now reports:
    - `implementationCounts`
    - per-issue `implementationState`
    - per-issue `implementationSummary`
    - per-issue `fieldName`
    - per-issue `autoClosable`
  - new command:
    - `openclaw code reconcile-validation-issues`
  - reconcile command can now close already-implemented command-layer
    validation issues directly from the repo-local CLI
  - reconcile JSON output now includes:
    - `closableImplementedIssues`
    - `closedIssues`
    - `nextAction`
- `src/cli/program/register.code.ts`
  - now exposes `reconcile-validation-issues` in the CLI
- `src/commands/openclawcode.test.ts`
  - now covers implemented/manual-review inventory states
  - now covers reconcile dry-run and close flows
- `src/openclawcode/validation-issues.test.ts`
  - now covers field-name parsing plus implemented/pending/manual-review
    assessment

Validated locally with:

- `pnpm exec vitest run src/openclawcode/validation-issues.test.ts src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Real proofs:

- with a sanitized direct CLI config:
  - `OPENCLAW_SKIP_CANVAS_HOST=1`
  - `OPENCLAW_CONFIG_PATH=<temp config with channels={}, bindings=[], plugins disabled>`
- `node dist/index.js code reconcile-validation-issues --repo-root /home/zyr/pros/openclawcode --json`
  detected `9` closable implemented issues and recommended:
  - `nextAction = "close-implemented-validation-issues"`
- `node dist/index.js code reconcile-validation-issues --repo-root /home/zyr/pros/openclawcode --close-implemented --json`
  then closed stale command-layer issues:
  - `#74`
  - `#75`
  - `#76`
  - `#77`
  - `#78`
  - `#79`
  - `#80`
  - `#81`
  - `#82`
- the live validation pool after that cleanup became:
  - docs/operator issues `#60`, `#86`, `#87`
  - no remaining open command-layer validation issue
- seeded the next command-layer issue directly through the repo-local CLI:
  - `#89` `failureDiagnosticToolCount`

Field note:

- direct CLI admin commands are still cleaner to prove through a sanitized
  config than through the fully loaded `~/.openclaw` operator config
- that does not change the long-lived live gateway baseline; it just avoids
  letting Feishu/plugin startup noise obscure CLI-admin proofs

## Failure Diagnostic Tool Count

Consumed validation issue `#89` by extending the stable run JSON contract.

- `src/commands/openclawcode.ts`
  - now emits `failureDiagnosticToolCount` from
    `failureDiagnostics.toolCount ?? null`
- `src/commands/openclawcode.test.ts`
  - now asserts `failureDiagnosticToolCount` for both missing and populated
    failure diagnostics
- `docs/openclawcode/run-json-contract.md`
  - now documents `failureDiagnosticToolCount`

Validated locally with:

- `pnpm exec vitest run src/commands/openclawcode.test.ts src/openclawcode/validation-issues.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Real proof:

- reran `reconcile-validation-issues --close-implemented --json`
  after the field landed
- live issue `#89` was then auto-closed as implemented
- seeded the next low-risk command-layer issue:
  - `#91` `failureDiagnosticUsageTotal`

## Failure Diagnostic Usage Total

Consumed validation issue `#91` by extending the stable run JSON contract one
step deeper into `failureDiagnostics`.

- `src/commands/openclawcode.ts`
  - now emits `failureDiagnosticUsageTotal` from
    `failureDiagnostics.lastCallUsageTotal ?? null`
- `src/commands/openclawcode.test.ts`
  - now asserts `failureDiagnosticUsageTotal` for both missing and populated
    failure diagnostics
- `docs/openclawcode/run-json-contract.md`
  - now documents `failureDiagnosticUsageTotal`

Validated locally with:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Real proof:

- reran `reconcile-validation-issues --close-implemented --json`
  after the field landed
- live issue `#91` was then auto-closed as implemented
- seeded the next low-risk command-layer issue:
  - `#93` `failureDiagnosticSystemPromptChars`

## Failure Diagnostic System Prompt Chars

Consumed validation issue `#93` by extending the stable run JSON contract one
step deeper into `failureDiagnostics`.

- `src/commands/openclawcode.ts`
  - now emits `failureDiagnosticSystemPromptChars` from
    `failureDiagnostics.systemPromptChars ?? null`
- `src/commands/openclawcode.test.ts`
  - now asserts `failureDiagnosticSystemPromptChars` for both missing and
    populated failure diagnostics
- `docs/openclawcode/run-json-contract.md`
  - now documents `failureDiagnosticSystemPromptChars`

Validated locally with:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Real proof:

- reran `reconcile-validation-issues --close-implemented --json`
  after the field landed
- live issue `#93` was then auto-closed as implemented
- seeded the next low-risk command-layer issue:
  - `#96` `failureDiagnosticSkillsPromptChars`

## Failure Diagnostic Skills Prompt Chars

Consumed validation issue `#96` by extending the stable run JSON contract one
step deeper into `failureDiagnostics`.

- `src/commands/openclawcode.ts`
  - now emits `failureDiagnosticSkillsPromptChars` from
    `failureDiagnostics.skillsPromptChars ?? null`
- `src/commands/openclawcode.test.ts`
  - now asserts `failureDiagnosticSkillsPromptChars` for both missing and
    populated failure diagnostics
- `docs/openclawcode/run-json-contract.md`
  - now documents `failureDiagnosticSkillsPromptChars`

Validated locally with:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Real proof:

- reran `reconcile-validation-issues --close-implemented --json`
  after the field landed
- live issue `#96` was then auto-closed as implemented
- seeded the next low-risk command-layer issue:
  - `#97` `failureDiagnosticToolSchemaChars`

## Failure Diagnostic Tool Schema Chars

Consumed validation issue `#97` by extending the stable run JSON contract one
step deeper into `failureDiagnostics`.

- `src/commands/openclawcode.ts`
  - now emits `failureDiagnosticToolSchemaChars` from
    `failureDiagnostics.toolSchemaChars ?? null`
- `src/commands/openclawcode.test.ts`
  - now asserts `failureDiagnosticToolSchemaChars` for both missing and
    populated failure diagnostics
- `docs/openclawcode/run-json-contract.md`
  - now documents `failureDiagnosticToolSchemaChars`

Validated locally with:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run src/openclawcode/testing/openclaw-plugin-store.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Real proof:

- reran `reconcile-validation-issues --close-implemented --json`
  after the field landed
- live issue `#97` was then auto-closed as implemented
- seeded the next low-risk command-layer issue:
  - `#98` `failureDiagnosticSkillCount`

## Validation Store Timeout Hardening

Tightened one slow store regression test after the second command-layer slice
surfaced it as a suite-timeout false negative.

- `src/openclawcode/testing/openclaw-plugin-store.test.ts`
  - `keeps only the newest GitHub delivery records` now uses the minimum record
    count that still crosses the trim boundary
  - the test now carries a `60000` ms timeout because it intentionally runs a
    large number of persisted store writes

Validated locally with:

- `pnpm exec vitest run src/openclawcode/testing/openclaw-plugin-store.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

## Failure Diagnostic Skill Count

Consumed validation issue `#98` by extending the stable run JSON contract one
step deeper into `failureDiagnostics`.

- `src/commands/openclawcode.ts`
  - now emits `failureDiagnosticSkillCount` from
    `failureDiagnostics.skillCount ?? null`
- `src/commands/openclawcode.test.ts`
  - now asserts `failureDiagnosticSkillCount` for both missing and populated
    failure diagnostics
- `docs/openclawcode/run-json-contract.md`
  - now documents `failureDiagnosticSkillCount`

Validated locally with:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Real proof:

- reran `reconcile-validation-issues --close-implemented --json`
  after the field landed
- live issue `#98` was then auto-closed as implemented
- seeded the next low-risk command-layer issue:
  - `#99` `failureDiagnosticInjectedWorkspaceFileCount`

## Failure Diagnostic Injected Workspace File Count

Consumed validation issue `#99` by extending the stable run JSON contract one
step deeper into `failureDiagnostics`.

- `src/commands/openclawcode.ts`
  - now emits `failureDiagnosticInjectedWorkspaceFileCount` from
    `failureDiagnostics.injectedWorkspaceFileCount ?? null`
- `src/commands/openclawcode.test.ts`
  - now asserts `failureDiagnosticInjectedWorkspaceFileCount` for both missing
    and populated failure diagnostics
- `docs/openclawcode/run-json-contract.md`
  - now documents `failureDiagnosticInjectedWorkspaceFileCount`

Validated locally with:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Real proof:

- reran `reconcile-validation-issues --close-implemented --json`
  after the field landed
- live issue `#99` was then auto-closed as implemented
- seeded the next low-risk command-layer issue:
  - `#100` `failureDiagnosticBootstrapWarningShown`

Field note:

- the current `command-json-boolean` seed template still assumes an
  array-presence source path and therefore phrases scalar-boolean issues like
  `#100` awkwardly
- the next slice after `#100` should either implement that field directly or
  harden the seed template wording for boolean source paths

## Failure Diagnostic Bootstrap Warning Shown

Consumed validation issue `#100` by extending the stable run JSON contract into
the boolean branch of `failureDiagnostics`.

- `src/commands/openclawcode.ts`
  - now emits `failureDiagnosticBootstrapWarningShown` from
    `failureDiagnostics.bootstrapWarningShown ?? false`
- `src/commands/openclawcode.test.ts`
  - now asserts `failureDiagnosticBootstrapWarningShown` for:
    - missing diagnostics
    - a populated `false` case
    - a populated `true` case
- `docs/openclawcode/run-json-contract.md`
  - now documents `failureDiagnosticBootstrapWarningShown`

Validated locally with:

- `pnpm exec vitest run src/commands/openclawcode.test.ts src/openclawcode/validation-issues.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Real proof:

- reran `reconcile-validation-issues --close-implemented --json`
  after the field landed
- live issue `#100` was auto-closed as implemented

## Validation String Template

Extended validation-pool tooling so command-layer string-or-null fields can use
the same repo-native seed/reconcile loop as the existing number and boolean
convenience fields.

- `src/openclawcode/validation-issues.ts`
  - new template id:
    - `command-json-string`
  - new draft builder for string-or-null convenience fields
  - legacy heuristics and implementation detection now recognize
    `: string | null`
- `src/openclawcode/validation-issues.test.ts`
  - now covers string template drafting, parsing, and stable template order
- `src/commands/openclawcode.test.ts`
  - now covers string-template dry-run output

Validated locally with:

- `pnpm exec vitest run src/commands/openclawcode.test.ts src/openclawcode/validation-issues.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Real proof:

- seeded the next low-risk command-layer issue through the new template:
  - `#101` `failureDiagnosticProvider`
- a follow-up `reconcile-validation-issues --close-implemented --json` then
  confirmed the live pool is actionable again instead of empty

## Failure Diagnostic Provider And Model

Consumed validation issues `#101` and `#102` together because both are direct
string mirrors of the same nested `failureDiagnostics` object.

- `src/commands/openclawcode.ts`
  - now emits:
    - `failureDiagnosticProvider`
    - `failureDiagnosticModel`
- `src/commands/openclawcode.test.ts`
  - now asserts both fields for missing and populated failure diagnostics
- `docs/openclawcode/run-json-contract.md`
  - now documents both top-level string fields

Validated locally with:

- `pnpm exec vitest run src/commands/openclawcode.test.ts src/openclawcode/validation-issues.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Real proof:

- seeded issue `#102` through the new `command-json-string` template
- reran `reconcile-validation-issues --close-implemented --json`
- live issues `#101` and `#102` were then auto-closed as implemented
- replenished the command-layer pool with:
  - `#103` `draftPullRequestTitle`

Field note:

- the common `failureDiagnostics` provider metadata is now mirrored at the
  top level end-to-end, which means downstream consumers no longer need to
  open the nested object for the usual provider/model/prompt-footprint fields

## Draft Pull Request Title

Consumed validation issue `#103` by mirroring the nested draft PR title onto
the stable top-level JSON contract.

- `src/commands/openclawcode.ts`
  - now emits `draftPullRequestTitle`
- `src/commands/openclawcode.test.ts`
  - now asserts `draftPullRequestTitle` for both populated and missing draft PR
    metadata
- `docs/openclawcode/run-json-contract.md`
  - now documents `draftPullRequestTitle`

Validated locally with:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Real proof:

- reran `reconcile-validation-issues --close-implemented --json`
- live issue `#103` was auto-closed as implemented
- replenished the command-layer pool with:
  - `#104` `draftPullRequestOpenedAt`

Field note:

- top-level draft PR metadata now covers branch, base branch, title, number,
  URL, disposition, and publication timing without forcing downstream tools to
  read the nested `draftPullRequest` object for the common cases

## Draft Pull Request Body And Opened At

Consumed validation issues `#104` and `#105` together because both are direct
string mirrors of the same nested draft PR metadata object.

- `src/commands/openclawcode.ts`
  - now emits:
    - `draftPullRequestOpenedAt`
    - `draftPullRequestBody`
- `src/commands/openclawcode.test.ts`
  - now asserts both fields for populated and missing draft PR metadata
- `docs/openclawcode/run-json-contract.md`
  - now documents both top-level string fields

Validated locally with:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Real proof:

- reran `reconcile-validation-issues --close-implemented --json`
- live issues `#104` and `#105` were auto-closed as implemented
- replenished the command-layer pool with:
  - `#106` `issueTitle`

Field note:

- the stable top-level draft PR mirrors now cover the most common operator
  automation reads: title, body, branch, base branch, opened-at timestamp,
  publication number, publication URL, and disposition

## Upstream Sync 2026-03-14

Opened a fresh upstream-sync branch and validated it as the next engineering
baseline.

- branch:
  - `sync/upstream-2026-03-14`
- upstream head merged:
  - `c08317203d`
- merge conflicts resolved in:
  - `package.json`
  - `src/agents/sandbox/fs-bridge.shell.test.ts`
  - `src/infra/safe-open-sync.test.ts`

Field note:

- immediately after the merge, the local install state was not safe to trust:
  - `@modelcontextprotocol/sdk` looked missing
  - `pnpm build` failed with `Command "tsdown" not found`
  - `pnpm exec vitest ...` failed with `Command "vitest" not found`
- the required recovery step was:
  - `pnpm install --frozen-lockfile`
- future sync sessions should do that install pass before treating missing
  package or missing bin failures as a source-level regression

Validated on `sync/upstream-2026-03-14` with:

- `pnpm install --frozen-lockfile`
- `pnpm exec vitest run src/agents/sandbox/fs-bridge.shell.test.ts src/infra/safe-open-sync.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`
- `./scripts/openclawcode-setup-check.sh --strict --json`

Real host proof:

- strict setup-check on `~/.openclaw` returned:
  - `summary.pass = 19`
  - `summary.warn = 0`
  - `summary.fail = 0`
  - `readiness.lowRiskProofReady = true`
  - `readiness.promotionReady = true`
  - `readiness.nextAction = "ready-for-low-risk-proof"`

Next slice:

- keep `sync/upstream-2026-03-14` as the active engineering branch
- use that refreshed branch for the next low-risk live proof before promoting
  it back to `main`

## Refreshed-Branch Low-Risk Live Proof

Used the refreshed branch for the next real low-risk merged proof and cleared
the promotion gate.

- proof issue:
  - `#87`
- real run:
  - `zhyongrui-openclawcode-87-1773494823680`
- real PR:
  - `#95`
- real outcome:
  - merged automatically against `sync/upstream-2026-03-14`

Validated by real workflow execution with:

- `OPENCLAW_SKIP_CANVAS_HOST=1`
- `OPENCLAW_DISABLE_LAZY_SUBCOMMANDS=1`
- `OPENCLAW_CONFIG_PATH=/home/zyr/.openclaw/openclaw.json`
- `node dist/index.js code run --issue 87 --owner zhyongrui --repo openclawcode --repo-root /home/zyr/pros/openclawcode --base-branch sync/upstream-2026-03-14 --open-pr --merge-on-approve --json`

Field note:

- this docs-only proof merged without explicit repo test commands
- the saved run artifact still reports:
  - `verificationHasMissingCoverage = true`
  - `verificationMissingCoverageCount = 1`
- current policy behavior therefore allows this style of low-risk docs run to
  auto-merge as long as verification has no findings and the issue remains
  policy-eligible

Next slice:

- promote `sync/upstream-2026-03-14` back to `main`
- restart or re-prove the long-lived `main` operator baseline on that promoted
  build

## Setup Check GitHub Hook Retry Hardening

Hardened `setup-check` after the first strict probe on promoted `main` hit a
transient GitHub API TLS failure during webhook subscription verification.

- `scripts/openclawcode-setup-check.sh`
  - `check_github_hook_subscription()` now retries transport/execution failures
    before reporting a hard fail
  - inactive-hook and missing-event failures still fail immediately after a
    successful API response
- `src/openclawcode/testing/setup-check.test.ts`
  - now covers the transient "first hook check fails, second succeeds" path

Validated with:

- `pnpm exec vitest run src/openclawcode/testing/setup-check.test.ts --pool threads`
- `./scripts/openclawcode-setup-check.sh --strict --json`

Real host proof:

- the first strict probe on promoted `main` failed with:
  - `GitHub webhook subscription check failed for hook 600049842`
  - underlying transport error:
    `SSL: UNEXPECTED_EOF_WHILE_READING`
- after the retry hardening landed, the same host returned:
  - `summary.pass = 19`
  - `summary.warn = 0`
  - `summary.fail = 0`
  - `readiness.nextAction = "ready-for-low-risk-proof"`
