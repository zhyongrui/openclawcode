# 2026-03-23

Extended the repo-local `operator-program` execution-policy slice across the
remaining operator-facing progress and status surfaces.

Completed subitems in this unfinished track:

1. added `operatorProgram.updatedAt` to `.openclawcode/project-progress.json`
2. added `operatorProgram.title` to `.openclawcode/project-progress.json`
3. added `operatorProgram.summary` to `.openclawcode/project-progress.json`
4. added `operatorProgram.mutableSurfacePathCount` to `.openclawcode/project-progress.json`
5. added `operatorProgram.mutableSurfacePathsPresent` to `.openclawcode/project-progress.json`
6. added `operatorProgram.advancementRuleSummary` to `.openclawcode/project-progress.json`
7. added `operatorProgram.keepCriteriaCount` to `.openclawcode/project-progress.json`
8. added `operatorProgram.discardCriteriaCount` to `.openclawcode/project-progress.json`
9. added `operatorProgram.retryCriteriaCount` to `.openclawcode/project-progress.json`
10. added `operatorProgram.simplificationBias` to `.openclawcode/project-progress.json`
11. added `operatorProgram.linkedBlueprintPath` to `.openclawcode/project-progress.json`
12. added `operatorProgram.linkedWorkItemsPath` to `.openclawcode/project-progress.json`
13. added `operatorProgram.linkedStageGatesPath` to `.openclawcode/project-progress.json`
14. mirrored `operatorProgram.updatedAt` into operator status snapshot repo summaries
15. mirrored `operatorProgram.title` into operator status snapshot repo summaries
16. mirrored `operatorProgram.summary` into operator status snapshot repo summaries
17. mirrored mutable-surface path counts and allowlist presence into operator snapshots
18. mirrored advancement-rule summary plus keep/discard/retry counts into operator snapshots
19. mirrored simplification-bias and linked artifact paths into operator snapshots
20. surfaced the richer shared summary through `openclaw code project-progress-show`,
    `/occode-progress`, `/occode-status`, `/occode-inbox`, and CLI
    `operator-status-snapshot-show`

Operator-facing text now shows:

- operator-program meta line with title and updated timestamp
- summary line
- scope line with allowlist/path count and simplification bias
- criteria counts
- advancement-rule summary
- linked artifact paths

Docs updated:

- `docs/openclawcode/development-plan.md`
- `docs/openclawcode/operator-status-snapshot-contract.md`

Validation:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "summarizes project progress and the autonomous loop artifact|reports a stable operator status snapshot for tracked queue and status state|creates and shows a repo-local operator program artifact"`
- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "shows project progress and autopilot state through chat commands|shows pre-code discipline context through /occode-status|shows pending, running, queued, and recent activity through /occode-inbox"`

Changed proactive chat setup so a configured concrete target no longer waits
for DM pairing approval before the first outbound setup push.

- `extensions/openclawcode/index.ts` now skips the proactive pairing gate when
  collecting configured setup targets
- the background setup loop also auto-promotes existing
  `awaiting-chat-pairing` sessions into GitHub device-auth kickoff instead of
  leaving them stalled forever
- this keeps manual unauthorized slash commands in pairing mode unchanged:
  they still return pairing guidance through the Feishu surface
- updated docs:
  - `docs/openclawcode/README.md`
  - `docs/openclawcode/chat-setup-onboarding.md`

Validation:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "proactively starts GitHub auth on service start for configured chat targets|proactively starts GitHub auth immediately for configured pairing-gated user chat targets|automatically continues from a saved awaiting-chat-pairing session into proactive GitHub auth|reports pairing-gated setup state through /occode-setup-status"`
- `pnpm exec vitest run extensions/feishu/src/bot.test.ts --pool threads -t "replies with pairing guidance for blocked plugin commands even when a pairing request already exists|replies with pairing guidance for blocked slash commands even when plugin routing is unavailable|creates pairing request and drops unauthorized DMs in pairing mode"`

Extended the operator-program execution-policy track into the remaining
machine-readable loop and run surfaces.

- `.openclawcode/autonomous-loop.json` now persists the shared
  `operatorProgram` summary alongside next-work, queue, and current-run state
- `openclaw code autonomous-loop-run --json` and
  `openclaw code autonomous-loop-show --json` now expose that summary directly
- `openclaw code run --json` now mirrors operator-program policy through
  stable top-level fields:
  - availability and artifact path
  - mutable-surface mode and allowlist/path summary
  - validation-budget summary and max primary command count
  - keep / discard / retry criteria counts
  - simplification bias, attempt-ledger requirement, and next-action guidance
  - linked blueprint / work-items / stage-gates paths

Docs updated:

- `docs/openclawcode/run-json-contract.md`
- `docs/openclawcode/development-plan.md`

Validation:

- `pnpm exec vitest run src/commands/openclawcode.test.ts -t "prints stable top-level JSON fields for workflow scope, pr metadata, review, and merge policy|mirrors operator-program policy into run --json output when the artifact exists|surfaces active-run stage and role routing through project progress and autopilot artifacts|runs one autonomous loop iteration and queues the selected issue when chatops bindings are present" --pool threads`
- `pnpm exec vitest run src/openclawcode/testing/run-issue.test.ts --pool threads`

Closed the remaining pairing/setup UX gap for configured Feishu setup DMs.

- `extensions/openclawcode/index.ts` now auto-writes configured `user:*`
  proactive setup targets into the channel pairing allow-from store before the
  GitHub device-flow kickoff, so the configured operator DM becomes trusted as
  soon as setup starts
- `extensions/feishu/src/bot.ts` now recognizes configured `openclawcode`
  setup lifecycle commands in pairing-gated DMs and narrowly bypasses the
  pairing gate for that exact configured operator target
- this covers:
  - the first proactive GitHub login push
  - manual `/occode-setup`
  - manual `/occode-setup-status`
  - manual `/occode-setup-retry`
  - manual `/occode-setup-cancel`
- unrelated DMs and unrelated commands still keep the original pairing
  behavior

Docs updated:

- `docs/openclawcode/README.md`
- `docs/openclawcode/chat-setup-onboarding.md`

Validation:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "proactively starts GitHub auth immediately for configured pairing-gated user chat targets|automatically continues from a saved awaiting-chat-pairing session into proactive GitHub auth"`
- `pnpm exec vitest run extensions/feishu/src/bot.test.ts --pool threads -t "replies with pairing guidance for blocked plugin commands even when a pairing request already exists|replies with pairing guidance for blocked slash commands even when plugin routing is unavailable|allows configured openclawcode setup commands through pairing-gated DMs"`
- `pnpm exec tsc -p tsconfig.plugin-sdk.dts.json`
  - the earlier `fileURLToPath` error in `src/agents/pi-tools.read.ts` is gone
  - compilation still fails on multiple pre-existing unrelated TypeScript
    issues, including missing `@anthropic-ai/vertex-sdk` declarations and
    upstream type errors in `src/openclawcode/*`

Refined post-login GitHub setup guidance to better match the product's
proactive positioning.

- post-login setup messages no longer print host-shell `gh auth logout` /
  `gh auth login` commands directly into chat
- the message now stays status-first:
  - confirms the current GitHub username and auth source
  - lightly reminds the operator that `/occode-github-switch` can start a
    fresh chat-native login later if needed
  - reminds the operator that `/occode-github-status` can re-check the current
    host login later
- added chat commands:
  - `/occode-github-status`
  - `/occode-github-switch`
- `/occode-github-switch` now clears the current `gh auth` login when OpenClaw
  is using `gh auth`, starts a fresh device-flow login, and preserves the
  active setup session so setup can continue in the same chat

Docs updated:

- `docs/openclawcode/README.md`
- `docs/openclawcode/chat-setup-onboarding.md`
- `docs/openclawcode/github-chat-auth-switch-design.md`

Validation:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "shows the current GitHub identity through /occode-setup-status when auth is already ready|shows the current host GitHub login through /occode-github-status|starts a fresh GitHub login through /occode-github-switch|promotes a pending setup session to authenticated through /occode-setup-status"`

Started the repo/blueprint classification onboarding slice that now sits
between GitHub auth and bootstrap.

- documented what `bootstrap` means in product terms:
  - local repo preparation
  - chat/plugin/webhook wiring
  - repo-local artifact seeding
  - explicitly after blueprint agreement, not immediately after login
- `/occode-setup` now classifies post-auth setup into:
  - existing repo + existing OpenClaw Code blueprint
  - existing repo + missing blueprint but useful repo context
  - new project where repo creation is still pending blueprint agreement
- existing repos with `PROJECT-BLUEPRINT.md` or `.openclawcode/` now stop in
  `repo-existing-blueprint-detected` instead of jumping straight into bootstrap
- existing repos without a standard blueprint now stop in
  `repo-nonstandard-context-detected` or
  `repo-missing-blueprint-required` and keep setup in blueprint-first mode
- new projects that already have a pending repo name but no blueprint agreement
  now stop in `repo-creation-pending` instead of creating the repo immediately
- `/occode-setup-retry` now acts as the explicit continue action once the
  operator is ready to move a classified setup forward into bootstrap
- `/occode-setup-status` now stays report-only for saved setup sessions instead
  of silently re-entering repo classification or bootstrap
- existing repos without an OpenClaw Code blueprint now get a first seeded
  draft from stable repo materials such as repo summary, `README.md`, and
  package metadata before the operator refines or agrees it
- existing repos that already have `PROJECT-BLUEPRINT.md` now surface a resume
  summary in chat with the current blueprint title, status, goal, and basic
  workstream/open-question/human-gate counts before bootstrap continues
- setup-state replies now also surface command-level next actions for the
  current blocker so operators can continue without inferring the right
  follow-up command
- adoption draft seeding now also checks stable repo docs such as architecture
  or roadmap notes instead of relying only on repo summary, `README.md`, and
  package metadata

Docs updated:

- `docs/openclawcode/repo-blueprint-onboarding-design.md`
- `docs/openclawcode/README.md`

Validation:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "classifies an existing repo with OpenClaw Code artifacts before bootstrap|drafts blueprint-first onboarding for an existing repo without an OpenClaw Code blueprint|keeps new-project repo creation blocked until the blueprint is agreed|continues an existing OpenClaw Code repo into bootstrap through /occode-setup-retry|shows the current GitHub identity through /occode-setup-status when auth is already ready|shows the current host GitHub login through /occode-github-status|starts a fresh GitHub login through /occode-github-switch|promotes a pending setup session to authenticated through /occode-setup-status"`

Started the Feishu QR-binding execution slice so the new onboarding copy now
has a real claim path behind it.

- added a dedicated QR binding session store under
  `src/operator-chat-targets/feishu-qr-binding.ts`
- QR sessions now support:
  - signed claim URL generation
  - lookup by binding id
  - claim signature validation
  - state transitions:
    - `pending-gateway-ready`
    - `ready-to-claim`
    - `claimed`
    - `expired`
- the bundled `openclawcode` plugin now registers
  `/openclaw/bind/feishu/*`
  - validates signed QR claim links
  - shows HTML status pages for invalid / expired / pending claims
  - when a claim arrives with Feishu `open_id`, persists
    `user:<open_id>` as the preferred target, auto-writes pairing allow-from,
    and proactively starts GitHub device auth in that DM
- `extensions/feishu/src/setup-surface.ts` continues to render the early QR
  note titled `用飞书扫码绑定`

Docs updated:

- `docs/openclawcode/README.md`
- `docs/openclawcode/feishu-qr-binding-design.md`

Validation:

- `pnpm -C /home/zyr/pros/openclawcode exec vitest run src/operator-chat-targets/feishu-qr-binding.test.ts --pool threads`
- `pnpm -C /home/zyr/pros/openclawcode exec vitest run extensions/feishu/src/setup-surface.test.ts --pool threads`
- `pnpm -C /home/zyr/pros/openclawcode exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "registers the Feishu QR binding route alongside the GitHub webhook route|claims a Feishu QR binding route hit when the request carries an open_id"`
- `git -C /home/zyr/pros/openclawcode diff --check`

Closed the remaining `bind-pending` Feishu setup gap that still forced the
operator back through pairing before GitHub login could start.

- Feishu pairing bypass for `openclawcode` setup lifecycle commands now also
  covers repos that are still configured as `bind-pending:*`
- the first real `/occode-setup` or `/occ-setup` DM on that path now claims
  the chat as the preferred operator target instead of stopping on pairing
- the setup handler now also auto-writes the same DM into the pairing
  allow-from store, so the follow-up setup and GitHub status commands keep
  working in that chat
- result: the first setup command in a fresh Feishu DM can immediately start
  the host-side GitHub device flow and return the verification URL plus code

Docs updated:

- `docs/openclawcode/README.md`
- `docs/openclawcode/chat-setup-onboarding.md`

Validation:

- `pnpm exec vitest run extensions/feishu/src/bot.test.ts --pool threads -t "allows configured openclawcode setup commands through pairing-gated DMs|allows bind-pending openclawcode setup commands through pairing-gated DMs"`
- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "claims a bind-pending Feishu setup DM and starts GitHub auth without pairing|proactively starts GitHub auth immediately for configured pairing-gated user chat targets|automatically continues from a saved awaiting-chat-pairing session into proactive GitHub auth"`

Documented the next-step Feishu first-binding direction as a QR flow.

- added `docs/openclawcode/feishu-qr-binding-design.md`
- the preferred fresh-install path is now:
  - save Feishu `app id` / `app secret`
  - show QR immediately
  - keep the primary copy simple: `用飞书扫码绑定`
  - complete binding when the gateway is ready
  - proactively continue OpenClaw Code in Feishu DM
- the design explicitly avoids exposing internal wording like
  `operator target` in product copy
- the design also makes QR issuance earlier than gateway-ready, while keeping
  actual claim and the first proactive DM gated on readiness

Extended the Feishu QR-binding slice so scans that arrive before the runner is fully ready can still complete automatically.

- QR binding sessions now persist pending Feishu identity fields:
  - `pendingClaimOpenId`
  - `pendingClaimUserId`
- the claim route now stores those fields when a signed QR hit arrives before runner readiness
- the `openclawcode` background loop now watches `ready-to-claim` sessions and finalizes them automatically once the runner is ready
- this removes the earlier need to rely on the claim page being revisited after startup finishes

Validation:

- `pnpm -C /home/zyr/pros/openclawcode exec vitest run src/operator-chat-targets/feishu-qr-binding.test.ts --pool threads`
- `pnpm -C /home/zyr/pros/openclawcode exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "registers the Feishu QR binding route alongside the GitHub webhook route|claims a Feishu QR binding route hit when the request carries an open_id"`
- `git -C /home/zyr/pros/openclawcode diff --check`

Added an explicit integration regression for the “scan early, continue later” Feishu QR path.

- the new `extensions/openclawcode/index.test.ts` case hits the signed QR route before service start
- it verifies the route stores `ready-to-claim` plus `pendingClaimOpenId`
- it then starts the runner and confirms the first proactive GitHub device-auth DM is sent automatically without another page hit

Validation:

- `pnpm -C /home/zyr/pros/openclawcode exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "finishes an early Feishu QR scan automatically after the runner becomes ready|registers the Feishu QR binding route alongside the GitHub webhook route|claims a Feishu QR binding route hit when the request carries an open_id"`

Closed the remaining Feishu QR identity gap so the scan flow no longer depends on a pre-supplied `open_id`.

- added `src/operator-chat-targets/feishu-qr-oauth.ts` to:
  - encode/decode signed QR OAuth state
  - build the Feishu or Lark `authen` authorize URL
  - exchange the callback `code` through the official SDK `authen.accessToken.create` path
- `/openclaw/bind/feishu/:bindingId` now redirects identity-less scans into an internal OAuth start route
- `/openclaw/bind/feishu/oauth/callback` now:
  - validates the signed QR session again
  - exchanges the Feishu OAuth code for `open_id`
  - claims the DM target and proactively continues setup
  - defers completion when startup is still finishing, just like the earlier direct-identity path
- added route regressions for:
  - claim route -> OAuth start -> Feishu authorize redirect
  - OAuth callback -> proactive GitHub device-auth continuation

Validation:

- `pnpm -C /home/zyr/pros/openclawcode exec vitest run src/operator-chat-targets/feishu-qr-oauth.test.ts --pool threads`
- `pnpm -C /home/zyr/pros/openclawcode exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "redirects a Feishu QR claim without open_id into Feishu OAuth|completes a Feishu OAuth callback and continues setup automatically"`
- `pnpm -C /home/zyr/pros/openclawcode exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "claims a Feishu QR binding route hit when the request carries an open_id|finishes an early Feishu QR scan automatically after the runner becomes ready|claims a bind-pending Feishu setup DM and starts GitHub auth without pairing"`

Implemented the new Feishu operator-binding stage-1 split so setup no longer mislabels loopback links as mobile-scannable QR binding.

- `extensions/feishu/src/setup-surface.ts` now distinguishes:
  - `qr-public-callback`
  - `local-browser-only`
- non-loopback callback URLs still render the QR path and browser fallback
- loopback-only setups now explicitly say:
  - phone QR scan will not work
  - open the bind URL in the same machine browser instead
  - or use Feishu Quick actions as the fallback binding path
- also fixed `buildFeishuQrBindingClaimUrl(...)` so callback bases with an existing path do not duplicate `/openclaw`

Docs updated:

- `docs/openclawcode/README.md`

Validation:

- `pnpm -C /home/zyr/pros/openclawcode exec vitest run extensions/feishu/src/setup-surface.test.ts src/operator-chat-targets/feishu-qr-binding.test.ts src/operator-chat-targets/feishu-qr-oauth.test.ts --pool threads`
- `git -C /home/zyr/pros/openclawcode diff --check`
