# 2026-03-23

Extended the repo-local `operator-program` execution-policy slice across the
remaining operator-facing progress and status surfaces.

Completed subitems in this unfinished track:

1. added `operatorProgram.updatedAt` to `.openclawcode/project-progress.json`
2. added `operatorProgram.title` to `.openclawcode/project-progress.json`
3. added `operatorProgram.summary` to `.openclawcode/project-progress.json`
4. added `operatorProgram.mutableSurfacePathCount` to `.openclawcode/project-progress.json`
5. added `operatorProgram.mutableSurfacePathsPresent` to `.openclawcode/project-progress.json`
6. added `operatorProgram.advancementRuleSummary` to `.openclawcode/project-progress.json`
7. added `operatorProgram.keepCriteriaCount` to `.openclawcode/project-progress.json`
8. added `operatorProgram.discardCriteriaCount` to `.openclawcode/project-progress.json`
9. added `operatorProgram.retryCriteriaCount` to `.openclawcode/project-progress.json`
10. added `operatorProgram.simplificationBias` to `.openclawcode/project-progress.json`
11. added `operatorProgram.linkedBlueprintPath` to `.openclawcode/project-progress.json`
12. added `operatorProgram.linkedWorkItemsPath` to `.openclawcode/project-progress.json`
13. added `operatorProgram.linkedStageGatesPath` to `.openclawcode/project-progress.json`
14. mirrored `operatorProgram.updatedAt` into operator status snapshot repo summaries
15. mirrored `operatorProgram.title` into operator status snapshot repo summaries
16. mirrored `operatorProgram.summary` into operator status snapshot repo summaries
17. mirrored mutable-surface path counts and allowlist presence into operator snapshots
18. mirrored advancement-rule summary plus keep/discard/retry counts into operator snapshots
19. mirrored simplification-bias and linked artifact paths into operator snapshots
20. surfaced the richer shared summary through `openclaw code project-progress-show`,
    `/occode-progress`, `/occode-status`, `/occode-inbox`, and CLI
    `operator-status-snapshot-show`

Operator-facing text now shows:

- operator-program meta line with title and updated timestamp
- summary line
- scope line with allowlist/path count and simplification bias
- criteria counts
- advancement-rule summary
- linked artifact paths

Docs updated:

- `docs/openclawcode/development-plan.md`
- `docs/openclawcode/operator-status-snapshot-contract.md`

Validation:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "summarizes project progress and the autonomous loop artifact|reports a stable operator status snapshot for tracked queue and status state|creates and shows a repo-local operator program artifact"`
- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "shows project progress and autopilot state through chat commands|shows pre-code discipline context through /occode-status|shows pending, running, queued, and recent activity through /occode-inbox"`

Changed proactive chat setup so a configured concrete target no longer waits
for DM pairing approval before the first outbound setup push.

- `extensions/openclawcode/index.ts` now skips the proactive pairing gate when
  collecting configured setup targets
- the background setup loop also auto-promotes existing
  `awaiting-chat-pairing` sessions into GitHub device-auth kickoff instead of
  leaving them stalled forever
- this keeps manual unauthorized slash commands in pairing mode unchanged:
  they still return pairing guidance through the Feishu surface
- updated docs:
  - `docs/openclawcode/README.md`
  - `docs/openclawcode/chat-setup-onboarding.md`

Validation:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "proactively starts GitHub auth on service start for configured chat targets|proactively starts GitHub auth immediately for configured pairing-gated user chat targets|automatically continues from a saved awaiting-chat-pairing session into proactive GitHub auth|reports pairing-gated setup state through /occode-setup-status"`
- `pnpm exec vitest run extensions/feishu/src/bot.test.ts --pool threads -t "replies with pairing guidance for blocked plugin commands even when a pairing request already exists|replies with pairing guidance for blocked slash commands even when plugin routing is unavailable|creates pairing request and drops unauthorized DMs in pairing mode"`
