# 2026-03-20

- Finished the next blueprint-first autonomy slice by landing issue
  materialization from selected work items.
  - added `src/openclawcode/issue-materialization.ts`
  - work-item issue drafts now embed stable HTML markers for:
    - `openclawcode-work-item-id`
    - `openclawcode-blueprint-revision`
  - added repo-local artifact:
    - `.openclawcode/issue-materialization.json`
  - added CLI:
    - `openclaw code issue-materialize`
    - `openclaw code issue-materialization-show`
  - added chat:
    - `/occode-materialize`
  - materialization now:
    - refreshes next-work first
    - reuses open GitHub issues only when both stable markers match
    - marks older same-work-item entries stale when the blueprint revision moved

- Added the first blueprint-aware project progress summary surface.
  - added `src/openclawcode/project-progress.ts`
  - added repo-local artifact:
    - `.openclawcode/project-progress.json`
  - added CLI:
    - `openclaw code project-progress-show`
  - added chat:
    - `/occode-progress`
  - progress now summarizes:
    - blueprint revision / status
    - selected work item
    - selected GitHub issue
    - role-routing state
    - stage-gate state
    - operator queue / current-run context when available

- Added the first supervised autonomous loop artifact and command surface.
  - added `src/openclawcode/autonomous-loop.ts`
  - added repo-local artifact:
    - `.openclawcode/autonomous-loop.json`
  - added CLI:
    - `openclaw code autonomous-loop-run --once`
    - `openclaw code autonomous-loop-show`
  - added chat:
    - `/occode-autopilot once|status|off`
  - the first loop slice now:
    - checks current next-work and operator pause/current-run state
    - materializes the next issue when ready
    - records whether the result was blocked, materialized-only, or
      materialized-and-queued

- Extended role-routing artifacts so later proofs can audit the actual route
  contract more directly.
  - `src/openclawcode/role-routing.ts` now records:
    - `runtimeCapable`
    - `rerouteCapable`
    - `resolvedBackend`
    - `resolvedAgentId`
    - `appliedSource`
  - workflow run snapshots now carry the same role-route audit fields through
    `src/openclawcode/app/run-issue.ts` and
    `src/openclawcode/contracts/types.ts`

- Tightened chat-native setup handoff and recovery messaging.
  - setup failure replies now report the failed step and retry path instead of
    only telling the operator to start over
  - bootstrap-complete setup sessions now point at `/occode-materialize` when
    issue projection is already ready

- Updated the roadmap and setup docs to reflect the new baseline.
  - `docs/openclawcode/development-plan-chat-native-autonomy.md`
  - `docs/openclawcode/chat-setup-onboarding.md`
  - Milestone 3 is now marked landed
  - Milestones 4/5/6 are now explicitly partial rather than implied future-only

- Absorbed the first reusable patterns from the `mattpocock/skills` analysis
  into the blueprint-first control plane.
  - added the evaluation note:
    - `docs/openclawcode/third-party-skill-evaluation.md`
  - `src/openclawcode/blueprint.ts` now exposes:
    - `priorityQuestion` on clarification reports
    - PRD-style prompts to capture user stories and proof-oriented success
      criteria
    - vertical-slice warnings for layer-only workstreams
    - bug-triage prompts for bug-fix workstreams
    - refactor-safety prompts for refactor workstreams
  - `src/openclawcode/work-items.ts` now classifies each planned work item as:
    - `feature`
    - `bugfix`
    - `refactor`
    - `research`
  - blueprint-derived GitHub issue drafts now embed:
    - delivery-policy guidance for thin tracer-bullet slices
    - testing-policy guidance with a red -> green -> refactor bias
    - bug-triage expectations for bug-fix work
    - refactor guardrails for structural work
    - research exit criteria for investigation work
  - chat + CLI blueprint surfaces now show the highest-priority clarification
    first instead of only dumping unordered prompts

- Extended the same analysis into chat intake and issue materialization.
  - `src/integrations/openclaw-plugin/chatops.ts` now builds mode-specific
    synthesized intake bodies for:
    - `feature`
    - `bugfix`
    - `refactor`
    - `research`
  - `extensions/openclawcode/index.ts` now classifies intake drafts and adds:
    - intake mode visibility
    - a priority clarification question
    - bug-triage-first prompts for one-line fix requests
    - refactor-safety prompts for one-line structural requests
  - `src/openclawcode/issue-materialization.ts` now persists
    `selectedWorkItemExecutionMode`
  - `/occode-materialize` now:
    - surfaces execution mode in chat
    - emits mode-specific guidance before execution
    - evaluates the full blueprint-derived issue body during suitability
      precheck instead of only the issue title

- Extended execution-mode handling into next-work selection and stage-gate
  policy.
  - `src/openclawcode/next-work.ts` now carries execution mode through the
    selected work item and changes next-work decisions accordingly
    - `feature` work can stay `ready-to-execute`
    - `bugfix` work stays autonomous but carries regression-proof guidance
    - `refactor` and `research` work now pause at `execution-start` until a
      human explicitly approves the checkpoint
  - `src/openclawcode/stage-gates.ts` now uses the selected work-item execution
    mode when deriving the `execution-start` gate
  - chat + CLI summaries now show execution mode for the selected next work
  - plugin runner lifecycle now resets worker state on service start/stop and
    ignores queue-drain attempts after shutdown, which removed a flaky runner
    test failure

- Finished the remaining visibility work for execution-mode-aware autonomy.
  - `src/openclawcode/project-progress.ts` now persists:
    - `nextWorkBlockingGateId`
    - `nextWorkPrimaryBlocker`
    - `selectedWorkItemExecutionMode`
  - `src/openclawcode/autonomous-loop.ts` now persists the same fields so the
    one-shot loop artifact explains why it stopped
  - `/occode-progress`, `/occode-autopilot`, and the CLI summaries now answer:
    - which gate is blocking progress
    - which execution mode the selected work item is in
    - what the first blocker is

- Deepened project-progress and autopilot reporting for active runs.
  - `src/openclawcode/project-progress.ts` now also persists:
    - `roleRouteSummary`
    - `operator.currentRunStage`
    - `operator.currentRunBranchName`
    - `operator.currentRunPullRequestNumber`
    - `operator.currentRunPullRequestUrl`
    - `operator.currentRunStatusUpdatedAt`
  - `src/openclawcode/autonomous-loop.ts` now mirrors:
    - `roleRouteSummary`
    - `currentRunStage`
    - `currentRunBranchName`
    - `currentRunPullRequestNumber`
    - `currentRunPullRequestUrl`
  - chat + CLI progress surfaces now answer:
    - who is routed to each engineering role right now
    - which stage the current run is in
    - which branch / PR the active run is using when one exists

- Extended the first autopilot slice into a bounded repeat-loop controller.
  - `src/openclawcode/autonomous-loop.ts` now supports:
    - `requestedIterationCount`
    - `completedIterationCount`
    - `iterations`
    - `queuedRunCount`
    - `mode: repeat`
  - repeat mode now stops cleanly when:
    - a run is already queued
    - a run is already active
    - provider pause is active
    - the next-work gate blocks execution
    - no queue handoff is configured after issue materialization
  - CLI + chat surfaces now expose:
    - `openclaw code autonomous-loop-run --iterations <n>`
    - `/occode-autopilot repeat [count]`
    - iteration-history summaries for the first few loop steps

- Made autopilot outcomes more actionable after they stop.
  - `src/openclawcode/autonomous-loop.ts` now records `nextSuggestedCommand`
  - CLI + chat summaries now surface that next step directly so operators can
    move from "why did it stop?" to "what should I do next?" without opening
    another artifact first
  - chat summaries now map the common next steps into operator-native commands:
    - `/occode-gates`
    - `/occode-materialize`
    - `/occode-progress`
    - `/occode-start`
    instead of echoing only the lower-level CLI form
  - CLI summaries now also show per-iteration `message=` details so the
    terminal path exposes the same stop-context quality as chat

- Validation:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts extensions/openclawcode/index.test.ts --pool threads`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Hardened next-step continuity across CLI, artifacts, and chat progress views.
  - added `src/openclawcode/next-suggested-command.ts` so CLI-to-chat command
    mapping is shared instead of duplicated inside the plugin presenter
  - `src/openclawcode/project-progress.ts` now persists both:
    - `nextSuggestedCommand`
    - `nextSuggestedChatCommand`
  - `src/openclawcode/autonomous-loop.ts` now mirrors the same pair so
    autopilot stop states preserve both the low-level CLI recovery action and
    the chat-native recovery action
  - `src/openclawcode/project-progress.ts` now reads older
    `.openclawcode/project-progress.json` files defensively and backfills the
    new chat-native field when the artifact predates this slice
  - `extensions/openclawcode/index.ts` now prefers the persisted
    `nextSuggestedChatCommand` and only falls back to live conversion when
    older artifacts are missing it
  - `src/commands/openclawcode.ts` now prints both suggested forms so terminal
    operators can audit exactly what chat will recommend

- Validation for the next-step continuity slice:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "shows project progress and autopilot state through chat commands|shows why autopilot is blocked for refactor work through chat commands|runs a supervised repeat autopilot loop until queued work blocks the next iteration|stops autopilot cleanly when queued work already exists for the repository"`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Added active-workstream summaries to project progress and autopilot surfaces.
  - `src/openclawcode/next-work.ts` now carries `workstreamIndex` on the
    selected work item so downstream artifacts know which blueprint workstream
    is active
  - `src/openclawcode/project-progress.ts` now persists:
    - `activeWorkstreamIndex`
    - `activeWorkstreamCount`
    - `activeWorkstreamTitle`
    - `activeWorkstreamSummary`
  - `src/openclawcode/autonomous-loop.ts` now mirrors the same workstream
    fields and also records `activeWorkstreamSummary` per iteration so repeat
    histories stay legible after the loop blocks
  - CLI and chat summaries now surface that active workstream directly before
    the selected work item details, so the operator can see the current
    blueprint lane rather than only the raw issue candidate
  - `docs/openclawcode/project-blueprint-schema.md` now documents the new
    workstream-facing artifact fields

- Validation for the active-workstream slice:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "shows project progress and autopilot state through chat commands|runs a supervised repeat autopilot loop until queued work blocks the next iteration"`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Taught CLI autopilot to hand off work into the operator queue.
  - `src/commands/openclawcode.ts` now resolves the repo's operator config from
    `OPENCLAW_STATE_DIR` / `--state-dir` and reuses the chatops queue contract
    when `openclaw code autonomous-loop-run` materializes an issue
  - CLI autopilot now:
    - queues directly when the repo is configured in operator state
    - writes the same queued status text into the durable store
    - falls back to `materialized-only` when no operator repo config exists
  - `src/openclawcode/autonomous-loop.ts` now distinguishes queue outcomes as:
    - `queued`
    - `gated`
    - `already-tracked`
  - this fixes the previous semantic gap where any non-queued result looked
    like "already tracked" even when the real reason was a future gate hold
  - `extensions/openclawcode/index.ts` now returns the richer queue outcome too
    so CLI and chat stay on the same queue-handoff contract

- Validation for the CLI autopilot queue slice:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "runs a supervised repeat autopilot loop until queued work blocks the next iteration|shows why autopilot is blocked for refactor work through chat commands|shows project progress and autopilot state through chat commands"`

- Remaining external proof gap for this slice:
  - real operator proof is still needed for:
    - `/occode-materialize` on the live host
    - `/occode-progress` during a real active run
    - `/occode-autopilot once` against the long-lived operator queue
    - setup failure recovery against real `gh` expiry / repo-create / bootstrap
      failure conditions

- Fixed Milestone 6 next-work alignment so approved execution-start gates
  actually unblock refactor/research work.
  - `src/openclawcode/next-work.ts` now derives the stage-gate artifact and
    makes next-work decisions from gate readiness instead of treating
    execution-mode guidance as a permanent autonomous blocker
  - `next-work` now respects recorded decisions for:
    - `goal-agreement`
    - `work-item-projection`
    - `execution-routing`
    - `execution-start`
  - once `execution-start` is approved, refactor slices now move to
    `ready-to-execute` instead of staying stuck at `blocked-on-human`
  - progress/autopilot behavior follows automatically because both consume the
    same next-work selection
  - targeted CLI/chat tests were added for both sides of the state change:
    - blocked before approval
    - ready/materializable after approval
  - chat autopilot proof now includes an explicit operator sender identity so
    the queue handoff path is exercised instead of silently falling back to
    `materialized-only`

- Validation for the stage-gate alignment slice:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "holds refactor work behind execution-start human approval in next-work selection|unblocks refactor next-work selection after execution-start approval is recorded|keeps autonomous loop blocked with explicit execution-mode context for refactor work|allows autonomous loop materialization after execution-start approval for refactor work"`
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "shows why autopilot is blocked for refactor work through chat commands|unblocks refactor progress and autopilot after execution-start approval in chat"`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Deepened active-run visibility and routing audit in progress/autopilot artifacts.
  - `src/openclawcode/project-progress.ts` now persists the full
    `roleRoutes` array from the role-routing artifact instead of exposing only
    the flattened `roleRouteSummary`
  - `src/openclawcode/autonomous-loop.ts` now mirrors those structured
    `roleRoutes` entries and also persists `currentRunIssueKey`
  - CLI and chat autopilot summaries now print `Current run: owner/repo#issue`
    when the loop is blocked because another run is already active
  - this keeps machine-readable progress/autopilot artifacts aligned with the
    richer role-routing contract while also answering the operator question
    "which issue is running right now?" directly from `/occode-autopilot`

- Validation for the active-run visibility slice:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "surfaces active-run stage and role routing through project progress and autopilot artifacts"`
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "shows active-run stage and role routing through progress and autopilot chat commands"`

- Deepened rerun lineage visibility across `/occode-status` and `/occode-inbox`.
  - `src/integrations/openclaw-plugin/store.ts` now persists manual-takeover
    rerun metadata in issue status snapshots:
    - `rerunManualTakeoverRequestedAt`
    - `rerunManualTakeoverActor`
    - `rerunManualTakeoverWorktreePath`
    - `rerunManualResumeNote`
  - `extensions/openclawcode/index.ts` now extends rerun ledger summaries with:
    - review decision / review timestamp
    - review summary / review URL
    - manual resume actor / timestamp
    - manual worktree path / manual note
  - `/occode-status` now prints that rerun lineage as top-level operator-visible
    context instead of hiding it only inside queue internals
  - `/occode-inbox` now keeps the same lineage visible for queued, running, and
    recent rerun entries, including structured resumes after manual edits

- Validation for the rerun-lineage slice:
  - `pnpm exec vitest run src/openclawcode/testing/openclaw-plugin-store.test.ts --pool threads -t "persists structured workflow run snapshots alongside issue statuses"`
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "queues a structured rerun after manual edits and clears the takeover hold|shows rerun review and manual resume lineage through /occode-status"`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Closed the explicit preview gap in chat-native intake drafts.
  - `extensions/openclawcode/index.ts` now registers `/occode-intake-preview`
    so operators can reopen the current pending draft without regenerating or
    editing it
  - the preview command reuses the same pending-draft summary builder as
    intake/edit/choose, which keeps body preview, clarification prompts, and
    scoped variants on one contract
  - pending draft guidance now also points operators at
    `/occode-intake-preview` when they want one more review pass before issue
    creation
  - `docs/openclawcode/chat-intake-styles.md` now documents the preview step as
    part of the narrowing flow

- Validation for the intake-preview slice:
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "pending chat intake draft through /occode-intake-preview|one-line /occode-intake request into a pending draft with clarification prompts|offers multiple scoped drafts for ambiguous one-line intake requests|supports editing and confirming a pending chat intake draft before issue creation"`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Added a real clarification loop for chat-native intake drafts.
  - `extensions/openclawcode/index.ts` now registers `/occode-intake-answer`
    so operators can answer one pending clarification without manually
    rebuilding the whole draft
  - pending drafts now persist structured clarification responses and reuse
    them in preview/confirm output, so the final GitHub issue body carries the
    operator's answers under a dedicated `Clarifications from operator` section
  - answering one clarification suppresses that exact question from the next
    draft refresh while leaving any remaining prompts visible

- Tightened the operator-facing escalation path for high-risk intake.
  - prechecked escalation statuses now record the explicit next step instead of
    only saying `Suitability: escalate`
  - chat intake escalation replies now tell the operator to inspect
    `/occode-status` first and only use `/occode-start-override` after a human
    accepts a one-run exception
  - `docs/openclawcode/chat-intake-styles.md` now documents both the
    clarification-answer flow and the escalation path

- Validation for the clarification/escalation slice:
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "prechecks high-risk /occode-intake issues into escalated snapshots|one-line /occode-intake request into a pending draft with clarification prompts|records clarification answers into the pending chat intake draft before confirmation|shows the current pending chat intake draft through /occode-intake-preview|supports editing and confirming a pending chat intake draft before issue creation|can choose a scoped draft before confirming chat intake|queues suitability overrides through /occode-start-override"`
  - `pnpm exec vitest run src/openclawcode/testing/openclaw-plugin-store.test.ts src/commands/openclawcode.test.ts --pool threads -t "persists one pending intake draft per repo and chat target|logs openclawcode operator status snapshot"`
  - `pnpm exec vitest run src/openclawcode/testing/run-issue.test.ts --pool threads -t "captures runtime routing selections before build and verification"`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Switched the missing-GitHub-auth onboarding note to chat-first guidance.
  - `src/wizard/setup.code.ts` no longer tells the operator only to run
    `gh auth login` manually when OpenClaw Code auth is missing
  - the note now prefers the chat-native path:
    - send `/occode-setup` in an already configured OpenClaw chat surface
    - let OpenClaw launch GitHub device auth and continue setup in chat
  - `gh auth login` remains as an explicit CLI fallback instead of the primary
    instruction

- Validation for the chat-first auth guidance slice:
  - `pnpm exec vitest run src/wizard/setup.code.test.ts --pool threads`

- Added a dedicated chat-native policy view for openclawcode operators.
  - `extensions/openclawcode/index.ts` now registers `/occode-policy`
    for both repo-level and issue-level policy inspection
  - repo-level policy output now mirrors the CLI policy snapshot in chat with:
    - suitability allowlist / denylist labels
    - build guardrail thresholds
    - provider auto-pause classes
    - explicit suitability and merge override entrypoints
  - issue-level policy output now layers the tracked issue snapshot on top of
    that baseline so operators can see the current suitability decision,
    override state, and auto-merge block reason in one place

- Validation for the chat policy slice:
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "repo-level policy guidance through /occode-policy|issue-specific policy context through /occode-policy|shows suitability override details through /occode-status|shows auto-merge policy explanation through /occode-status when auto-merge is disallowed"`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Added policy shortcuts to `/occode-inbox` for policy-sensitive issues.
  - recent and active inbox entries now append `policy: /occode-policy owner/repo#issue`
    whenever the tracked snapshot shows a non-default suitability decision,
    manual suitability override, or blocked auto-merge policy
  - this keeps operators one hop away from the full policy view instead of
    making them infer override paths from the ledger text alone

- Documented the current narrow auto-merge policy as a standalone operator
  contract.
  - added `docs/openclawcode/auto-merge-policy.md` with the exact eligibility
    conditions mirrored from `resolveAutoMergePolicy(...)`
  - linked that doc from `policy.md`, `README.md`, and the `/occode-policy`
    chat output so the human-facing explanation matches the runtime rule set

- Validation for the inbox-policy and auto-merge-doc slice:
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "repo-level policy guidance through /occode-policy|issue-specific policy context through /occode-policy|shows suitability override details through /occode-status|shows auto-merge policy explanation through /occode-status when auto-merge is disallowed|shows policy shortcuts for queued policy-sensitive issues through /occode-inbox|shows recent activity through /occode-inbox"`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Added executable approval actions directly to `/occode-inbox` pending entries.
  - manual pending approvals now render:
    - `action: /occode-start owner/repo#issue`
  - execution-start gated approvals now render:
    - `action: /occode-gates owner/repo`
    - `approve: /occode-gate-decide owner/repo execution-start approved [note]`
  - this keeps inbox pending rows actionable without making operators jump to
    other docs or infer the right command from status text alone

- Validation for the inbox-approval-actions slice:
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "shows recent activity through /occode-inbox|shows execution-start approval actions through /occode-inbox|shows policy shortcuts for queued policy-sensitive issues through /occode-inbox"`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Added next-step actions for queued and running work in `/occode-inbox`.
  - running entries now append:
    - `action: /occode-status owner/repo#issue`
    - `takeover: /occode-takeover owner/repo#issue [note]`
  - queued entries now append:
    - `action: /occode-status owner/repo#issue`
    - `skip: /occode-skip owner/repo#issue [reason]`
  - this keeps `/occode-inbox` usable as a compact operator console instead of
    just a passive ledger

- Validation for the inbox-next-actions slice:
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "shows recent activity through /occode-inbox|shows policy shortcuts for queued policy-sensitive issues through /occode-inbox|shows execution-start approval actions through /occode-inbox"`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Tightened the Feishu DM pairing UX for blocked OpenClaw Code slash commands.
  - unauthorized `dmPolicy: pairing` DMs now detect plugin slash commands before the auth return path
  - when an operator sends `/occode-setup` (or another plugin command) before pairing is approved, Feishu now replies with:
    - the pairing code
    - an explicit note that the chat command is blocked pending pairing approval
    - a direct instruction to resend the same command after approval
  - if a pairing request already exists, slash commands now still return the reminder instead of failing silently

- Validation for the Feishu pairing-command reminder slice:
  - `pnpm exec vitest run src/pairing/pairing-messages.test.ts --pool threads`
  - `pnpm exec vitest run extensions/feishu/src/bot.test.ts --pool threads -t "creates pairing request and drops unauthorized DMs in pairing mode|replies with pairing guidance for blocked plugin commands even when a pairing request already exists|normalizes invisible characters and slash variants before plugin-command probing"`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Closed the first blueprint-discussion completion slice for openclawcode.
  - blueprint summaries and clarification reports now include:
    - validation errors
    - validation warnings
    - clarification-history counts
  - `openclaw code blueprint-set-status --status agreed` now refuses to agree a blueprint while validation errors remain
  - added repo-local clarification-history persistence at `.openclawcode/blueprint-discussion.json`
  - added `/occode-blueprint-answer` so operators can answer one clarification from chat, write it back into the right blueprint section, refresh stage gates, and keep an auditable history entry
  - `/occode-blueprint` and other blueprint update replies now show validation state and clarification-history counts directly in chat
  - corrected `openclaw-capability-mapping.md` to mark already-landed takeover/resume/override items plus the new blueprint discussion closure items as complete

- Validation for the blueprint-discussion completion slice:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "records the explicit blueprint agreement checkpoint|blocks blueprint agreement while validation errors remain|reports clarification questions and suggestions for the default blueprint scaffold"`
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "marks the blueprint as agreed from chat|blocks /occode-blueprint-agree when blueprint validation errors remain|answers one blueprint clarification from chat and records history|updates arbitrary blueprint sections from chat without manual file edits"`

- Deepened blueprint-backed work-item identity and GitHub projection.
  - `openclaw code blueprint-decompose` is now incremental instead of rebuilding
    a fresh backlog every time
  - planned work items now persist:
    - richer `class` values (`feature`, `bugfix`, `docs`, `sync`,
      `validation`, `incident`)
    - stable content `fingerprint`
    - live `githubIssue.current` plus `githubIssue.history`
  - when a later blueprint revision removes a planned item, the inventory now
    keeps it as `superseded` instead of silently dropping it
  - `openclaw code issue-materialize` now writes the selected live GitHub issue
    back into `.openclawcode/work-items.json`
  - issue reuse now keys off the stable work-item fingerprint so revision-only
    blueprint edits do not reopen duplicate issues
  - `next-work` and `stage-gates` now ignore `superseded` backlog entries when
    selecting execution candidates
  - corrected `project-blueprint-schema.md` and
    `openclaw-capability-mapping.md` so the documented backlog/projection state
    matches the landed implementation

- Validation for the incremental work-item / issue-projection slice:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "derives and persists repo-local work items from an agreed blueprint|adds bug-triage and refactor guardrails to blueprint-derived issue drafts|classifies blueprint-derived work items into richer work-item classes|preserves work-item ids, status, and GitHub links across incremental re-decomposition|materializes the selected work item into a GitHub issue artifact|reuses an existing linked GitHub issue after blueprint revision changes without reopening duplicates"`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Expanded runtime discovery so `openclawcode` can surface live operator/repo problems even before a persisted discovery artifact is refreshed.
  - `deriveProjectDiscoveryInventory(...)` now augments persisted signals with runtime evidence for:
    - setup-check regressions (`setup-check-regression`)
    - active provider pauses (`provider-pause-active`)
    - upstream drift against `upstream/main` (`upstream-sync-drift`)
  - discovered runtime items now carry explicit work-item class and execution-mode shaping so downstream selection can tell validation, incident, and sync work apart
  - `deriveProjectStageGateArtifact(...)` now derives from live discovery inventory instead of only the last saved discovery artifact, fixing the stale-discovery mismatch where `next-work` could see maintenance work but stage gates would not block on it yet

- Validation for the runtime-discovery slice:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "discovers a missing work-item artifact from an agreed blueprint|discovers stale work-item drift after the blueprint changes|discovers setup-check regressions, provider pauses, and upstream sync drift|selects discovery-maintenance work first when the work-item artifact is stale"`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Hardened chat-native setup status continuity on the operator host.
  - `extensions/openclawcode/index.ts` now starts with an immediate setup-session
    poll and keeps polling saved sessions that are still in
    `awaiting-github-device-auth`
  - browser-side GitHub device approval now pushes the next setup message back
    to the originating chat automatically on both success and failure paths
  - setup replies now explicitly tell the operator that OpenClaw Code will push
    the next status automatically and that `/occode-setup-status` is the manual
    fallback, not the only continuation path
  - bootstrap summaries and setup-check readiness now also surface:
    - plugin activation state
    - `chatSetupRoutingReady`
    - explicit `repair-plugin-activation` next action when slash-command
      routing is still blocked by plugin config
  - persisted setup sessions now keep one-shot notification state so the same
    auth-completion message is not replayed repeatedly after restart
  - updated planning/operator docs so the two new-machine findings are now
    recorded as landed hardening, while live operator proof remains open

- Validation for the setup-hardening slice:
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads -t "starts chat-native setup by launching GitHub device auth and persisting the session|promotes a pending setup session to authenticated through /occode-setup-status|replays the saved bootstrap summary through /occode-setup-status|pushes the next setup message automatically after GitHub auth completes|pushes a retry message automatically after GitHub auth fails"`
  - `pnpm exec vitest run src/openclawcode/testing/openclaw-plugin-store.test.ts --pool threads -t "persists one setup session per chat target"`
  - `pnpm exec vitest run src/openclawcode/testing/setup-check.test.ts --pool threads -t "emits machine-readable JSON with --json|fails readiness explicitly when plugin activation is missing|distinguishes a passing built startup proof from a down live gateway in readiness json"`
  - `pnpm exec vitest run src/wizard/setup.code.test.ts --pool threads`
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads -t "bootstrap|setup-check|promotion gate|rollback suggestion"`
