## Upstream Sync Refresh

Refreshed the upstream baseline after confirming the real divergence had grown
to `239 ahead / 199 behind` relative to `openclaw/openclaw:main`.

- branched from local `main` to `sync/upstream-2026-03-16`
- fetched upstream to `413d2ff3da`
- merged `upstream/main` into the sync branch

Resolved merge conflicts in:

- `package.json`
- `tsdown.config.ts`
- `src/plugins/discovery.ts`
- `src/plugins/discovery.test.ts`
- `extensions/feishu/src/bot.ts`
- `extensions/feishu/src/bot.test.ts`

Conflict-resolution notes:

- kept upstream `runtime-postbuild.mjs` build flow and preserved the local
  `copy-extension-manifests.ts` postbuild step
- kept both bundled plugin build-entry discovery paths in `tsdown.config.ts`
- preserved the upstream bundled-plugin discovery metadata while keeping the
  local candidate-source and setup-source annotations
- combined the local Feishu plugin-command mocks with the upstream ACP/session
  binding mocks and resets

Validation notes:

- targeted conflict surface passed:
  - `pnpm exec vitest run src/plugins/discovery.test.ts extensions/feishu/src/bot.test.ts --pool threads`
- first `vitest.openclawcode.config.mjs` rerun failed because the dedicated
  OpenClawCode config did not carry the upstream `openclaw/plugin-sdk/*` alias
  table, and the sync now reaches `extensions/discord/src/account-inspect.ts`
- fixed that by upgrading `vitest.openclawcode.config.mjs` to a
  `defineConfig(...)` file with `openclaw/extension-api` and
  `openclaw/plugin-sdk/*` aliases copied from the main repo Vitest config
- first `pnpm build` rerun then failed in `build:plugin-sdk:dts` because
  `extensions/zalouser/src/zca-js-exports.d.ts` was not included in
  `tsconfig.plugin-sdk.dts.json`
- fixed that by explicitly including the shim in
  `tsconfig.plugin-sdk.dts.json`

Final validation passed:

- `pnpm exec vitest run src/plugins/discovery.test.ts extensions/feishu/src/bot.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Field notes:

- upstream now warns that Node `22.16.0` is already on the deprecation floor
  for `tsdown`; the next safe floor is `22.18.0+`
- the sync branch is now suitable for push and later promotion proof, but
  `main` should stay untouched until that proof is done

## Setup-Check Model Inventory Hardening

The first strict readiness run on `sync/upstream-2026-03-16` still had one
warning even after the merge itself was green:

- `unable to parse model inventory from models list --json`

Root cause:

- upstream `models list --json` can now emit config-warning text before the
  JSON payload
- `scripts/openclawcode-setup-check.sh` still assumed stdout was raw JSON and
  called `json.loads(...)` directly

Fixes:

- `scripts/openclawcode-setup-check.sh`
  - now extracts the final JSON payload from mixed stdout instead of assuming
    the entire stdout buffer is JSON
- `src/openclawcode/testing/setup-check.test.ts`
  - now covers the real mixed-output shape where warning text prefixes the
    model inventory JSON payload

Validation passed:

- `pnpm exec vitest run src/openclawcode/testing/setup-check.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`
- `./scripts/openclawcode-setup-check.sh --strict --json`
- `./scripts/openclawcode-setup-check.sh --strict --probe-built-startup --json`

Current refreshed-branch readiness on `sync/upstream-2026-03-16`:

- `19 pass / 0 warn / 0 fail` for strict setup-check
- `20 pass / 0 warn / 0 fail` for strict setup-check with built-startup proof
- `readiness.lowRiskProofReady = true`
- `readiness.promotionReady = true`
- `readiness.builtStartupProofReady = true`
- `readiness.nextAction = "ready-for-low-risk-proof"`

## Low-Risk Proof: Validation Issue #124

Used the refreshed sync branch for a real low-risk command-layer proof against
validation issue `#124` (`publishedPullRequestBody`).

Important field notes from the two reruns:

- the first direct `code run` attempt on `sync/upstream-2026-03-16` hit
  sandbox edit ambiguity in `src/commands/openclawcode.ts` and
  `src/commands/openclawcode.test.ts`
- that exposed a prompt-quality gap rather than a generic edit bridge failure,
  so the command-layer builder guidance was tightened to prefer wider
  multi-line replacements and explicit retry behavior after ambiguous edits
- after that prompt hardening, rerun
  `zhyongrui-openclawcode-124-1773628889091` progressed cleanly through:
  - `draft-pr-opened`
  - `verifying`
  - `ready-for-human-review`
- the rerun reused draft PR `#125`
- the verifier approved the slice for human review and the run artifact now
  records that `publishedPullRequestBody` is aligned with issue `#124`

Backport notes:

- the issue worktree branch already contained the real implementation commit
  for `publishedPullRequestBody`
- that patch was cherry-picked back onto `sync/upstream-2026-03-16`
- the sync branch also picked up the additional url-only published assertion
  proven by the rerun, so the refreshed baseline now explicitly checks:
  - populated published-PR body
  - null when no published PR exists
  - populated body when publication is inferred from URL-only history

Validation passed on the sync branch after the backport:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

## Automatic Resume After Execution-Start Approval

Closed the first actual handoff-to-resume loop for blueprint-first chatops.

Implemented:

- `src/integrations/openclaw-plugin/store.ts` now tracks `approvalKind` on
  pending approvals and supports atomic pending-upsert for blocked start
  attempts
- auto webhook intake and `/occode-intake` now tag held approvals as
  `execution-start-gated`
- `/occode-start` now converts a blocked start attempt into the same
  resumable hold instead of only returning guidance
- `/occode-gate-decide execution-start approved ...` now automatically
  promotes held execution-start approvals into the durable queue

Protection kept in place:

- untouched manual approvals still do not auto-resume

Validation passed:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`

## Auto-Merge Policy Explainability In /occode-status

`/occode-status` now shows why a run at `ready-for-human-review` is not
eligible for auto-merge.

Implemented:

- extracted shared workflow-derived helpers for:
  - auto-merge policy eligibility/reason
  - auto-merge disposition/reason
- plugin status snapshots now persist:
  - `autoMergePolicyEligible`
  - `autoMergePolicyReason`
  - `autoMergeDisposition`
  - `autoMergeDispositionReason`
- `/occode-status` now appends:
  - `Auto-merge policy: blocked | <reason>`
    when the tracked run is at `ready-for-human-review` and not eligible

Why this matters:

- operators can now see both major policy blockers in chat:
  - suitability
  - auto-merge
- the store, command JSON, and chat surfaces now derive auto-merge policy from
  the same shared logic instead of duplicating behavior

Validation passed:

- `pnpm exec vitest run src/commands/openclawcode.test.ts src/openclawcode/testing/openclaw-plugin-store.test.ts extensions/openclawcode/index.test.ts --pool threads`

## Promotion And Rollback Readiness In Chat

Operator chat surfaces now consume the real setup-check readiness probe.

Implemented:

- `/occode-inbox` now shows:
  - promotion readiness
  - low-risk/fallback proof readiness
  - rollback target/readiness
  - setup-check summary counts
- new command:
  - `/occode-promotion-checklist`
- the command and inbox both call:
  - `scripts/openclawcode-setup-check.sh --strict --json`
    through the plugin runtime command runner

Why this matters:

- operators no longer need to infer promotion state from scattered shell output
- readiness now comes from the existing real host probe instead of a second
  duplicate heuristic

Validation passed:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`
- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

## Pre-Run Provider Reroute Controls

Added the first writable provider-routing path instead of only showing role
routing summaries.

Implemented:

- `src/openclawcode/blueprint.ts`
  - added provider-role parsing helpers
  - added blueprint mutation for a single `Provider Strategy` role assignment
- `src/commands/openclawcode.ts`
  - added `openclaw code blueprint-set-provider-role`
  - the command now refreshes role-routing and stage-gates after each change
- `extensions/openclawcode/index.ts`
  - added `/occode-routing`
  - added `/occode-route-set`
  - chat-side reroute updates now mutate the blueprint and refresh
    role-routing/stage-gate artifacts immediately
- tests now cover:
  - CLI provider-role mutation
  - chat routing summary
  - chat route mutation

Important scope boundary:

- this is a pre-run reroute surface
- it does not yet switch providers in the middle of an active run

Validation passed:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`

## Validation And Release Contract Cleanup

Closed the remaining contract-only checklist gaps around validation-pool and
release-readiness artifacts.

Implemented:

- `contractVersion: 1` for:
  - `openclaw code list-validation-issues --json`
  - `openclaw code reconcile-validation-issues --json`
- explicit docs for:
  - `docs/openclawcode/validation-pool-contract.md`
  - `docs/openclawcode/release-artifacts-contract.md`

Code and docs updated:

- `src/commands/openclawcode.ts`
- `src/commands/openclawcode.test.ts`
- `docs/openclawcode/validation-pool-contract.md`
- `docs/openclawcode/release-artifacts-contract.md`
- `docs/openclawcode/README.md`
- `docs/openclawcode/development-plan.md`
- `docs/openclawcode/master-delivery-checklist.md`

Validation passed:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`

## Validation-Pool Reconciliation Contract

Stabilized the repo-local validation-pool JSON so external automation does not
have to infer whether it is reading a compatible shape.

Implemented:

- `contractVersion: 1` for:
  - `openclaw code list-validation-issues --json`
  - `openclaw code reconcile-validation-issues --json`
- new contract doc:
  - `docs/openclawcode/validation-pool-contract.md`

Code and docs updated:

- `src/commands/openclawcode.ts`
- `src/commands/openclawcode.test.ts`
- `docs/openclawcode/validation-pool-contract.md`
- `docs/openclawcode/README.md`
- `docs/openclawcode/development-plan.md`
- `docs/openclawcode/master-delivery-checklist.md`

Validation passed:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`

## Blueprint Stage Gates

Added the first repo-local stage-gate artifact instead of leaving human
handoff as a docs-only promise.

Implemented:

- `openclaw code stage-gates-refresh`
- `openclaw code stage-gates-show`
- `openclaw code stage-gates-decide`

Code changes:

- added `src/openclawcode/stage-gates.ts`
  - derives `.openclawcode/stage-gates.json`
  - tracks five gate ids:
    - `goal-agreement`
    - `work-item-projection`
    - `execution-routing`
    - `execution-start`
    - `merge-promotion`
  - records durable decisions:
    - `approved`
    - `changes-requested`
    - `blocked`
- updated `src/openclawcode/index.ts`
  - exported the new stage-gate helpers
- updated `src/commands/openclawcode.ts`
  - wired refresh/show/decide commands and JSON output
- updated `src/cli/program/register.code.ts`
  - registered the new CLI commands and examples
- extended `src/commands/openclawcode.test.ts`
  - covered gate derivation, readiness counts, and persisted human decisions

What this closes:

- repo-local stage-gate decisions are now machine-readable
- the blueprint-first plan no longer depends on chat-only memory for approval
  state

Remaining gap:

- gate decisions do not yet steer the live runtime or chat surfaces
- manual worktree takeover and resume are still future slices

Post-backport validation inventory:

- `openclaw code list-validation-issues --json` now classifies `#124` as
  `implemented`

## Blueprint-First Phase B3 Footing

Extended the repo-local blueprint control plane so it can now emit and persist
planned work items instead of only storing the agreed target.

Code landed:

- `src/openclawcode/blueprint.ts`
  - added machine-readable blueprint revision metadata:
    - `revisionId`
    - `contentSha256`
    - `statusChangedAt`
    - `defaultedSections`
    - `providerRoleAssignments`
    - workstream/open-question/human-gate counts
- `src/openclawcode/work-items.ts`
  - introduced the first repo-local work-item model
  - derives planned work items from `PROJECT-BLUEPRINT.md`
  - persists `.openclawcode/work-items.json`
  - marks the artifact stale when the blueprint revision changes
  - attaches GitHub issue drafts to each planned work item
- `src/commands/openclawcode.ts`
  - added:
    - `openclaw code blueprint-decompose`
    - `openclaw code work-items-show`
- `src/cli/program/register.code.ts`
  - registered the new CLI surface and help examples

Validation passed:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm build`

Documentation updated:

- `project-blueprint-schema.md`
- `blueprint-first-delivery-plan.md`
- `development-plan.md`
- `master-delivery-checklist.md`
- `README.md`

## Discovery And Role Routing

Continued the same blueprint-first track with the first repo-local discovery
pipeline and the first provider-neutral role-routing artifact.

Code landed:

- `src/openclawcode/discovery.ts`
  - added machine-readable discovery evidence records
  - added stable dedupe keys
  - added severity and priority scoring
  - added discovered work-item drafts for:
    - missing work-item artifacts
    - stale work-item artifacts
    - unresolved blueprint open questions
- `src/openclawcode/role-routing.ts`
  - added a provider-neutral role plan for:
    - planner
    - coder
    - reviewer
    - verifier
    - doc-writer
  - normalizes Codex and Claude Code into shared adapter ids
  - persists `.openclawcode/role-routing.json`
- `src/commands/openclawcode.ts`
  - added:
    - `openclaw code discover-work-items`
    - `openclaw code role-routing-refresh`
    - `openclaw code role-routing-show`

Validation passed:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

## Release Receipts

Closed the remaining repo-local release-artifact gap by adding explicit
promotion and rollback receipts instead of leaving successful release actions in
chat scrollback only.

Implemented:

- `openclaw code promotion-receipt-record`
- `openclaw code promotion-receipt-show`
- `openclaw code rollback-receipt-record`
- `openclaw code rollback-receipt-show`
- new artifacts:
  - `.openclawcode/promotion-receipt.json`
  - `.openclawcode/rollback-receipt.json`

Behavior:

- promotion receipts snapshot:
  - source branch and commit
  - promoted branch and commit
  - linked promotion-gate and rollback-suggestion artifacts
  - readiness and proof signals captured at record time
- rollback receipts snapshot:
  - the regressed source branch and commit
  - the restored branch and commit
  - linked rollback-suggestion and promotion-gate artifacts
  - the operator-facing rollback reason captured at record time

Code and docs updated:

- `src/openclawcode/promotion-artifacts.ts`
- `src/commands/openclawcode.ts`
- `src/cli/program/register.code.ts`
- `src/commands/openclawcode.test.ts`
- `docs/openclawcode/release-artifacts-contract.md`
- `docs/openclawcode/README.md`
- `docs/openclawcode/development-plan.md`
- `docs/openclawcode/blueprint-first-delivery-plan.md`
- `docs/openclawcode/master-delivery-checklist.md`

Validation passed:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`

## Merge-Promotion Runtime Wiring

Completed the first live runtime path for explicit merge-policy exceptions.

Implemented:

- approved `pull_request_review` webhooks now merge automatically when:
  - `mergeOnApprove` is enabled and the tracked run is policy-eligible, or
  - the operator explicitly approved the `merge-promotion` stage gate
- `/occode-gate-decide merge-promotion approved ...` now attempts the merge
  immediately when exactly one review-approved candidate is waiting in the repo
- merge success or failure now persists back into the tracked status snapshot
  with updated auto-merge disposition fields

Code and docs updated:

- `extensions/openclawcode/index.ts`
- `extensions/openclawcode/index.test.ts`
- `docs/openclawcode/README.md`
- `docs/openclawcode/blueprint-first-delivery-plan.md`
- `docs/openclawcode/development-plan.md`
- `docs/openclawcode/master-delivery-checklist.md`

Validation passed:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`
- `pnpm exec vitest run src/commands/openclawcode.test.ts extensions/openclawcode/index.test.ts src/openclawcode/testing/openclaw-plugin-store.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

## Operator Status Snapshot Contract

Added the first stable machine-readable contract for chat-visible operator state
instead of forcing operators or automation to scrape `/occode-status` text.

Implemented:

- `openclaw code operator-status-snapshot-show --json`
- `contractVersion: 1` for the operator snapshot contract
- stable top-level fields for:
  - approval counts
  - intake-draft counts
  - manual takeover counts
  - queue and current-run state
  - repo bindings
  - tracked issue snapshots
  - repo-level operator summaries

Code and docs updated:

- `src/openclawcode/operator-status.ts`
- `src/openclawcode/index.ts`
- `src/integrations/openclaw-plugin/store.ts`
- `src/commands/openclawcode.ts`
- `src/cli/program/register.code.ts`
- `src/commands/openclawcode.test.ts`
- `docs/openclawcode/operator-status-snapshot-contract.md`
- `docs/openclawcode/README.md`
- `docs/openclawcode/master-delivery-checklist.md`

Validation passed:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`

## Machine-Readable Promotion And Rollback Artifacts

Added the first repo-local release-readiness artifacts so sync and promotion
flows no longer depend only on chat text and setup-check output.

Implemented:

- new CLI:
  - `openclaw code promotion-gate-refresh`
  - `openclaw code promotion-gate-show`
  - `openclaw code rollback-suggestion-refresh`
  - `openclaw code rollback-suggestion-show`
- `.openclawcode/promotion-gate.json` now persists:
  - branch and commit
  - baseline branch and rollback commit
  - strict setup-check readiness
  - low-risk and fallback proof readiness
  - merge-promotion gate readiness
  - machine-readable blockers and suggestions
- `.openclawcode/rollback-suggestion.json` now persists:
  - target branch and commit
  - target ref
  - promotion artifact linkage
  - a stable rollback reason

Code and docs updated:

- `src/openclawcode/promotion-artifacts.ts`
- `src/openclawcode/index.ts`
- `src/commands/openclawcode.ts`
- `src/cli/program/register.code.ts`
- `src/commands/openclawcode.test.ts`
- `docs/openclawcode/README.md`
- `docs/openclawcode/blueprint-first-delivery-plan.md`
- `docs/openclawcode/development-plan.md`
- `docs/openclawcode/master-delivery-checklist.md`

Validation passed:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`

## Chat Agreement And Draft Rejection

Closed two remaining chat-native control-plane gaps: explicit blueprint
agreement from chat, and explicit rejection of an ambiguous pending intake
draft.

Implemented:

- new chat commands:
  - `/occode-blueprint-agree`
  - `/occode-intake-reject`
- `/occode-blueprint-agree` now records the agreed checkpoint without opening
  `PROJECT-BLUEPRINT.md`
- `/occode-intake-reject` now discards a pending draft cleanly instead of
  forcing confirmation or silent abandonment

Code and docs updated:

- `extensions/openclawcode/index.ts`
- `extensions/openclawcode/index.test.ts`
- `docs/openclawcode/README.md`
- `docs/openclawcode/blueprint-first-delivery-plan.md`
- `docs/openclawcode/development-plan.md`
- `docs/openclawcode/master-delivery-checklist.md`

Validation passed:

- `pnpm exec vitest run src/commands/openclawcode.test.ts extensions/openclawcode/index.test.ts src/openclawcode/testing/openclaw-plugin-store.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

## Suitability Policy Explainability In /occode-status

`/occode-status` now exposes the suitability block reason directly in the main
status body when a tracked snapshot is not eligible for autonomous execution.

Implemented:

- when a status snapshot records:
  - `suitabilityDecision = needs-human-review`
  - or `suitabilityDecision = escalate`
- the status output now appends:
  - `Suitability policy: <decision> | <summary>`

Why this matters:

- operators can now see the blocking suitability reason directly in chat
  without reading nested artifacts or reconstructing the policy decision from
  earlier intake messages

Validation passed:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`

## Structured Rerun-Time Runtime Overrides

The next provider-routing slice is now live for reruns, instead of stopping at
pre-run blueprint mutation.

Implemented:

- new chat command:
  - `/occode-reroute-run owner/repo#123 <coder|verifier> <agent-id>`
- new CLI flags:
  - `--rerun-coder-agent`
  - `--rerun-verifier-agent`
- rerun context now persists:
  - `requestedCoderAgentId`
  - `requestedVerifierAgentId`
- executable runtime routing now resolves those requested reroutes before
  ordinary explicit builder/verifier agent ids
- plugin status snapshots now keep the requested reroute agent ids so chat
  surfaces can show the current override intent
- `openclaw code run --json` now exposes:
  - `rerunRequestedCoderAgentId`
  - `rerunRequestedVerifierAgentId`

Why this slice matters:

- blueprint provider-role edits were still being masked by repo-configured
  explicit `builderAgent` and `verifierAgent` settings on chatops reruns
- this new rerun-time override path gives operators a real structured way to
  retry a failed or review-blocked run with a different coder or verifier
  agent without editing shared repo config first

Validation passed:

- `pnpm exec vitest run src/integrations/openclaw-plugin/chatops.test.ts src/openclawcode/testing/openclaw-plugin-store.test.ts extensions/openclawcode/index.test.ts src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Remaining gap:

- the override is applied when queueing a rerun, not to an already-running
  builder or verifier stage
- manual worktree takeover and post-edit resume are still missing

## Deferred Runtime Reroutes For Active Runs

Closed the next runtime-routing gap by letting operators change the next
execution attempt while the current run is still active.

Implemented:

- `/occode-reroute-run` no longer rejects an already-running issue
- the plugin store now persists deferred runtime reroutes
- `/occode-status` now shows the pending reroute while the current run is still
  active
- if the active run finishes `Failed`, openclawcode automatically queues a
  rerun with the deferred coder/verifier override
- queued runs also accept in-place runtime reroute updates before execution
  starts

Code and docs updated:

- `src/integrations/openclaw-plugin/store.ts`
- `extensions/openclawcode/index.ts`
- `src/openclawcode/testing/openclaw-plugin-store.test.ts`
- `extensions/openclawcode/index.test.ts`
- `docs/openclawcode/master-delivery-checklist.md`
- `docs/openclawcode/blueprint-first-delivery-plan.md`
- `docs/openclawcode/development-plan.md`

Validation passed:

- `pnpm exec vitest run src/openclawcode/testing/openclaw-plugin-store.test.ts extensions/openclawcode/index.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Remaining gap:

- reroutes are still deferred to the next attempt rather than hot-swapping an
  in-flight builder or verifier stage
- blueprint-first live proof and fresh-host proof still remain

## Operator Status Contract Tracks Deferred Reroutes

Extended the operator-status JSON contract so automation can see the same
pending runtime reroute state that chat users now see in `/occode-status`.

Implemented:

- `openclaw code operator-status-snapshot-show --json` now includes:
  - `deferredRuntimeRerouteCount`
  - `deferredRuntimeReroutes`
- per-repo summaries now include `deferredRuntimeRerouteCount`

Code and docs updated:

- `src/openclawcode/operator-status.ts`
- `src/commands/openclawcode.test.ts`
- `docs/openclawcode/operator-status-snapshot-contract.md`
- `docs/openclawcode/development-plan.md`

Validation passed:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`

## Chat Surfaces Show Release Receipts

Extended the operator-facing release summary so promotion and rollback receipts
do not stay buried in repo-local JSON artifacts only.

Implemented:

- `/occode-inbox` now shows the latest promotion receipt when one exists
- `/occode-inbox` now shows the latest rollback receipt when one exists
- `/occode-promotion-checklist` now shows the latest promotion receipt when one
  exists

Code and docs updated:

- `extensions/openclawcode/index.ts`
- `extensions/openclawcode/index.test.ts`
- `docs/openclawcode/dev-log/2026-03-16.md`

Validation passed:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`

## Sync, Promotion, and Rollback Runbook

Captured the current sync discipline and release-control steps in a single
operator runbook instead of leaving them spread across checklist bullets and
chat summaries.

Implemented:

- new document:
  - `docs/openclawcode/sync-promotion-runbook.md`
- the runbook now defines:
  - preferred sync cadence: every 7 days
  - forced sync threshold: `>= 150` commits behind `upstream/main`
  - exact sync-branch creation commands
  - exact validation commands and expected outputs
  - promotion receipt recording commands
  - rollback receipt recording commands
  - a disaster-recovery checklist for a broken promotion

Docs updated:

- `docs/openclawcode/README.md`
- `docs/openclawcode/master-delivery-checklist.md`
- `docs/openclawcode/development-plan.md`
- `docs/openclawcode/full-program-roadmap.md`

## Operator Context In /occode-status

The chat status surface now shows where it is operating from instead of only
showing workflow state.

Implemented:

- `/occode-status` now appends:
  - `Operator repo root: ...`
  - `Operator baseline: ...`
- when the gateway process exposes them, status also appends:
  - `OpenClaw profile: ...`
  - `OpenClaw config: ...`

Why this matters:

- operators can see the current repo root and baseline without leaving chat
- status output now makes it much harder to confuse copied roots, promoted
  branches, or alternate OpenClaw profiles during live operations

Validation passed:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`

## Runtime Routing Reaches Executable Roles

Pushed provider-role routing one step deeper into the real execution path.

Implemented:

- `AgentBackedBuilder` now resolves a concrete `coder` agent selection before
  build execution
- `AgentBackedVerifier` now resolves a concrete `verifier` agent selection
  before verification
- routing precedence is now:
  - explicit CLI override
  - role-scoped env mapping
  - adapter-scoped env mapping
  - runner default
- workflow runs now persist the selected runtime routing for executable roles
- `openclaw code run --json` now exposes stable top-level mirrors for:
  - `runtimeRoutingSelectionCount`
  - `runtimeRoutingCoderAgentId`
  - `runtimeRoutingCoderAgentSource`
  - `runtimeRoutingVerifierAgentId`
  - `runtimeRoutingVerifierAgentSource`

Code updated:

- `src/openclawcode/contracts/types.ts`
- `src/openclawcode/roles/interfaces.ts`
- `src/openclawcode/roles/agent-backed.ts`
- `src/openclawcode/app/run-issue.ts`
- `src/commands/openclawcode.ts`
- `src/openclawcode/roles/agent-backed.test.ts`
- `src/openclawcode/testing/run-issue.test.ts`
- `src/commands/openclawcode.test.ts`

Validation passed:

- `pnpm exec vitest run src/openclawcode/roles/agent-backed.test.ts --pool threads`
- `pnpm exec vitest run src/openclawcode/testing/run-issue.test.ts src/commands/openclawcode.test.ts --pool threads`

Remaining gap after this slice:

- provider reroute now affects the first executable stages
- mid-run provider switching is still missing
- manual worktree takeover and structured resume are still missing

## Stage-Gate Handoff Chat Commands

Added the first chat-side write path for blueprint-first handoff instead of
leaving gate decisions confined to local CLI.

Implemented:

- new `/occode-gates` command for chat-visible stage-gate summaries
- new `/occode-gate-decide` command for recording gate decisions from chat
- command output now shows:
  - gate readiness
  - latest recorded decision
  - blockers or suggestions
- chat decision recording now persists actor + note into the existing
  `.openclawcode/stage-gates.json` artifact

Code and docs updated:

- `extensions/openclawcode/index.ts`
- `extensions/openclawcode/index.test.ts`
- `docs/openclawcode/master-delivery-checklist.md`
- `docs/openclawcode/openclaw-capability-mapping.md`
- `docs/openclawcode/blueprint-first-delivery-plan.md`
- `docs/openclawcode/development-plan.md`

Validation passed:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

## Gate-Aware Autonomous Intake

Extended the execution-start gate so it now governs autonomous intake, not
just explicit `/occode-start`.

Implemented:

- webhook `triggerMode=auto` now stops at a pending approval when
  `execution-start` still needs human signoff
- `/occode-intake` now creates the GitHub issue but does not queue it when the
  same gate is not ready
- both paths now emit gate guidance that points operators to:
  - `/occode-gates`
  - `/occode-gate-decide`
  - `/occode-start`

Code and docs updated:

- `extensions/openclawcode/index.ts`
- `extensions/openclawcode/index.test.ts`
- `docs/openclawcode/master-delivery-checklist.md`
- `docs/openclawcode/openclaw-capability-mapping.md`
- `docs/openclawcode/blueprint-first-delivery-plan.md`
- `docs/openclawcode/development-plan.md`

Validation passed:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

## Gate-Aware Start Flow

Closed the first loop where a chat-side gate decision can actually change
whether execution proceeds.

Implemented:

- `approved` now upgrades `needs-human-decision` stage gates to `ready`
- `/occode-start` now refreshes `execution-start` before queueing
- if the gate is still not ready, start returns a gate-specific guidance
  message instead of queuing work
- after a human records `execution-start approved`, `/occode-start` can queue
  the issue through the normal path

Code and docs updated:

- `src/openclawcode/stage-gates.ts`
- `extensions/openclawcode/index.ts`
- `extensions/openclawcode/index.test.ts`
- `docs/openclawcode/master-delivery-checklist.md`
- `docs/openclawcode/openclaw-capability-mapping.md`
- `docs/openclawcode/blueprint-first-delivery-plan.md`
- `docs/openclawcode/development-plan.md`

Validation passed:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

## Blueprint-First Operator Status Surface

Extended the existing chat/operator status message so blueprint-first control
state is visible even when the operator is reading a workflow update from chat
instead of inspecting local artifacts.

Implemented:

- blueprint status and revision lines in `buildRunStatusMessage(...)`
- provider-role routing summary in `buildRunStatusMessage(...)`
- stage-gate readiness summary in `buildRunStatusMessage(...)`

Code and docs updated:

- `src/integrations/openclaw-plugin/chatops.ts`
- `src/openclawcode/testing/openclaw-plugin-integration.test.ts`
- `docs/openclawcode/master-delivery-checklist.md`
- `docs/openclawcode/openclaw-capability-mapping.md`
- `docs/openclawcode/blueprint-first-delivery-plan.md`
- `docs/openclawcode/development-plan.md`

Validation passed:

- `pnpm exec vitest run src/openclawcode/testing/openclaw-plugin-integration.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

## Blueprint-First Inbox Backlog Surface

Continued Phase M8 by exposing blueprint work-item backlog state through
`/occode-inbox` instead of limiting blueprint-first visibility to per-run
status messages.

Implemented:

- `/occode-inbox` now reads `.openclawcode/work-items.json`
- inbox output now shows:
  - work-item totals
  - planned/discovered breakdown
  - artifact staleness vs current blueprint revision
  - execution and issue-projection readiness
  - blocker and suggestion counts

Code and docs updated:

- `extensions/openclawcode/index.ts`
- `extensions/openclawcode/index.test.ts`
- `docs/openclawcode/master-delivery-checklist.md`
- `docs/openclawcode/openclaw-capability-mapping.md`
- `docs/openclawcode/blueprint-first-delivery-plan.md`
- `docs/openclawcode/development-plan.md`

Validation passed:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts src/openclawcode/testing/openclaw-plugin-integration.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

## Blueprint Discussion Command

Added the first explicit chat-side blueprint discussion surface instead of
keeping blueprint inspection in local CLI only.

Implemented:

- new `/occode-blueprint` command
- chat-visible blueprint summary with:
  - title, status, revision, and goal summary
  - workstream/open-question/human-gate/defaulted counts
  - provider strategy summary
  - current clarification questions
  - proactive suggestions
- clarification rules now also surface:
  - unresolved `Open Questions`
  - partially assigned provider roles

Code and docs updated:

- `extensions/openclawcode/index.ts`
- `extensions/openclawcode/index.test.ts`
- `src/openclawcode/blueprint.ts`
- `docs/openclawcode/master-delivery-checklist.md`
- `docs/openclawcode/openclaw-capability-mapping.md`
- `docs/openclawcode/blueprint-first-delivery-plan.md`
- `docs/openclawcode/development-plan.md`

Validation passed:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

## Blueprint-First Run Snapshots

Carried the repo-local blueprint-first state into workflow run artifacts instead
of keeping it isolated under `.openclawcode/*.json`.

Implemented:

- workflow runs now snapshot:
  - blueprint status and revision metadata
  - normalized planner/coder/reviewer/verifier/doc-writer routing
  - current stage-gate readiness and latest decisions
- `openclaw code run --json` now exposes top-level mirrors for that state:
  - blueprint status, revision, agreement, and count fields
  - role-routing mixed-mode, fallback, unresolved-role, and per-role adapters
  - stage-gate blocked/human-decision counts and per-gate readiness mirrors

Code changes:

- updated `src/openclawcode/contracts/types.ts`
  - added explicit workflow blueprint/role-routing/stage-gate snapshot types
- updated `src/openclawcode/app/run-issue.ts`
  - captures blueprint-first context at run creation time
- updated `src/openclawcode/stage-gates.ts`
  - derives execution-routing readiness from the current resolvable route plan,
    not only from a previously persisted artifact
- updated `src/commands/openclawcode.ts`
  - mirrors the new blueprint-first run state into `openclaw code run --json`
- updated `src/commands/openclawcode.test.ts`
  - covered the new top-level JSON contract fields
- updated `src/openclawcode/testing/run-issue.test.ts`
  - covered persisted workflow-run snapshots

Validation passed:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run src/openclawcode/testing/run-issue.test.ts --pool threads`
- the next low-risk published-PR candidate in the local backlog is
  `publishedPullRequestHasNumber`

## Published Pull Request Presence Mirrors

Continued the refreshed sync branch command-layer queue after `#124` by
consuming two more boolean presence mirrors from the same field family.

Implemented and validated:

- `#126` for `publishedPullRequestHasNumber`
- `#127` for `publishedPullRequestHasUrl`

Both slices were handled entirely on `sync/upstream-2026-03-16`:

- added the new top-level booleans in `src/commands/openclawcode.ts`
- extended `src/commands/openclawcode.test.ts` to cover:
  - the normal published PR path
  - the no-published-PR path
  - the url-only published path
- updated `docs/openclawcode/run-json-contract.md`

Validation passed for both slices:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Validation-pool state after the two follow-up slices:

- `reconcile-validation-issues --close-implemented --json` auto-closed `#126`
- `reconcile-validation-issues --close-implemented --json` auto-closed `#127`
- seeded the next command-layer issue:
  - `#128` for `publishedPullRequestHasOpenedAt`

## Published Pull Request Opened-At Presence

Kept the same published-PR boolean family moving on the refreshed sync branch.

Implemented and validated:

- `#128` for `publishedPullRequestHasOpenedAt`

This slice added one more stable top-level boolean convenience field:

- `publishedPullRequestHasOpenedAt`

Validation passed:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

## OpenClaw Capability Mapping

Turned the latest product-direction discussion into a persistent architecture
document instead of leaving it as a chat-only conclusion.

Added:

- `docs/openclawcode/openclaw-capability-mapping.md`

This document now records two things in one place:

- which existing `openclaw` capabilities can be reused directly for the
  blueprint-first product
- the complete remaining build plan for what still needs to be added above the
  substrate

Key conclusions captured there:

- `openclaw` already gives the product most of its lower layers:
  - channels
  - gateway control plane
  - sessions and multi-agent isolation
  - plugin and extension runtime
  - model/provider/auth plumbing
  - automation inputs such as cron, hooks, health, and logs
  - browser, canvas, and device-node surfaces
- `openclawcode` already gives the product the issue-driven execution layer:
  - isolated coding runs
  - builder/verifier execution
  - PR publication
  - rerun/review/merge handling
- the remaining work is mostly the missing upstream control plane:
  - goal discussion
  - blueprint-centered state
  - work-item decomposition
  - general discovery
  - provider-role routing
  - stage-level human handoff

Also linked that document into:

- `README.md`
- `development-plan.md`
- `blueprint-first-delivery-plan.md`
- `master-delivery-checklist.md`
- `full-program-roadmap.md`

Validation-pool state after this slice:

- `reconcile-validation-issues --close-implemented --json` auto-closed `#128`
- seeded the next command-layer issue:
  - `#129` for `publishedPullRequestHasTitle`

## Blueprint-First Product Direction

Documented the newly clarified product direction instead of leaving it only in
chat:

- users should be able to discuss a project goal with the system before issue
  creation
- the agreed goal should be written into a fixed blueprint document
- the system should later derive or discover work items from that blueprint
- execution stages should support Codex, Claude Code, or both through a shared
  provider-role abstraction
- humans should be able to intervene at every major stage, while the system
  continues autonomously when they do not intervene

Captured that gap analysis in:

- `docs/openclawcode/blueprint-first-orchestration.md`

The key finding recorded there is:

- the current codebase is already strong in the issue-driven execution half
- the major missing pieces are the upstream control plane:
  - blueprint discussion
  - blueprint storage
  - blueprint-to-work-item decomposition
  - general self-discovery
  - provider-neutral multi-agent routing
  - stage-level human handoff

Linked that document into:

- `README.md`
- `development-plan.md`
- `full-program-roadmap.md`
- `master-delivery-checklist.md`

## Blueprint Foundation: Fixed Path, Schema, And CLI

Turned the first blueprint-first control-plane slice into real repo-local
infrastructure instead of leaving it as a design gap.

Implemented:

- fixed blueprint path:
  - `PROJECT-BLUEPRINT.md`
- first schema and lifecycle statuses in:
  - `docs/openclawcode/project-blueprint-schema.md`
- first execution plan in:
  - `docs/openclawcode/blueprint-first-delivery-plan.md`
- repo-local CLI commands:
  - `openclaw code blueprint-init`
  - `openclaw code blueprint-show`
  - `openclaw code blueprint-set-status`

Code changes:

- added `src/openclawcode/blueprint.ts`
  - resolves the fixed blueprint path
  - creates the scaffold
  - reads machine-readable blueprint state
  - records explicit lifecycle transitions such as `agreed`
- updated `src/commands/openclawcode.ts`
  - wired the new blueprint commands into the existing command surface
- updated `src/cli/program/register.code.ts`
  - registered the new CLI commands and examples
- extended `src/commands/openclawcode.test.ts`
  - covered blueprint scaffold creation
  - covered missing blueprint inspection
  - covered the explicit `agreed` checkpoint

Checklist movement:

- completed `define a fixed project blueprint document path and schema`
- completed `support an explicit "blueprint agreed" checkpoint`

Validation passed:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Next blueprint-first slice:

- add the first goal-discussion surface before issue creation

## Chat-Native Goal Discussion And Intake Draft Review

Moved blueprint-first discussion from read-only reporting into the first
mutable repo-local and chat-native loop.

Implemented:

- new CLI:
  - `openclaw code blueprint-set-section`
- new chat commands:
  - `/occode-goal`
  - `/occode-blueprint-edit`
  - `/occode-intake-edit`
  - `/occode-intake-confirm`
- ambiguous one-line `/occode-intake` now:
  - writes a pending intake draft to the plugin store
  - emits clarification prompts
  - requires confirmation before GitHub issue creation
  - allows draft editing before creation

Code updated:

- `src/openclawcode/blueprint.ts`
- `src/commands/openclawcode.ts`
- `src/cli/program/register.code.ts`
- `src/integrations/openclaw-plugin/chatops.ts`
- `src/integrations/openclaw-plugin/store.ts`
- `extensions/openclawcode/index.ts`
- `src/commands/openclawcode.test.ts`
- `src/openclawcode/testing/openclaw-plugin-store.test.ts`
- `extensions/openclawcode/index.test.ts`

Checklist movement:

- completed `support a goal-discussion loop before issue creation`
- completed `add a confirmation step for ambiguous chat-native intake`
- completed `allow editing a generated issue draft before creation`
- completed `support clarification prompts when a one-line request is underspecified`

## Manual Takeover And Post-Edit Resume

Added the first structured human worktree-handoff path instead of leaving manual
edits completely outside the tracked workflow model.

Implemented:

- new chat commands:
  - `/occode-takeover`
  - `/occode-resume-after-edit`
- plugin store now persists active manual takeovers
- `/occode-status` now shows active manual takeover state
- reruns queued from `/occode-resume-after-edit` now preserve:
  - `manualTakeoverRequestedAt`
  - `manualTakeoverActor`
  - `manualTakeoverWorktreePath`
  - `manualResumeNote`

Code and docs updated:

- `src/openclawcode/contracts/types.ts`
- `src/commands/openclawcode.ts`
- `src/integrations/openclaw-plugin/store.ts`
- `extensions/openclawcode/index.ts`
- `src/commands/openclawcode.test.ts`
- `src/openclawcode/testing/openclaw-plugin-store.test.ts`
- `extensions/openclawcode/index.test.ts`
- `docs/openclawcode/run-json-contract.md`
- `docs/openclawcode/blueprint-first-delivery-plan.md`
- `docs/openclawcode/master-delivery-checklist.md`
- `docs/openclawcode/README.md`
- `docs/openclawcode/development-plan.md`

Validation passed:

- `pnpm exec vitest run src/commands/openclawcode.test.ts src/openclawcode/testing/openclaw-plugin-store.test.ts extensions/openclawcode/index.test.ts --pool threads`

## Blueprint Clarification Surface

Started Phase B2 without waiting for chat wiring by adding a deterministic
repo-local clarification pass over `PROJECT-BLUEPRINT.md`.

Implemented:

- `openclaw code blueprint-clarify`

Behavior:

- if the blueprint does not exist yet, it asks the operator to create it
- if the blueprint still contains scaffold placeholders, it emits concrete
  clarification questions
- it also emits proactive suggestions for:
  - lifecycle checkpoints such as `agreed`
  - provider-role selection
  - human-gate decisions

Code and docs updated:

- `src/openclawcode/blueprint.ts`
- `src/commands/openclawcode.ts`
- `src/cli/program/register.code.ts`
- `src/commands/openclawcode.test.ts`
- `docs/openclawcode/blueprint-first-delivery-plan.md`
- `docs/openclawcode/project-blueprint-schema.md`

Validation passed:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`
