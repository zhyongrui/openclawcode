# 2026-03-19

- Continued the next real unfinished slice after the first-50 audit:
  Milestone 1 from `development-plan-chat-native-autonomy.md`
  (`empty-repo blueprint bootstrap`).
- Finished the bootstrap-side empty-repo detection so it now only enters the
  blueprint-first path when the target repo is genuinely scaffold-only.
  - `detectBootstrapTestCommands()` no longer treats every repo with
    zero tracked git files as empty
  - bootstrap still prefers explicit project manifests first:
    - `vitest.openclawcode.config.mjs`
    - `package.json`
    - `go.mod`
    - `Cargo.toml`
    - `pyproject.toml` / `pytest.ini`
  - only after those checks does it fall back to
    `empty-repo-blueprint`, and only when the repo has no meaningful
    non-scaffold entries
- Added a focused bootstrap regression in `src/commands/openclawcode.test.ts`.
  - verifies that a truly empty repo now returns:
    - `testCommands = []`
    - `testCommandSource = "empty-repo-blueprint"`
    - `blueprintFirstBootstrap = true`
    - `nextAction = "clarify-project-blueprint"`
  - preserved the existing package-manager bootstrap behavior for repos that
    already contain project files but are not yet tracked in git
- Updated operator-facing install docs so they describe the new empty-repo
  bootstrap handoff instead of implying a placeholder test command path.
  - `docs/openclawcode/fresh-host-install.md`
  - `docs/openclawcode/codex-openclawcode-install.md`
- Validation for this slice:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
  - `pnpm exec vitest run src/wizard/setup.code.test.ts --pool threads`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- Expected broader-host validation caveat remains unchanged:
  - full `pnpm build` in this checkout is still blocked by unrelated optional
    dependency / type-resolution gaps outside the `openclawcode` slice

- Started the chat-native setup slice so GitHub auth can begin inside Feishu or
  another live chat instead of requiring the operator to switch to a shell
  first.
  - added `docs/openclawcode/chat-setup-onboarding.md`
  - added durable per-chat setup sessions in
    `src/integrations/openclaw-plugin/store.ts`
  - added host-side GitHub CLI device-login helpers in
    `src/wizard/setup.code.ts`
  - added `/occode-setup` and `/occode-setup-status` in
    `extensions/openclawcode/index.ts`
  - the first MVP now:
    - detects existing GitHub auth from `GH_TOKEN`, `GITHUB_TOKEN`, or
      `gh auth token`
    - starts `gh auth login --web` on the host when auth is missing
    - captures the GitHub device-flow URL and one-time code
    - sends those details back to the active chat
    - persists the pending setup session so `/occode-setup-status` can resume
      after browser approval
- Validation for the chat-native setup slice:
  - `pnpm exec vitest run src/wizard/setup.code.test.ts --pool threads`
  - `pnpm exec vitest run src/openclawcode/testing/openclaw-plugin-store.test.ts --pool threads`
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Extended the chat-native setup slice with the repository stage.
  - `/occode-setup existing owner/repo` now validates and persists an existing
    repo selection after GitHub auth is ready
  - `/occode-setup new repo-name` now creates a new GitHub repo through the
    host-side `gh repo create` path after GitHub auth is ready
  - setup sessions now remember:
    - `projectMode`
    - `pendingRepoName`
    - selected repo state across auth completion
  - the setup handoff now points directly at:
    - `openclaw code bootstrap --repo owner/repo --mode auto`
    - followed by chat-native blueprint alignment through `/occode-goal` and
      `/occode-blueprint`
- Validation for the repository-stage slice:
  - `pnpm exec vitest run src/wizard/setup.code.test.ts --pool threads`
  - `pnpm exec vitest run src/openclawcode/testing/openclaw-plugin-store.test.ts --pool threads`
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Extended the same setup flow again so chat no longer stops at "repo selected";
  it now continues into bootstrap automatically.
  - when GitHub auth is ready and the repo is known, `/occode-setup` and
    `/occode-setup-status` now run `openclaw code bootstrap --repo owner/repo`
    through the existing helper path
  - setup sessions now persist a `bootstrap-complete` state with:
    - repo root
    - blueprint path and revision metadata
    - next action
    - proof readiness
    - exact handoff commands for blueprint, gates, CLI proof, chat bind, and
      chat proof
  - the setup reply now points the operator directly at the next blueprint
    alignment commands instead of only repeating the bootstrap shell command
- Validation for the bootstrap-in-chat slice:
  - `pnpm exec vitest run src/wizard/setup.code.test.ts --pool threads`
  - `pnpm exec vitest run src/openclawcode/testing/openclaw-plugin-store.test.ts --pool threads`
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Extended bootstrap-complete setup state with blueprint alignment context for
  existing repos.
  - after bootstrap completes, the setup flow now reads
    `PROJECT-BLUEPRINT.md` and stores:
    - goal summary
    - workstream / open-question / human-gate counts
    - clarification questions
    - clarification suggestions
  - setup replies now include the first blueprint alignment prompts directly,
    so the operator can stay inside chat instead of manually opening the file
- Validation for the blueprint-alignment setup slice:
  - `pnpm exec vitest run src/openclawcode/testing/openclaw-plugin-store.test.ts --pool threads`
  - `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Recorded the remaining chat-native setup backlog in:
  - `docs/openclawcode/chat-setup-onboarding.md`
  - `docs/openclawcode/development-plan-chat-native-autonomy.md`
  - next implementation priority is now explicit:
    - `new-project` blueprint-first chat flow
    - repo-name suggestions
    - setup-aware blueprint agreement loop
    - auto-bind and recovery controls

- Finished the next setup slice so `new-project` onboarding now works as a
  blueprint-first chat-native flow instead of stopping at repo auth or manual
  CLI handoff.
  - `/occode-setup new-project` now starts a setup-local blueprint draft
    without forcing GitHub auth first
  - `/occode-goal`, `/occode-blueprint-edit`, `/occode-blueprint-agree`, and
    `/occode-blueprint` now become setup-aware when an active draft session is
    present in the current chat
  - agreeing the setup draft now derives deterministic repo-name suggestions
    and moves the session into repo-choice state
  - `/occode-setup new <repo-name>` now continues from the agreed draft through
    auth, repo creation, bootstrap, blueprint sync, work-item refresh, and
    stage-gate refresh
  - bootstrap now auto-binds the active chat when safe and persists
    setup-specific recovery context (`lastFailure`) plus next-command summary
  - setup control commands now include `/occode-setup-cancel` and
    `/occode-setup-retry`
- Updated the planning docs to reflect the new baseline.
  - `development-plan-chat-native-autonomy.md` now marks Milestone 1 complete
    and promotes Milestone 2 (`blueprint-driven next-work selection`) as the
    next main slice
  - `chat-setup-onboarding.md` now documents the landed `new-project`,
    setup-aware blueprint, auto-bind, and recovery-control flow
- Validation for the `new-project` setup slice:
  - `pnpm exec vitest run src/wizard/setup.code.test.ts src/openclawcode/testing/openclaw-plugin-store.test.ts extensions/openclawcode/index.test.ts --pool threads`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`

- Fixed the Feishu `/occode-setup` routing regression and finished Milestone 2
  (`blueprint-driven next-work selection`).
  - Feishu command probing now normalizes invisible characters and slash
    variants before command-auth probing and plugin command matching
  - `extensions/feishu/src/bot.ts` now feeds normalized command bodies into
    both command authorization and plugin execution
  - the Feishu test harness now partially mocks
    `src/plugins/commands.ts` so the real normalization helper stays active
  - added `.openclawcode/next-work.json` through
    `src/openclawcode/next-work.ts`
  - added `openclaw code next-work-show --json`
  - added `/occode-next` for chat-visible next-work decisions
  - next-work output now explains whether progress is:
    - ready to execute
    - blocked on human
    - blocked on missing clarification
    - blocked on policy
  - updated the roadmap and chat-setup docs so Milestone 2 is marked landed
    and Milestone 3 (`issue materialization from work items`) is the next
    recommended slice
- Validation for the routing + next-work slice:
  - `pnpm exec vitest run src/plugins/commands.test.ts extensions/feishu/src/bot.test.ts src/commands/openclawcode.test.ts extensions/openclawcode/index.test.ts --pool threads`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- Known local build caveat remains unchanged:
  - full `pnpm build` is still blocked in this checkout by the unrelated
    wrong-platform `esbuild` package (`@esbuild/android-arm64` present while
    this host needs `@esbuild/linux-x64`)
