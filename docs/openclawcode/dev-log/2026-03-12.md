# OpenClawCode Dev Log - 2026-03-12

## Upstream Sync Baseline Refresh

Started the next integration slice by refreshing the fork against the current
`upstream/main` instead of continuing live validation on a stale integration
base.

Implemented:

- created `sync/upstream-2026-03-12` from local `main`
- fetched both remotes before evaluating divergence:
  - `origin/main` stayed aligned with local `main`
  - `upstream/main` had advanced by 38 commits beyond the previous merge base
- merged `upstream/main` into `sync/upstream-2026-03-12`
  - merge base:
    - `62d5df28dc4ac50dc6f0fbfad784a0b70d009101`
  - upstream head merged:
    - `841ee24340 fix(daemon): address clanker review findings for kickstart restart`

Why this mattered:

- the earlier "not behind upstream" read was stale because local remote refs had
  not been refreshed yet
- once refs were updated, the fork was actually:
  - `110 ahead`
  - `38 behind`
    relative to `upstream/main`
- the new upstream commits touched adjacent runtime layers such as:
  - `src/agents/pi-embedded-runner/run.ts`
  - `src/agents/pi-embedded-runner/run/attempt.ts`
  - `src/agents/tool-catalog.ts`
  - `src/gateway/tools-invoke-http.ts`
- that made it cheaper to refresh the integration branch first than to prove the
  next live `openclawcode` slice on an outdated base and then re-validate again

Validation:

- merge completed cleanly with no direct file overlap against the recent
  `openclawcode` sandbox-edit and runner-switch commits
- `pnpm build`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`

Result:

- `sync/upstream-2026-03-12` is now the best validation base for the next live
  `OPENCLAWCODE_ENABLE_FS_TOOLS=edit` trial
- local `main` stays untouched and continues to match `origin/main`
- the next recommended step is a controlled live issue run from the sync branch,
  not another local-only hardening slice

## Sync-Branch Live Validation Follow-Ups

Ran the next controlled live validation against issue `#36` from
`sync/upstream-2026-03-12`.

Observed live results:

- the first run reached the doc change cleanly but failed the host validation
  command because `scripts/openclawcode-setup-check.sh` accepted an inherited
  `OPENCLAWCODE_GITHUB_WEBHOOK_SECRET` even when the configured env file did
  not define it
- fixed that health-check semantics gap and added regression coverage so the
  script now requires the secret in the configured env file and also verifies
  that sourcing the file loads it
- reran issue `#36` with `OPENCLAWCODE_ENABLE_FS_TOOLS=edit`
- the second run reached `ready-for-human-review` and published `PR #47`, but
  exposed two more real issues:
  - draft PR metadata still hard-coded the base branch to `main`, so the PR
    opened against the wrong base and expanded to a large diff
  - deterministic sandbox edits still failed in the live builder, even though
    the builder could recover with a narrow shell rewrite

Root cause and repair:

- reproduced the edit failure directly against the real live sandbox container
  instead of through the agent loop
- confirmed `SandboxFsBridge.writeFile(...)` itself was writing empty files in
  the live container
- traced the bug to the pinned mutation helper shell plan using
  `python3 - <<'PY' ...`, which consumes stdin for the embedded Python script
  and leaves an empty payload for the actual file write
- changed the helper to materialize the embedded Python into a shell variable
  and execute it via `python3 -c`, preserving stdin for the file payload
- fixed draft PR metadata to inherit the workflow workspace base branch instead
  of hard-coding `main`

Validation after the repair:

- `pnpm exec vitest run src/agents/sandbox/fs-bridge-mutation-helper.test.ts --pool threads`
- `pnpm exec vitest run src/agents/sandbox/fs-bridge.e2e-docker.test.ts src/agents/pi-tools.read.sandbox-edit.e2e-docker.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- local reproduction against the real live sandbox container now shows
  `SandboxFsBridge.writeFile(...)` updating both host and bridge readback with
  the expected payload

Live closure after the repair:

- retargeted `PR #47` from `main` to `sync/upstream-2026-03-12`, collapsing it
  back to the expected one-file README diff
- reran issue `#36` on `openclawcode/issue-36-sync-upstream-2026-03-12-proof4`
  with `OPENCLAWCODE_ENABLE_FS_TOOLS=edit`
- the first rerun still surfaced one more deterministic-edit bug:
  - `verifySandboxEditApplied(...)` required the raw `oldText` substring to
    disappear from the mounted file
  - that falsely failed when the inserted block embedded the old block as a
    prefix
- fixed the sandbox edit verifier to compare against the exact expected
  post-edit file contents and added a regression in
  `src/agents/pi-tools.read.sandbox-edit-recovery.test.ts`
- validated that fix with:
  - `pnpm exec vitest run src/agents/pi-tools.read.sandbox-edit-recovery.test.ts --pool threads`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- reran issue `#36` again and confirmed:
  - run `zhyongrui-openclawcode-36-1773282645164` reached
    `ready-for-human-review`
  - the builder's live session used sandbox `edit` successfully without
    falling back to shell `exec`
  - the verifier also reached `approve-for-human-review`

## Default Edit Re-Enable

After the successful sync-branch live proof, narrowed the default
`OpenClawAgentRunner` filesystem deny from `edit,write` to `write` only.

Implementation and validation:

- updated `src/openclawcode/runtime/agent-runner.ts` so default live sessions
  keep `write` denied but no longer add a runner-level `edit` deny
- updated `src/openclawcode/runtime/agent-runner.test.ts` for the new default
  and kept `OPENCLAWCODE_ENABLE_FS_TOOLS=write` as the remaining full fs-tool
  rollout switch
- validated with:
  - `pnpm exec vitest run src/openclawcode/runtime/agent-runner.test.ts --pool threads`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
  - `pnpm build`
- ran one more live issue `#36` proof on
  `openclawcode/issue-36-sync-upstream-2026-03-12-proof5` with no
  `OPENCLAWCODE_ENABLE_FS_TOOLS` override
- confirmed:
  - builder session `openclawcode-6886e842-99eb-4d16-8d20-a5bb80c8a3ea` used
    sandbox `edit` successfully under the default runner config
  - run `zhyongrui-openclawcode-36-1773282908481` reached
    `ready-for-human-review`

## Sandbox Directory Read Cleanup

Closed the next live builder gap on `sync/upstream-2026-03-12` by teaching the
sandbox `read` path how to page in-boundary directories instead of treating
them as invalid file reads.

Implementation:

- widened `safe-open-sync` stat validation to accept both files and directories
  for the sandbox stat path
- updated the sandbox fs bridge stat plan so directory targets such as
  `/workspace/docs/openclawcode` pass boundary validation cleanly
- extended `createSandboxedReadTool(...)` so it:
  - detects in-boundary directories through the bridge
  - resolves the host path for that directory
  - returns a paged listing through the existing `read` offset or limit
    contract instead of surfacing a boundary-check failure
- added focused regression coverage in:
  - `src/infra/safe-open-sync.test.ts`
  - `src/agents/sandbox/fs-bridge.shell.test.ts`
  - `src/agents/pi-tools.create-openclaw-coding-tools.adds-claude-style-aliases-schemas-without-dropping.test.ts`

Validation:

- `pnpm exec vitest run src/infra/safe-open-sync.test.ts src/agents/sandbox/fs-bridge.shell.test.ts src/agents/pi-tools.create-openclaw-coding-tools.adds-claude-style-aliases-schemas-without-dropping.test.ts -t "returns a paged directory listing when sandbox read targets a directory" --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- `pnpm build`

Live proof:

- reran issue `#36` on branch
  `openclawcode/issue-36-sync-upstream-2026-03-12-proof7`
- run `zhyongrui-openclawcode-36-1773283954561` reached
  `ready-for-human-review`
- the builder session no longer emitted the earlier boundary-check warning for
  `/workspace/docs/openclawcode`
- the remaining builder read noise is now narrower and more actionable:
  a stale issue-context file hint still points at
  `docs/openclawcode/plugin-integration.md`, which does not exist in the repo;
  the real file is `docs/openclawcode/openclaw-plugin-integration.md`

Next step:

- clean up the stale issue-context file hints so live builders stop spending a
  first read on the nonexistent `plugin-integration.md` path
- only after that, stage a real validation of the remaining runner-level
  `write` deny removal

## Issue-Context Hint Cleanup

Closed the next prompt-hygiene gap by tightening the builder's issue-context
file hints so docs issues point at the real plugin integration document instead
of a guessed file name.

Implementation:

- updated `src/openclawcode/roles/agent-backed.ts` so builder prompts now:
  - promote explicit backticked file paths from the issue text into the hinted
    file list
  - add the real
    `docs/openclawcode/openclaw-plugin-integration.md` path when an issue
    mentions both plugin integration and `/occode-sync`
- added regression coverage in
  `src/openclawcode/roles/agent-backed.test.ts` to pin the exact docs issue
  wording used in the live run

Validation:

- `pnpm exec vitest run src/openclawcode/roles/agent-backed.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- `pnpm build`

Live proof:

- reran issue `#36` on branch
  `openclawcode/issue-36-sync-upstream-2026-03-12-proof8`
- run `zhyongrui-openclawcode-36-1773284400697` reached
  `ready-for-human-review`
- the builder prompt now listed:
  - `docs/openclawcode/README.md`
  - `docs/openclawcode/openclaw-plugin-integration.md`
- the live builder stopped trying to read the nonexistent
  `docs/openclawcode/plugin-integration.md`

Next step:

- stage a real validation of the remaining runner-level `write` deny removal
- once `write` is stable, repeat a low-risk merged live run and then re-check
  fresh-operator setup proof
