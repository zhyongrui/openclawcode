# OpenClawCode Dev Log - 2026-03-12

## Upstream Sync Baseline Refresh

Started the next integration slice by refreshing the fork against the current
`upstream/main` instead of continuing live validation on a stale integration
base.

Implemented:

- created `sync/upstream-2026-03-12` from local `main`
- fetched both remotes before evaluating divergence:
  - `origin/main` stayed aligned with local `main`
  - `upstream/main` had advanced by 38 commits beyond the previous merge base
- merged `upstream/main` into `sync/upstream-2026-03-12`
  - merge base:
    - `62d5df28dc4ac50dc6f0fbfad784a0b70d009101`
  - upstream head merged:
    - `841ee24340 fix(daemon): address clanker review findings for kickstart restart`

Why this mattered:

- the earlier "not behind upstream" read was stale because local remote refs had
  not been refreshed yet
- once refs were updated, the fork was actually:
  - `110 ahead`
  - `38 behind`
    relative to `upstream/main`
- the new upstream commits touched adjacent runtime layers such as:
  - `src/agents/pi-embedded-runner/run.ts`
  - `src/agents/pi-embedded-runner/run/attempt.ts`
  - `src/agents/tool-catalog.ts`
  - `src/gateway/tools-invoke-http.ts`
- that made it cheaper to refresh the integration branch first than to prove the
  next live `openclawcode` slice on an outdated base and then re-validate again

Validation:

- merge completed cleanly with no direct file overlap against the recent
  `openclawcode` sandbox-edit and runner-switch commits
- `pnpm build`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`

Result:

- `sync/upstream-2026-03-12` is now the best validation base for the next live
  `OPENCLAWCODE_ENABLE_FS_TOOLS=edit` trial
- local `main` stays untouched and continues to match `origin/main`
- the next recommended step is a controlled live issue run from the sync branch,
  not another local-only hardening slice

## Sync-Branch Live Validation Follow-Ups

Ran the next controlled live validation against issue `#36` from
`sync/upstream-2026-03-12`.

Observed live results:

- the first run reached the doc change cleanly but failed the host validation
  command because `scripts/openclawcode-setup-check.sh` accepted an inherited
  `OPENCLAWCODE_GITHUB_WEBHOOK_SECRET` even when the configured env file did
  not define it
- fixed that health-check semantics gap and added regression coverage so the
  script now requires the secret in the configured env file and also verifies
  that sourcing the file loads it
- reran issue `#36` with `OPENCLAWCODE_ENABLE_FS_TOOLS=edit`
- the second run reached `ready-for-human-review` and published `PR #47`, but
  exposed two more real issues:
  - draft PR metadata still hard-coded the base branch to `main`, so the PR
    opened against the wrong base and expanded to a large diff
  - deterministic sandbox edits still failed in the live builder, even though
    the builder could recover with a narrow shell rewrite

Root cause and repair:

- reproduced the edit failure directly against the real live sandbox container
  instead of through the agent loop
- confirmed `SandboxFsBridge.writeFile(...)` itself was writing empty files in
  the live container
- traced the bug to the pinned mutation helper shell plan using
  `python3 - <<'PY' ...`, which consumes stdin for the embedded Python script
  and leaves an empty payload for the actual file write
- changed the helper to materialize the embedded Python into a shell variable
  and execute it via `python3 -c`, preserving stdin for the file payload
- fixed draft PR metadata to inherit the workflow workspace base branch instead
  of hard-coding `main`

Validation after the repair:

- `pnpm exec vitest run src/agents/sandbox/fs-bridge-mutation-helper.test.ts --pool threads`
- `pnpm exec vitest run src/agents/sandbox/fs-bridge.e2e-docker.test.ts src/agents/pi-tools.read.sandbox-edit.e2e-docker.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- local reproduction against the real live sandbox container now shows
  `SandboxFsBridge.writeFile(...)` updating both host and bridge readback with
  the expected payload

Live closure after the repair:

- retargeted `PR #47` from `main` to `sync/upstream-2026-03-12`, collapsing it
  back to the expected one-file README diff
- reran issue `#36` on `openclawcode/issue-36-sync-upstream-2026-03-12-proof4`
  with `OPENCLAWCODE_ENABLE_FS_TOOLS=edit`
- the first rerun still surfaced one more deterministic-edit bug:
  - `verifySandboxEditApplied(...)` required the raw `oldText` substring to
    disappear from the mounted file
  - that falsely failed when the inserted block embedded the old block as a
    prefix
- fixed the sandbox edit verifier to compare against the exact expected
  post-edit file contents and added a regression in
  `src/agents/pi-tools.read.sandbox-edit-recovery.test.ts`
- validated that fix with:
  - `pnpm exec vitest run src/agents/pi-tools.read.sandbox-edit-recovery.test.ts --pool threads`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- reran issue `#36` again and confirmed:
  - run `zhyongrui-openclawcode-36-1773282645164` reached
    `ready-for-human-review`
  - the builder's live session used sandbox `edit` successfully without
    falling back to shell `exec`
  - the verifier also reached `approve-for-human-review`

## Default Edit Re-Enable

After the successful sync-branch live proof, narrowed the default
`OpenClawAgentRunner` filesystem deny from `edit,write` to `write` only.

Implementation and validation:

- updated `src/openclawcode/runtime/agent-runner.ts` so default live sessions
  keep `write` denied but no longer add a runner-level `edit` deny
- updated `src/openclawcode/runtime/agent-runner.test.ts` for the new default
  and kept `OPENCLAWCODE_ENABLE_FS_TOOLS=write` as the remaining full fs-tool
  rollout switch
- validated with:
  - `pnpm exec vitest run src/openclawcode/runtime/agent-runner.test.ts --pool threads`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
  - `pnpm build`
- ran one more live issue `#36` proof on
  `openclawcode/issue-36-sync-upstream-2026-03-12-proof5` with no
  `OPENCLAWCODE_ENABLE_FS_TOOLS` override
- confirmed:
  - builder session `openclawcode-6886e842-99eb-4d16-8d20-a5bb80c8a3ea` used
    sandbox `edit` successfully under the default runner config
  - run `zhyongrui-openclawcode-36-1773282908481` reached
    `ready-for-human-review`

## Sandbox Directory Read Cleanup

Closed the next live builder gap on `sync/upstream-2026-03-12` by teaching the
sandbox `read` path how to page in-boundary directories instead of treating
them as invalid file reads.

Implementation:

- widened `safe-open-sync` stat validation to accept both files and directories
  for the sandbox stat path
- updated the sandbox fs bridge stat plan so directory targets such as
  `/workspace/docs/openclawcode` pass boundary validation cleanly
- extended `createSandboxedReadTool(...)` so it:
  - detects in-boundary directories through the bridge
  - resolves the host path for that directory
  - returns a paged listing through the existing `read` offset or limit
    contract instead of surfacing a boundary-check failure
- added focused regression coverage in:
  - `src/infra/safe-open-sync.test.ts`
  - `src/agents/sandbox/fs-bridge.shell.test.ts`
  - `src/agents/pi-tools.create-openclaw-coding-tools.adds-claude-style-aliases-schemas-without-dropping.test.ts`

Validation:

- `pnpm exec vitest run src/infra/safe-open-sync.test.ts src/agents/sandbox/fs-bridge.shell.test.ts src/agents/pi-tools.create-openclaw-coding-tools.adds-claude-style-aliases-schemas-without-dropping.test.ts -t "returns a paged directory listing when sandbox read targets a directory" --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- `pnpm build`

Live proof:

- reran issue `#36` on branch
  `openclawcode/issue-36-sync-upstream-2026-03-12-proof7`
- run `zhyongrui-openclawcode-36-1773283954561` reached
  `ready-for-human-review`
- the builder session no longer emitted the earlier boundary-check warning for
  `/workspace/docs/openclawcode`
- the remaining builder read noise is now narrower and more actionable:
  a stale issue-context file hint still points at
  `docs/openclawcode/plugin-integration.md`, which does not exist in the repo;
  the real file is `docs/openclawcode/openclaw-plugin-integration.md`

Next step:

- clean up the stale issue-context file hints so live builders stop spending a
  first read on the nonexistent `plugin-integration.md` path
- only after that, stage a real validation of the remaining runner-level
  `write` deny removal

## Issue-Context Hint Cleanup

Closed the next prompt-hygiene gap by tightening the builder's issue-context
file hints so docs issues point at the real plugin integration document instead
of a guessed file name.

Implementation:

- updated `src/openclawcode/roles/agent-backed.ts` so builder prompts now:
  - promote explicit backticked file paths from the issue text into the hinted
    file list
  - add the real
    `docs/openclawcode/openclaw-plugin-integration.md` path when an issue
    mentions both plugin integration and `/occode-sync`
- added regression coverage in
  `src/openclawcode/roles/agent-backed.test.ts` to pin the exact docs issue
  wording used in the live run

Validation:

- `pnpm exec vitest run src/openclawcode/roles/agent-backed.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- `pnpm build`

Live proof:

- reran issue `#36` on branch
  `openclawcode/issue-36-sync-upstream-2026-03-12-proof8`
- run `zhyongrui-openclawcode-36-1773284400697` reached
  `ready-for-human-review`
- the builder prompt now listed:
  - `docs/openclawcode/README.md`
  - `docs/openclawcode/openclaw-plugin-integration.md`
- the live builder stopped trying to read the nonexistent
  `docs/openclawcode/plugin-integration.md`

Next step:

- stage a real validation of the remaining runner-level `write` deny removal
- once `write` is stable, repeat a low-risk merged live run and then re-check
  fresh-operator setup proof

## Staged Write Rollout

Closed the next local-plus-live fs-tool slice by proving that the remaining
runner-level `write` deny can be lifted in a controlled way without
destabilizing the live docs issue flow.

Implementation and validation:

- added `src/agents/pi-tools.read.sandbox-write.e2e-docker.test.ts`
- that new docker-gated regression proves `createSandboxedWriteTool(...)` can:
  - create a new mounted workspace file through alias-style `file_path` params
  - keep linked-worktree writes visible through both `/workspace` and the
    absolute worktree mount path used by live issue runs
- validated with:
  - `pnpm exec vitest run src/agents/pi-tools.read.sandbox-write.e2e-docker.test.ts --pool threads`
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
  - `pnpm build`

Live proof:

- reran issue `#36` on branch
  `openclawcode/issue-36-sync-upstream-2026-03-12-proof9`
- run `zhyongrui-openclawcode-36-1773284933205` reached
  `ready-for-human-review`
- the builder session tool list now exposed:
  - `read`
  - `edit`
  - `write`
  - `exec`
  - `process`
- the expanded fs-tool surface did not introduce a new live stability failure
  on the docs issue path

Operator note:

- during this live run, the builder attempted `pnpm exec prettier --check ...`
  inside the sandbox and hit `corepack` network bootstrap failure
  (`ECONNREFUSED`)
- the host-side issue validation still completed through the configured
  `pnpm exec vitest run --config vitest.openclawcode.config.mjs`
  command, so the workflow artifact still landed at
  `ready-for-human-review`

Next step:

- repeat one low-risk merged live proof with
  `OPENCLAWCODE_ENABLE_FS_TOOLS=write`
- once that passes, switch to fresh-operator-environment setup proof and
  policy-doc cleanup

## Builder Prompt Hardening For Sandbox Validation

Closed a follow-up prompt hygiene gap uncovered during the staged write rollout:
even though the host already owns validation, the builder could still try
package-manager or formatter commands inside the sandbox and trip network
bootstrap behavior.

Implementation:

- updated `src/openclawcode/roles/agent-backed.ts` so builder prompts now
  explicitly say:
  - do not run package-manager commands inside the sandbox
  - do not run formatter commands inside the sandbox
  - leave validation to the workflow host and use only file-local sanity checks
- updated `src/openclawcode/roles/agent-backed.test.ts` to pin that rule in the
  generated prompt

Validation:

- `pnpm exec vitest run src/openclawcode/roles/agent-backed.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- `pnpm build`

Live proof:

- reran issue `#36` on branch
  `openclawcode/issue-36-sync-upstream-2026-03-12-proof10`
- run `zhyongrui-openclawcode-36-1773285317777` reached
  `ready-for-human-review`
- the builder summary explicitly recorded:
  - it did not run package-manager commands in the sandbox
  - it did not run formatter commands in the sandbox
  - it left validation to the workflow host
- unlike the previous write-rollout run, this replay did not surface the
  `corepack`-driven `pnpm exec prettier --check ...` attempt

Next step:

- repeat one low-risk merged live proof with
  `OPENCLAWCODE_ENABLE_FS_TOOLS=write`
- then switch to fresh-operator-environment setup proof and policy-doc cleanup

## Long-Range Roadmap Refresh

Aligned the working roadmap with the current delivery reality so the next
iterations are no longer just "whatever the next live bug is."

Documented in `docs/openclawcode/development-plan.md`:

- a six-phase rolling program:
  - stable live baseline
  - fresh-operator reproducibility
  - autonomous suitability gating
  - runtime simplification
  - operator productization
  - continuous validation loop
- an explicit rule that when the validation pool is empty, Codex may seed a
  narrow repository-local validation issue directly through GitHub CLI/API
  instead of blocking on manual issue creation

Why this mattered:

- the live loop is now good enough that the main risk is drift between local
  fixes, operator docs, and the next real validation path
- the roadmap now names what "done for this phase" actually means, which makes
  it easier to keep iterating without re-deciding priorities every slice

## Expanded Write-Surface Merged Live Proof

Closed the remaining Phase 1 proof gap by running one low-risk merged command
issue under `OPENCLAWCODE_ENABLE_FS_TOOLS=write`.

Issue seeding:

- the command-layer validation pool was empty, so a new low-risk repository
  issue was created directly through the GitHub API:
  - issue `#48`
  - title:
    `[Feature]: Expose verificationHasFindings in openclaw code run --json output`

Live run:

- ran:
  - `OPENCLAWCODE_ENABLE_FS_TOOLS=write node dist/index.js code run --issue 48 --owner zhyongrui --repo openclawcode --repo-root /home/zyr/pros/openclawcode --base-branch sync/upstream-2026-03-12 --branch-name openclawcode/issue-48-sync-upstream-2026-03-12 --builder-agent main --verifier-agent main --test "pnpm exec vitest run --config vitest.openclawcode.config.mjs" --open-pr --merge-on-approve --json`
- workflow run:
  - `zhyongrui-openclawcode-48-1773286729110`
- build result stayed correctly scoped to:
  - `src/commands/openclawcode.ts`
  - `src/commands/openclawcode.test.ts`
- the builder added one new stable top-level JSON field:
  - `verificationHasFindings`
- the verifier approved the change for human review with no blocking findings

GitHub outcome:

- `PR #49` opened at `2026-03-12T03:41:34Z`
- `PR #49` merged at `2026-03-12T03:42:24Z`
- merge commit:
  - `551622028183f1ecfa75f0c8e6838b57828b9f01`
- issue `#48` closed at `2026-03-12T03:42:25Z`

External verification:

- confirmed via GitHub API that:
  - `pulls/49` reported:
    - `"state": "closed"`
    - `"merged": true`
    - `"merged_at": "2026-03-12T03:42:24Z"`
  - `issues/48` reported:
    - `"state": "closed"`
    - `"state_reason": "completed"`

Outcome:

- Phase 1's expanded-fs-surface merged proof is now complete
- the next planned slice moves to fresh-operator-environment proof instead of
  more `write` rollout validation

## Fresh Operator Root Hardening

Started the first real Phase 2 slice by shrinking the amount of hidden local
state needed to replay operator setup from a fresh root.

Problem found:

- the scripts still assumed too much implicit `~/.openclaw` state
- `openclawcode-setup-check.sh` only became practical with three separate file
  overrides:
  - env file
  - config file
  - chatops state file
- the first real copied-root proof also exposed a stricter bug:
  - even if a fresh env file persisted `OPENCLAWCODE_GITHUB_HOOK_ID`,
    `openclawcode-setup-check.sh` read the hook id too early, before sourcing
    the env file

Implementation:

- updated `scripts/openclawcode-setup-check.sh` so it now derives:
  - `openclawcode.env`
  - `openclaw.json`
  - `plugins/openclawcode/chatops-state.json`
    from one `OPENCLAWCODE_OPERATOR_ROOT` / `OPENCLAWCODE_SETUP_OPERATOR_ROOT`
    override
- updated `scripts/openclawcode-webhook-tunnel.sh` so it now derives its
  default env file from the same operator-root override
- fixed `openclawcode-setup-check.sh` to refresh GitHub repo, hook id, and
  hook event settings after sourcing `openclawcode.env`, so copied roots can
  persist webhook metadata in the env file itself
- updated `docs/openclawcode/operator-setup.md` to document the fresh-root path
  and to keep webhook repo or hook metadata in the env file instead of ad hoc
  shell exports
- extended `src/openclawcode/testing/setup-check.test.ts` with:
  - a root-only path-derivation regression
  - a strict-mode regression that proves hook metadata loaded from the fresh
    env file is honored

Validation:

- `pnpm exec vitest run src/openclawcode/testing/setup-check.test.ts --pool threads`
- `bash -n scripts/openclawcode-setup-check.sh scripts/openclawcode-webhook-tunnel.sh`

Real copied-root proof:

- created a temporary operator root under `/tmp/openclawcode-fresh-root-*`
- copied:
  - `openclawcode.env`
  - `openclaw.json`
  - `plugins/openclawcode/chatops-state.json`
- persisted webhook metadata into that copied env file:
  - `OPENCLAWCODE_GITHUB_REPO=zhyongrui/openclawcode`
  - `OPENCLAWCODE_GITHUB_HOOK_ID=600049842`
  - `OPENCLAWCODE_GITHUB_HOOK_EVENTS=issues,pull_request,pull_request_review`
- ran:
  - `OPENCLAWCODE_SETUP_OPERATOR_ROOT=<tmp-root> ./scripts/openclawcode-setup-check.sh --strict`
- confirmed the copied root now passes strict verification with:
  - `14 pass`
  - `0 warn`
  - `0 fail`

Remaining Phase 2 gap:

- the next proof still needs a true fresh-root gateway startup and one
  end-to-end issue run from that copied operator environment

## Fresh Operator Gateway Startup Proof

Pushed the copied-root Phase 2 proof one step further by launching a second
local gateway from the copied operator root instead of only reusing the
long-lived local gateway.

Setup:

- created another temporary copied root under `/tmp/openclawcode-fresh-gateway-*`
- copied:
  - `openclawcode.env`
  - `openclaw.json`
  - `plugins/openclawcode/chatops-state.json`
- persisted webhook metadata into the copied env file:
  - `OPENCLAWCODE_GITHUB_REPO=zhyongrui/openclawcode`
  - `OPENCLAWCODE_GITHUB_HOOK_ID=600049842`
  - `OPENCLAWCODE_GITHUB_HOOK_EVENTS=issues,pull_request,pull_request_review`

Gateway startup proof:

- launched:
  - `OPENCLAW_STATE_DIR=<tmp-root> OPENCLAW_CONFIG_PATH=<tmp-root>/openclaw.json OPENCLAW_GATEWAY_PORT=18889 node dist/index.js gateway run --bind loopback --port 18889`
- confirmed the copied-root gateway reached:
  - `listening on ws://127.0.0.1:18889`
  - plugin registration for the bundled `openclawcode` route
  - Feishu channel startup on the copied-root instance

Strict health check against the copied gateway:

- ran:
  - `OPENCLAWCODE_SETUP_OPERATOR_ROOT=<tmp-root> OPENCLAWCODE_SETUP_GATEWAY_URL=http://127.0.0.1:18889 ./scripts/openclawcode-setup-check.sh --strict`
- result:
  - `14 pass`
  - `0 warn`
  - `0 fail`

Outcome:

- Phase 2 no longer needs more copied-root setup or gateway bring-up work
- the remaining proof gap is now one real issue run from that copied operator
  environment

## Fresh Operator End-to-End Issue Proof

Closed the remaining Phase 2 gap by running a real issue end to end from the
copied operator root and copied gateway instance.

Setup:

- reused the copied operator root at `/tmp/openclawcode-fresh-run-jiqEfg`
- kept the copied gateway running on `127.0.0.1:18889`
- created a new repository issue through the GitHub API:
  - issue `#51`
  - title:
    `[Feature]: Expose verificationHasFollowUps in openclaw code run --json output`
- sent a locally signed `issues.opened` webhook payload directly to the copied
  gateway so the copied-root plugin, not the long-lived main operator, handled
  the intake

Copied-root workflow result:

- the copied gateway accepted issue `#51` and auto-enqueued it
- run:
  - `zhyongrui-openclawcode-51-1773297182598`
- build stayed scoped to:
  - `src/commands/openclawcode.ts`
  - `src/commands/openclawcode.test.ts`
- the run opened:
  - `PR #52`
- verifier approved the change for human review with no blocking findings

GitHub outcome:

- `PR #52` merged at `2026-03-12T06:35:52Z`
- merge commit:
  - `baa3b032c3adf5792b74a2d48045eff94f8ee36a`
- issue `#51` closed at `2026-03-12T06:35:54Z`

Copied-root state proof:

- `plugins/openclawcode/chatops-state.json` under the copied operator root now
  records issue `#51` with:
  - `stage: "merged"`
  - `pullRequestNumber: 52`
  - sent notification metadata under the copied-root state, not only the main
    operator state

Outcome:

- Phase 2 is now complete
- a fresh operator root can now:
  - pass strict setup validation
  - boot a local gateway
  - accept a real issue webhook
  - run the issue to merged completion

## Suitability Gate Rollout

Started Phase 3 by moving issue suitability out of vague roadmap language and
into a real workflow gate.

Implementation:

- added `SuitabilityAssessment` and `SuitabilityDecision` to workflow contracts
- added `src/openclawcode/roles/suitability.ts` to classify each planned issue
  as:
  - `auto-run`
  - `needs-human-review`
  - `escalate`
- the assessment now records:
  - scope classification
  - planner risk level
  - reasons
  - evaluation timestamp
- `runIssueWorkflow(...)` now evaluates suitability after planning and before
  workspace preparation
- obviously high-risk issues now move straight to `escalated` before any
  worktree preparation or PR publication
- guarded auto-merge now also requires `suitability.decision === "auto-run"`
- `openclaw code run --json` now exposes stable top-level suitability fields:
  - `suitabilityDecision`
  - `suitabilitySummary`
  - `suitabilityReasons`
  - `suitabilityClassification`
  - `suitabilityRiskLevel`
  - `suitabilityEvaluatedAt`

Validation:

- `pnpm exec vitest run src/openclawcode/roles/suitability.test.ts --pool threads`
- `pnpm exec vitest run src/openclawcode/testing/run-issue.test.ts --pool threads`
- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- `pnpm build`

Real proof:

- created issue `#53` through the GitHub API:
  - title:
    `[Validation]: Auth and secret suitability gate should escalate before branch mutation`
- ran:
  - `node dist/index.js code run --issue 53 --owner zhyongrui --repo openclawcode --repo-root /home/zyr/pros/openclawcode --base-branch sync/upstream-2026-03-12 --json`
- workflow run:
  - `zhyongrui-openclawcode-53-1773298188208`
- result:
  - `stage: "escalated"`
  - no workspace prepared
  - no changed files
  - no PR publication
- recorded suitability output:
  - `decision: "escalate"`
  - summary:
    `Suitability escalated the issue before branch mutation. Issue text references high-risk areas: auth, secrets, permissions.`

Outcome:

- Phase 3 now has a real pre-build risk gate instead of only prompt guidance
- the next gap is not classification itself; it is surfacing the same
  `auto-run` / `needs-human-review` / `escalate` decisions more clearly in
  operator views and validating them on the long-lived main operator

## Webhook Precheck Routing

Carried the new suitability signal into webhook and chat intake so obviously
high-risk issues stop before approval or auto-queue.

Implementation:

- exported shared issue risk-signal detection from
  `src/openclawcode/roles/suitability.ts`
- taught `decideIssueWebhookIntake(...)` to return a `precheck` escalation when
  the issue text obviously touches high-risk areas such as auth, secrets, or
  permissions
- added `buildIssueEscalationMessage(...)` for the chat-side escalation notice
- added `recordPrecheckedEscalation(...)` to the plugin store so the route can
  persist an `escalated` snapshot without inventing a full workflow run
- updated the webhook route to:
  - skip `pendingApprovals`
  - skip `queue`
  - persist an `escalated` snapshot with `runId: intake-precheck-<issueNumber>`
  - return `reason: "precheck-escalated"`

Validation:

- `pnpm exec vitest run src/openclawcode/testing/openclaw-plugin-integration.test.ts --pool threads`
- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- `pnpm build`

Live route proof:

- rebuilt the branch and restarted the copied-root gateway on `127.0.0.1:18889`
- delivered a locally signed synthetic webhook for issue `#9053` with auth or
  secret or permission language in the title and body
- confirmed the copied-root route persisted:
  - `delivery.reason: "precheck-escalated"`
  - `statusSnapshotsByIssue["zhyongrui/openclawcode#9053"].stage === "escalated"`
  - `runId: "intake-precheck-9053"`
- confirmed the copied-root operator left:
  - `pendingApprovals: []`
  - `queue: []`

Outcome:

- suitability is no longer a direct-CLI-only behavior
- the webhook route now makes the same class of high-risk issue visible to chat
  without first inviting a misleading approval decision

## Suitability Operator Surfaces

Finished the next Phase 3 slice by making the new suitability signal visible in
operator-facing status and inbox views instead of only in raw workflow JSON.

Implementation:

- extended plugin status snapshots to persist:
  - `suitabilityDecision`
  - `suitabilitySummary`
- updated `buildRunStatusMessage(...)` so workflow status text now includes:
  - `Suitability: ...`
  - `Suitability summary: ...`
- updated the precheck escalation snapshot path to persist suitability metadata
- updated `/occode-inbox` recent ledger rendering to add:
  - `suitability: <decision> | <summary>`

Validation:

- `pnpm exec vitest run src/openclawcode/testing/openclaw-plugin-integration.test.ts --pool threads`
- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- `pnpm build`

Outcome:

- suitability decisions are now visible through both operator JSON and chat-side
  status surfaces
- the next slice is no longer operator visibility; it is promoting this branch
  back to the long-lived `main` operator baseline and repeating the live proofs

## Published PR Number Follow-Up

Closed the verifier follow-up on issue `#56` by tightening how
`openclaw code run --json` derives published PR metadata.

Implementation:

- kept `publishedPullRequestNumber` behind the same
  `resolvePublishedPullRequest(...)` helper that already decides whether a PR is
  actually published
- documented the workflow invariant in code: the single persisted
  `draftPullRequest` object becomes the published PR metadata source once
  GitHub assigns a number or URL
- added a regression in `src/commands/openclawcode.test.ts` for the case where
  publication only records a URL, proving the top-level published number stays
  `null` instead of guessing a value

Validation:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- `pnpm build`

Outcome:

- `PR #57` now carries the verifier follow-up instead of leaving the field wired
  straight to raw draft PR metadata

## Long-Lived Main Baseline Revalidation

Closed the repaired low-risk command-layer proof back on the long-lived `main`
operator baseline.

Implementation and validation:

- pushed the `issue-56` follow-up commit to `origin/openclawcode/issue-56`
- merged `PR #57`
- closed issue `#56`
- fast-forwarded local `main` to merge commit
  `316ea9a5571159cc85e11f11cc4cccd87ffdd632`
- restored the long-lived operator config from temporary `triggerMode: "auto"`
  back to `triggerMode: "approve"`
- revalidated the main baseline with:
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
  - `pnpm build`
  - `./scripts/openclawcode-setup-check.sh --strict`

Outcome:

- the long-lived `main` operator baseline is back on the current validated code
- strict setup health now reports `15 pass`, `0 warn`, `0 fail` on the restored
  operator config

## Long-Lived Main Webhook Precheck Proof

Repeated the high-risk webhook suitability proof on the restored long-lived
`main` operator instead of only on the copied-root validation environment.

Live proof:

- created issue `#58`:
  `[Validation]: Webhook intake should precheck-escalate auth and secret issue`
- confirmed the long-lived operator persisted:
  - accepted delivery reason `precheck-escalated`
  - snapshot stage `escalated`
  - run id `intake-precheck-58`
  - `suitabilityDecision: "escalate"`
  - no `pendingApprovals` entry
  - no queued run
- closed issue `#58` after the validation pass to keep the repo tidy

Outcome:

- the suitability precheck behavior is now proved on both:
  - copied-root validation infrastructure
  - the long-lived `main` operator baseline

## Setup-Check Restart Window Retry

Closed one operator-ergonomics gap found while restoring the long-lived gateway:
`scripts/openclawcode-setup-check.sh --strict` could false-fail if it ran
during the gateway's short restart window after a config change.

Implementation:

- added retryable gateway reachability probing to
  `scripts/openclawcode-setup-check.sh`
- added retryable signed webhook probing to the same script
- exposed:
  - `OPENCLAWCODE_SETUP_RETRY_ATTEMPTS`
  - `OPENCLAWCODE_SETUP_RETRY_DELAY_SECONDS`
    so tests can keep the retry window short while production keeps a safer
    default
- added a regression in
  `src/openclawcode/testing/setup-check.test.ts` that forces the first gateway
  reachability check and first webhook probe to fail, then verifies the script
  succeeds on retry

Validation:

- `bash -n scripts/openclawcode-setup-check.sh`
- `pnpm exec vitest run src/openclawcode/testing/setup-check.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- `pnpm build`

Outcome:

- operator health checks no longer flap during brief self-restart windows after
  config changes
