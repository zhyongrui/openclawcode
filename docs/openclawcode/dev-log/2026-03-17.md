# 2026-03-17

## Upstream Refresh

Opened `sync/upstream-2026-03-17` from the previously validated
`sync/upstream-2026-03-16-refresh` branch and merged the latest
`upstream/main` (`0a93e22b37`).

Conflict hotspots this round:

- `src/plugins/bundled-dir.ts`
- `src/plugins/bundled-dir.test.ts`

Resolution kept both behaviors:

- preserve local watch-mode behavior that prefers source `extensions/`
- absorb upstream preference for staged `dist-runtime/extensions`
- keep tests deterministic without `process.chdir()` in Vitest workers

Validation passed:

- `pnpm exec vitest run src/plugins/bundled-dir.test.ts src/plugins/discovery.test.ts extensions/feishu/src/bot.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

## Command-Layer Queue

Implemented `#129` for `publishedPullRequestHasTitle` on top of the refreshed
sync branch.

Updated:

- `src/commands/openclawcode.ts`
- `src/commands/openclawcode.test.ts`
- `docs/openclawcode/run-json-contract.md`
- `docs/openclawcode/master-delivery-checklist.md`
- `docs/openclawcode/command-layer-backlog.md`

Followed immediately with the next published-PR convenience mirror:
`publishedPullRequestHasBody`.

Then completed the full draft-PR convenience-signal batch:

- `draftPullRequestHasNumber`
- `draftPullRequestHasUrl`
- `draftPullRequestHasOpenedAt`
- `draftPullRequestHasTitle`
- `draftPullRequestHasBody`

Then moved into issue metadata convenience signals:

- `issueHasBody`
- `issueBodyLength`
- `issueTitleLength`

Then completed the workspace convenience-signal batch:

- `workspaceHasPreparedAt`
- `workspaceHasWorktreePath`
- `workspaceRepoRootPresent`
- `workspaceBranchMatchesIssue`

Then moved into build convenience signals:

- `buildSummary`
- `buildSummaryPresent`
- `changedFilesPresent`
- `testCommandsPresent`
- `testResultsPresent`
- `notesPresent`

Then completed the next queue slice:

- `changedFileListStable`
- `scopeBlockedFilesPresent`
- `scopeBlockedFirstFile`
- `scopeBlockedLastFile`
- `verificationSummaryPresent`
- `verificationDecisionIsApprove`
- `verificationDecisionIsRequestChanges`
- `verificationDecisionIsEscalate`

Then completed the remaining verification/suitability decision-and-presence signals:

- `verificationFindingsPresent`
- `verificationMissingCoveragePresent`
- `verificationFollowUpsPresent`
- `suitabilitySummaryPresent`
- `suitabilityReasonsPresent`
- `suitabilityDecisionIsAutoRun`
- `suitabilityDecisionIsNeedsHumanReview`
- `suitabilityDecisionIsEscalate`

Then closed the remaining command-layer backlog families in one final batch:

- issue metadata:
  - `issueRepoOwnerPair`
  - `issueLabelListPresent`
  - `issueFirstLabel`
  - `issueLastLabel`
- run lifecycle:
  - `runHasUpdatedAt`
  - `runAgeSeconds`
  - `runLastStageEnteredAt`
  - `runHasHistory`
  - `runHasStageRecords`
  - `runHistoryTextPresent`
- failure diagnostics:
  - `failureDiagnosticsPresent`
  - `failureDiagnosticProviderPresent`
  - `failureDiagnosticModelPresent`
  - `failureDiagnosticSummaryPresent`
- execution spec:
  - `acceptanceCriteriaPresent`
  - `openQuestionsPresent`
  - `risksPresent`
  - `assumptionsPresent`
  - `testPlanPresent`
  - `scopeItemsPresent`
  - `outOfScopePresent`
- rerun context:
  - `rerunReasonPresent`
  - `rerunReviewSummaryPresent`
  - `rerunReviewUrlPresent`
  - `rerunReviewDecisionPresent`

That completed the current seed-ready `command-layer-backlog.md` queue for
stable top-level `openclaw code run --json` mirrors.

## Policy, Guardrails, And Operator Docs

Started the next repo-local productization slice after the command-layer queue:

- added `src/openclawcode/policy.ts`
  - stable low-risk suitability allowlist
  - stable high-risk suitability denylist
  - explicit build guardrail thresholds for:
    - changed-line budget
    - broad fan-out
    - generated-file detection
  - stable provider failure class tables for:
    - queue auto-pause
    - non-auto-pause retry classes
- added `openclaw code policy-show --json`
  - stable machine-readable policy snapshot
  - documented in `docs/openclawcode/policy-contract.md`
- extended suitability handling:
  - `runIssueWorkflow(...)` now accepts an explicit suitability override
  - `assessIssueSuitability(...)` now persists:
    - allowlist / denylist matches
    - original decision
    - override actor / reason
  - chat now supports `/occode-start-override`
- hardened merge policy:
  - auto-merge now blocks when a suitability override was used
  - auto-merge now blocks on generated files
  - auto-merge now blocks on large diffs
  - auto-merge now blocks on broad fan-out
- builder artifacts now persist build policy signals and mirror them through
  `openclaw code run --json`
- added focused regression coverage for:
  - suitability allowlist / denylist behavior
  - suitability overrides
  - build policy signals
  - large-worktree prompt deduping
  - chat-side suitability override queueing
  - operator status visibility for override state
  - policy-show JSON output
- published the next operator docs set:
  - `policy.md`
  - `support-matrix.md`
  - `fresh-host-install.md`
  - `upgrade-and-rotation.md`
  - `release-runbook.md`
  - `troubleshooting.md`
  - `proof-matrix.md`
  - `sample-operator-config.md`
  - `sample-automation-integration.md`

Validation for this slice:

- `pnpm exec vitest run src/openclawcode/roles/suitability.test.ts src/openclawcode/roles/agent-backed.test.ts src/openclawcode/testing/run-issue.test.ts src/integrations/openclaw-plugin/chatops.test.ts extensions/openclawcode/index.test.ts src/commands/openclawcode.test.ts --pool threads`

## Documentation Sweep

Closed the next documentation-only block of checklist items:

- added `chat-intake-styles.md`
- added `security-and-retention.md`
- added `sync-conflict-history.md`
- extended `fresh-host-install.md` with explicit proof-gate outputs
- updated README reading order to include the new canonical docs
- marked the completed fresh-host-doc, rotation-doc, conflict-history, prompt-style,
  retention-doc, and plan-alignment checklist items

The next remaining high-signal work after this doc sweep is still external:

- fresh-host live proof
- fallback-model live proof
- latest-baseline live proof matrix

## Validation-Pool Governance

Closed the repo-local validation-pool governance slice:

- added dedicated validation templates for:
  - timestamp-like string fields
  - URL string fields
  - enum-like string fields
- defined and documented the minimum open validation-pool targets:
  - `command-layer = 0`
  - `operator-docs = 1`
  - `high-risk-validation = 1`
- added `openclaw code seed-validation-issue --balanced`
- added `openclaw code reconcile-validation-issues --enforce-minimum-pool-size`
- extended `list-validation-issues --json` and
  `reconcile-validation-issues --json` with minimum-pool and seeding signals
- added `docs/openclawcode/validation-pool-maintenance.md`

Validation for this slice:

## Single-Login Bootstrap Proposal

Added a product-facing proposal for reducing fresh-host setup to one GitHub
login plus one repo choice:

- added `docs/openclawcode/single-login-bootstrap-proposal.md`
- updated `docs/openclawcode/README.md`
- updated `docs/openclawcode/fresh-host-install.md`
- updated `docs/openclawcode/codex-openclawcode-install.md`

The proposal records:

- the current manual gaps
- the desired split between `openclaw` and `openclawcode`
- the target `openclaw code bootstrap --repo owner/repo` flow
- the minimum user actions for a near-zero-touch fresh-host setup

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`

## Ambiguous Intake Drafts

Completed the next repo-local chat-intake productization slice:

- ambiguous one-line intake requests can now produce multiple scoped draft
  variants
- pending draft messages now surface those scoped variants directly in chat
- operators can now pick one variant with `/occode-intake-choose`
- manual edits still clear the generated scoped suggestions and become the new
  source of truth before `/occode-intake-confirm`

Validation for this slice:

- `pnpm exec vitest run extensions/openclawcode/index.test.ts src/openclawcode/testing/openclaw-plugin-store.test.ts src/commands/openclawcode.test.ts --pool threads --maxWorkers 1`

## Consistency And Security Audit

Ran the next repo-local completion audit against docs, contracts, and
operator-facing surfaces.

Confirmed locally:

- provider failures are diagnosable from:
  - saved run artifacts
  - top-level `run --json` mirrors
  - chat-visible status surfaces
- machine-readable contracts are versioned and documented for:
  - `run --json`
  - `policy-show --json`
  - `operator-status-snapshot-show --json`
  - validation-pool JSON output
  - release artifacts
- upstream sync repetition is now guarded by the documented refresh-branch
  workflow in `sync-promotion-runbook.md`
- stage-level human handoff coverage now includes:
  - execution-start gates
  - takeover / resume-after-edit
  - provider role reroute
  - deferred runtime reroute
- README reading order, release docs, promotion docs, and rollback docs were
  rechecked against the current command set
- real CLI output was spot-checked for:
  - `openclaw.mjs code policy-show --json`
  - `openclaw.mjs code operator-status-snapshot-show --json`
  - `openclaw.mjs code promotion-gate-show --json`
  - `openclaw.mjs code rollback-suggestion-show --json`

Still external after this audit:

- fresh-host live proofs
- fallback-model live proof
- latest-baseline proof matrix re-run

The current proof matrix is now explicitly recorded in:

- `docs/openclawcode/proof-matrix.md`
- `docs/openclawcode/dev-log/2026-03-17.md`

## Fresh-Host And Fallback Decisions

Closed the last repo-local decision/docs items before the remaining live proofs:

- added `fresh-host-readiness-audit.md`
- added `operator-proof-routine.md`
- made the fallback stance explicit in `support-matrix.md`
  - fallback remains proof-only until a second discoverable model and a real
    live fallback proof exist

## Live Proof Repairs And Re-Proofs

Closed the next live-proof slice on top of `sync/upstream-2026-03-17`:

- fixed the no-op rerun path so zero-change reruns now short-circuit before PR
  reuse
- stale open pull requests on the same issue branch are now closed before the
  run transitions to `completed-without-changes`
- added a regression test in `src/openclawcode/testing/run-issue.test.ts` for
  the stale-PR/no-op path
- merged the latest `origin/main` proof commit (`22e43ad3cb`, issue `#130`
  merged as `#135`) back into the sync branch and revalidated
- fixed `openclaw code run --json` for escalated-before-workspace runs so it no
  longer crashes when `workspace` is absent
- added a command test covering `stage = escalated` with null workspace fields

Validation for the repair slice:

- `pnpm exec vitest run src/openclawcode/testing/run-issue.test.ts --pool threads`
- `pnpm exec vitest run src/openclawcode/testing/run-issue.test.ts src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm build`

Live proof outcomes recorded on 2026-03-17:

- latest promoted baseline merged low-risk proof:
  - `origin/main` advanced to `22e43ad3cb`
  - commit subject: `feat: implement issue #130 (#135)`
- latest promoted baseline no-op proof:
  - run `zhyongrui-openclawcode-134-1773739257883`
  - final stage `completed-without-changes`
  - closed issue automatically after verification
- latest promoted baseline blocked/escalated proof:
  - run `zhyongrui-openclawcode-132-1773739717720`
  - final stage `escalated`
  - suitability escalated before workspace preparation
- fresh external-style host proofs already established earlier the same day:
  - zero-to-bind
  - zero-to-escalated

Remaining external-only proofs after this update:

- fresh external-style host zero-to-merged low-risk run
- fresh external-style host zero-to-rerun path
- second discoverable model and fallback proof
- blueprint-first end-to-end live proof

## Fresh-Host, Fallback, And Resume Proof Closure

Closed the remaining non-blueprint live-proof checklist items on the same
branch:

- confirmed a fresh external-style merged low-risk proof via run
  `zhyongrui-openclawcode-130-1773737938218`
  - final stage `merged`
  - PR `#135`
- confirmed a fresh external-style rerun proof via:
  - intentional failed run `zhyongrui-openclawcode-134-1773741281887`
  - rerun `zhyongrui-openclawcode-134-1773741499523`
  - final stage `completed-without-changes`
- confirmed a second discoverable model on the live operator host and recorded
  the result in the proof matrix
- recorded two long-lived fallback proofs:
  - negative auth-path proof:
    `zhyongrui-openclawcode-129-1773741126413`
  - fallback-engaged proof:
    `zhyongrui-openclawcode-134-1773741968419`
    - log showed fallback handoff from `anthropic/claude-opus-4-6` to
      `crs/gpt-5.4`
    - the run then hit a separate sandbox-path issue, so fallback remains
      proof-only rather than production-safe
- proved that a fresh AI session can resume from docs and memory without hidden
  context via an independent `codex exec` run
  - startup files read:
    - `AGENTS.md`
    - `SOUL.md`
    - `USER.md`
    - `memory/2026-03-17.md`
    - `memory/2026-03-16.md`
    - `openclawcode/AGENTS.md`
  - reconstructed branch and remaining checklist items correctly

After this proof closure pass, the only live-proof item still intentionally left
open is the blueprint-first end-to-end proof.

## Refreshed-Branch Live Proof

After syncing `sync/upstream-2026-03-17-refresh2` with `upstream/main`, ran a
fresh no-op live proof directly on the refreshed branch:

- run `zhyongrui-openclawcode-134-1773744106266`
- branch `openclawcode/issue-134-refresh2-noop-proof`
- final stage `completed-without-changes`
- draft PR intentionally skipped because no new commits were produced
- issue `#134` closed automatically after verification

This gave the refreshed branch a real post-sync operator proof in addition to:

- targeted conflict tests
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

## Local CLI Self-Hosting Fix

On local `main`, a direct `openclaw code run` smoke had exposed a real
self-hosting regression: direct CLI runs ignored the operator repo binding's
default `testCommands`, `builderAgent`, and `verifierAgent` when those flags
were omitted.

Fixed this with `00639cc4bf` (`fix: default cli runs to operator test commands`):

- `src/commands/openclawcode.ts` now resolves the operator repo binding from
  `${OPENCLAW_STATE_DIR}/openclaw.json`
- direct CLI runs now fall back to the bound repo's:
  - `testCommands`
  - `builderAgent`
  - `verifierAgent`
- `src/commands/openclawcode.test.ts` now includes a focused regression that
  covers this fallback path

Validation after the fix:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Then re-ran a real CLI smoke on local `main`:

- run `zhyongrui-openclawcode-129-1773746786842`
- final stage `ready-for-human-review`
- `buildResult.testCommands` correctly recorded:
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- `buildResult.testResults` correctly recorded:
  - `PASS pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- verifier completed and approved the run for human review
- the CLI process exited normally after emitting the final run JSON

## Additional Local Live Validation

Continued local live validation on `main` after the direct CLI fallback fix:

- ran high-risk validation issue `#132`
  - run `zhyongrui-openclawcode-132-1773748856241`
  - final stage `escalated`
  - suitability escalated before workspace preparation, build, or PR publication
  - confirmed the precheck-escalation path still holds locally
- then ran command-layer issue `#133`
  - the live run surfaced a remaining command-layer edge case in
    `runHasUpdatedAt`
  - existing code treated any non-null `updatedAt` as present
  - the validation issue expects the top-level convenience boolean to stay
    `false` for empty list-like signals while remaining `true` for non-empty
    strings
- fixed that edge case directly on `main` by tightening the boolean derivation
  in `src/commands/openclawcode.ts` and adding a narrow regression in
  `src/commands/openclawcode.test.ts`
- validation after the fix:
  - `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
    passed (`89` tests)
  - `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
    passed (`8` files / `93` tests)
  - `env PATH=/home/zyr/.local/node-v22.16.0/bin:$PATH pnpm build` passed

## Fresh-Branch Local Proof Follow-Up

Continued local proof work using a fresh issue branch name for `#133` so the
result would not be polluted by stale reusable-branch history:

- `openclaw code run --issue 133 --branch-name openclawcode/issue-133-fresh-local-proof`
  successfully prepared a brand-new worktree under:
  - `.openclawcode/worktrees/zhyongrui-openclawcode-133-1773749893480`
- this confirmed that the earlier `git merge main` failure on the long-lived
  `openclawcode/issue-133` branch was reusable-branch-specific, not a baseline
  failure on fresh issue branches
- the fresh-branch live run then exposed a more relevant operator-host
  portability problem:
  - embedded coder output attempted to invoke `bunx`
  - the operator host does not have Bun installed
  - that makes `bun`/`bunx` an unsafe assumption for fresh-host and new-repo
    testing
- tightened the builder prompt in `src/openclawcode/roles/agent-backed.ts` so
  it now explicitly warns the coding agent not to assume optional runtimes such
  as `bun` or `bunx` are installed on the workflow host
- added focused prompt coverage in
  `src/openclawcode/roles/agent-backed.test.ts`
- validation for this prompt hardening:
  - `pnpm exec vitest run src/openclawcode/roles/agent-backed.test.ts --pool threads`
    passed (`18` tests)
  - `env PATH=/home/zyr/.local/node-v22.16.0/bin:$PATH pnpm build` passed

## Fresh-Branch Local Proof Closure

Re-ran `#133` on a second fresh issue branch after the Bun portability warning
was added to the builder prompt:

- run `zhyongrui-openclawcode-133-1773750291674`
- branch `openclawcode/issue-133-fresh-local-proof-2`
- final stage `ready-for-human-review`

What this proved locally:

- fresh issue branches no longer depend on the stale reusable `issue-133`
  branch history
- builder completed on a fresh worktree without assuming `bun` or `bunx`
- host-side validation still ran and passed:
  - `PASS pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads`
- verifier completed and approved the run for human review
- the run stayed scoped and correctly produced:
  - `changedFiles: []`
  - `draftPullRequestHasNumber: false`
  - `verificationDecision: approve-for-human-review`

This closes the local readiness gate for moving on to a true fresh-machine,
new-repository test. Remaining unknowns are now external-environment concerns
only: fresh host install, fresh credentials, and the target repository's own
setup.

## Codex Fresh-Host Install Runbook

Added a dedicated operator-facing tutorial for using Codex to bootstrap
`openclawcode` on a fresh machine:

- `docs/openclawcode/codex-openclawcode-install.md`

The new runbook explicitly separates:

- what Codex should do automatically on the fresh host
- what the user must still do manually
- how to proceed after install into either:
  - CLI-only repo validation
  - chatops repo validation with `/occode-bind`

Linked the new runbook from:

- `docs/openclawcode/README.md`
- `docs/openclawcode/fresh-host-install.md`

Followed up the same day after a real fresh-host Codex attempt showed that the
first version was still too implicit about the source repository.

The runbook now states explicitly that Codex must clone:

- `https://github.com/zhyongrui/openclawcode.git`
- shorthand: `zhyongrui/openclawcode`

It also now warns Codex not to search for a separate public repository named
`openclawcode`, and to stop immediately if the checkout does not contain
`scripts/openclawcode-setup-check.sh`.

## Bootstrap MVP

Implemented the first real `openclaw code bootstrap --repo owner/repo` command.

What it now does automatically:

- parses `owner/repo`
- clones or attaches the target repository
- persists `GH_TOKEN`, `OPENCLAWCODE_GITHUB_REPO`, and a generated
  `OPENCLAWCODE_GITHUB_WEBHOOK_SECRET` into `~/.openclaw/openclawcode.env`
- writes or updates the bundled `openclawcode` repo entry in `openclaw.json`
- persists a bootstrap repo binding in `plugins/openclawcode/chatops-state.json`
- seeds `PROJECT-BLUEPRINT.md` in the target repo when missing
- refreshes role-routing, discovery, and stage-gate artifacts
- attempts to start the local gateway
- runs strict setup-check plus built-startup proof and returns the result
  through `--json`

What it still does not automate:

- GitHub webhook create/reuse
- OpenClaw-side GitHub login flow
- provider credential onboarding
- auto-detecting the real active chat target

Validation for this bootstrap slice:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
  passed (`92` tests)
