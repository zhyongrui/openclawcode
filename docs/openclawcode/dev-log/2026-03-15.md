## Workspace Repo Root

Consumed validation issue `#111` by mirroring the nested workspace repo-root
value into the stable top-level `openclaw code run --json` contract.

- `src/commands/openclawcode.ts`
  - now emits `workspaceRepoRoot` from `run.workspace?.repoRoot ?? null`
- `src/commands/openclawcode.test.ts`
  - now asserts `workspaceRepoRoot` for both populated and missing workspace
    metadata
- `docs/openclawcode/run-json-contract.md`
  - now documents `workspaceRepoRoot`

Validated with:

- `pnpm exec vitest run src/commands/openclawcode.test.ts --pool threads`
- `pnpm exec vitest run src/commands/openclawcode.test.ts src/openclawcode/testing/openclaw-plugin-store.test.ts src/openclawcode/testing/run-issue.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Live pool follow-up:

- `node dist/index.js code reconcile-validation-issues --repo-root /home/zyr/pros/openclawcode --close-implemented --json`
  auto-closed issue `#111` as implemented
- replenished the command-layer pool with:
  - `#112` for `workspacePreparedAt`

## Slow Integration Timeout Hardening

The first full-suite rerun after `workspaceRepoRoot` landed exposed three
time-based false reds on this host even though the targeted behavior was
correct:

- `src/openclawcode/testing/openclaw-plugin-store.test.ts`
  - `persists pending approvals and consumes them when approved`
  - `persists queue entries and statuses across store instances`
- `src/openclawcode/testing/run-issue.test.ts`
  - `publishes and merges when verification approves and merge is enabled`

Field note:

- each test passed in isolation, but each single-test run still took about
  19 seconds wall clock because transform/import time dominated on this host
- the default `testTimeout = 30000` in `vitest.openclawcode.config.mjs` left
  very little headroom for the serial full-suite run
- fix chosen:
  - keep the global timeout unchanged
  - raise only those three slow integration tests to `60_000`

This keeps genuine hangs visible while preventing fresh sessions from
misclassifying host-speed variance as a functional regression.
