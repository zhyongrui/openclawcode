# 2026-03-13

## Upstream Sync: `sync/upstream-2026-03-13`

Opened the next dedicated upstream sync branch immediately after the refreshed
provider-resilience and JSON-contract slices landed.

- branch:
  - `sync/upstream-2026-03-13`
- upstream head merged:
  - `80e7da92ce`
  - `fix: stabilize macos daemon onboarding`
- merge result:
  - clean merge with no manual conflict resolution required
  - merge commit:
    - `9ed7810068`

## Validation

Validated the new sync baseline before treating it as the next engineering
branch.

- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Both passed on the merged branch.

## Operational Result

- `sync/upstream-2026-03-13` is now the active feature branch
- `main` remains the long-lived Feishu operator baseline
- the next slices should continue from this new sync branch instead of the
  older `sync/upstream-2026-03-12-refresh`

## Next Slice

The sync itself did not change the current product bottleneck.

The next engineering decision remains:

1. add another discoverable model on the live operator host and run a real
   fallback proof for issue `#87`
2. or, if that is not possible yet, move to deeper provider/model diagnostics
   instead of more prompt trimming

## Persisted Failure Diagnostics

Promoted compact provider diagnostics from free-form failed-note text into the
persisted workflow artifact and stable CLI JSON contract.

- code:
  - `src/openclawcode/contracts/types.ts`
  - `src/openclawcode/app/run-issue.ts`
  - `src/commands/openclawcode.ts`
- tests:
  - `src/openclawcode/testing/run-issue.test.ts`
  - `src/commands/openclawcode.test.ts`
- docs:
  - `docs/openclawcode/run-json-contract.md`
  - `docs/openclawcode/README.md`
  - `docs/openclawcode/development-plan.md`
  - `docs/openclawcode/full-program-roadmap.md`

Result:

- failed workflow artifacts now persist `failureDiagnostics`
- `openclaw code run --json` now exposes:
  - `failureDiagnostics`
  - `failureDiagnosticsSummary`
- downstream tools and future AI sessions no longer need to parse
  `run.history.at(-1)` just to recover provider/model prompt diagnostics

## Operator Failure Diagnostics Surface

Extended the same structured diagnostics into the chat-visible operator state.

- code:
  - `src/integrations/openclaw-plugin/store.ts`
  - `src/integrations/openclaw-plugin/chatops.ts`
  - `extensions/openclawcode/index.ts`
- tests:
  - `src/openclawcode/testing/openclaw-plugin-integration.test.ts`
  - `extensions/openclawcode/index.test.ts`

Result:

- status snapshots now persist `failureDiagnostics`
- `/occode-status` can show a structured diagnostics line for failed runs
- `/occode-inbox` recent-ledger entries can show the same diagnostics line
- provider pause context and structured failure diagnostics now coexist instead
  of forcing operators to choose one or the other

## Setup-Check Readiness Output

Extended `scripts/openclawcode-setup-check.sh --json` so fresh-host automation
does not need to reverse-engineer rollout state from raw pass or warn counts.

- code:
  - `scripts/openclawcode-setup-check.sh`
- tests:
  - `src/openclawcode/testing/setup-check.test.ts`
- docs:
  - `docs/openclawcode/operator-setup.md`
  - `docs/openclawcode/README.md`
  - `docs/openclawcode/full-program-roadmap.md`

Result:

- setup-check JSON now includes:
  - `readiness.basic`
  - `readiness.strict`
  - `readiness.lowRiskProofReady`
  - `readiness.fallbackProofReady`
  - `readiness.promotionReady`
  - `readiness.nextAction`
- human-readable output now prints the same readiness summary and next action at
  the end of the run

## Bounded Builder And Verifier Timeouts

The next refreshed-branch live proof exposed a new blocker distinct from the
earlier provider `HTTP 400` failures.

- proof target:
  - issue `#85`
- proof state dir:
  - `.openclawcode-proof-sync-20260313`
- observed live symptom before the fix:
  - run `zhyongrui-openclawcode-85-1773414881879` stalled in `building`
  - saved history stopped at `Build started`
  - no bounded failed artifact surfaced until the outer proof wrapper timed out

Diagnosis:

- the embedded runner already supported `timeoutSeconds`
- `AgentBackedBuilder` and `AgentBackedVerifier` were not forwarding that field
  into `agentRunner.run(...)`
- `openclaw code run` was also not setting workflow-specific timeout defaults,
  so live proofs inherited the host's broader generic agent timeout

Implemented fix:

- `AgentBackedBuilder` now forwards `timeoutSeconds`
- `AgentBackedVerifier` now forwards `timeoutSeconds`
- `openclaw code run` now sets workflow defaults:
  - builder: `300` seconds
  - verifier: `180` seconds
- operator overrides are now available through:
  - `OPENCLAWCODE_BUILDER_TIMEOUT_SECONDS`
  - `OPENCLAWCODE_VERIFIER_TIMEOUT_SECONDS`
- generic non-provider workflow failures now also persist
  `failureDiagnostics.summary`, so timeout-style failures remain visible in
  structured workflow artifacts and operator status

Validation:

- `pnpm exec vitest run src/openclawcode/roles/agent-backed.test.ts src/commands/openclawcode.test.ts src/openclawcode/testing/run-issue.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Additional test-harness hardening during this slice:

- widened the explicit timeout budget for two known heavy full-suite cases:
  - `src/openclawcode/testing/run-issue.test.ts`
  - `src/openclawcode/testing/orchestrator.test.ts`
- reason:
  - both cases pass in isolation, but the refreshed upstream baseline can push
    them past Vitest's default `30_000ms` budget during the full
    `vitest.openclawcode.config.mjs` run

## Refreshed-Branch Low-Risk Merged Proof

Re-ran the refreshed sync branch against a real low-risk validation issue after
the timeout hardening landed.

- issue:
  - `#85`
  - `[Feature]: Expose scopeCheckSummaryPresent in openclaw code run --json output`
- proof state dir:
  - `/tmp/openclawcode-proof-sync-20260313-85`
- run:
  - `zhyongrui-openclawcode-85-1773416913744`
- PR:
  - `#88`
- final outcome:
  - merged automatically into `sync/upstream-2026-03-13`

Notable result:

- the refreshed branch no longer needed another provider-focused recovery slice
  before proving a low-risk merge
- the timeout hardening changed the risk profile from "this proof can hang
  indefinitely in `building`" to "the workflow completes normally and can still
  surface a bounded failure if a future provider stall returns"

Post-proof validation on the merged branch head:

- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Both passed again after fast-forwarding the local branch to the `PR #88` merge
commit.

## Promotion Back To `main`

Promoted `sync/upstream-2026-03-13` back to `main` after the refreshed branch
cleared both test validation and a real merged low-risk proof.

- local branch:
  - `main`
- promotion result:
  - fast-forwarded to the refreshed branch head
  - pushed to `origin/main`

This restores `main` as both:

- the active engineering baseline
- the intended long-lived Feishu operator target branch

## No-Op Completion Terminal Stage

The first direct proof on promoted `main` found a real product gap rather than
another provider issue.

- proof issue:
  - `#44`
- first direct run result:
  - the run finished at `ready-for-human-review`
  - verification approved the work as a no-op
  - no PR was opened
  - the issue stayed open

Implemented follow-up:

- added a new terminal workflow stage:
  - `completed-without-changes`
- workflow now recognizes the "no commits between base and issue branch"
  result as a first-class no-op completion signal
- when verification approves that state, the workflow:
  - records a no-op completion note
  - transitions to `completed-without-changes`
  - closes the GitHub issue automatically

Validation for the slice:

- `pnpm exec vitest run src/openclawcode/testing/run-issue.test.ts src/commands/openclawcode.test.ts extensions/openclawcode/index.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs --pool threads --maxWorkers 1`
- `pnpm build`

Direct proof after the fix:

- proof issue:
  - `#44`
- proof run:
  - `zhyongrui-openclawcode-44-1773418941601`
- final stage:
  - `completed-without-changes`
- final effect:
  - issue `#44` closed automatically at `2026-03-13T16:28:24Z`
  - no PR was needed because no new commits were produced against `main`

## Promoted-`main` Gateway Startup Regression

The same promotion exposed a remaining operator-only blocker on this host.

- documented command:
  - `node dist/index.js gateway run --bind loopback --port 18789`
- observed behavior:
  - plugin initialization logs appeared
  - no TCP listener bound to `127.0.0.1:18789`
- contrast:
  - direct `tsx`-driven `openclaw code run ...` proofs on `main` still worked

That makes the next slice explicit:

- keep using direct workflow proofs for code validation when needed
- repair the repo-local built gateway startup path before trusting the
  long-lived Feishu operator again
